# DD 人工测试

> 当前批次：**Windows 881×657 无边框 + 微信式桌面 UI 第二轮 + 头像 + 聊天内通话 + 输入性能回归**
> 更新时间：2026-08-08 09:03（UTC+8）
> 原则：只测真实窗口、真实浏览器、真实输入法、真实设备和真实音视频。Go / Flutter / OpenAPI 能自动测的项目不要重复点。

---

## 0. 本轮明确不要测

以下仍属于后续阶段，不要当成本轮 Bug 重复报：

```text
Sticker / 图片表情
GIF 真正发送
图片消息
文件消息
语音条录制 / 播放
独立“已送达”状态
群聊
朋友圈
```

聊天内的**语音通话 / 视频通话**本轮已经接入，但当前仍复用 P0 Call API + LiveKit 媒体链，不代表 P7 正式持久化通话模块已经完成。

---

# 1. 启动环境

确保 Docker Desktop 已启动，然后普通 PowerShell：

```powershell
cd C:\Users\admin\Desktop\复刻微信

if (Test-Path .\.data\auth-dev.json) {
    powershell -ExecutionPolicy Bypass -File .\scripts\stop-auth-dev.ps1
}

powershell -ExecutionPolicy Bypass -File .\scripts\run-auth-dev.ps1
```

本轮继续复用两个已经是好友的账号，例如：

```text
Windows / Web = Alice
Android       = Bob
```

不要重新注册、不要重复加好友，除非旧数据已经没了。

---

# 2. Windows 原生窗口验收【新增重点】

双击根目录：

```text
DD-Windows.lnk
```

### 2.1 默认尺寸 / 外观

- [ ] 第一次打开的默认客户端尺寸约为 **881×657**。
- [ ] 没有 Windows 系统默认的白色/黑色原生标题栏。
- [ ] 顶部只有 DD 自己的窄窗口栏。
- [ ] 左侧导航约 60px，中间会话栏约 248px，右侧是聊天区。
- [ ] 默认尺寸下不会退化成手机单栏布局。
- [ ] 没有横向 overflow / BOTTOM OVERFLOWED。

### 2.2 窗口行为

依次操作：

- [ ] 按住 DD 顶部空白处能拖动窗口。
- [ ] 双击顶部能最大化，再双击能还原。
- [ ] 四边和四角都能缩放。
- [ ] 最小化正常。
- [ ] 最大化正常，并且**不会盖住 Windows 任务栏**。
- [ ] 关闭按钮正常退出。
- [ ] 顶部图钉按钮可以切换“窗口置顶 / 取消置顶”。
- [ ] 窗口缩小到最低尺寸附近时，主要 UI 不炸裂。

---

# 3. 桌面 UI 与右键菜单

## 3.1 消息页

- [ ] 左侧是窄导航，不像手机底栏直接拉宽。
- [ ] 会话栏顶部是“搜索 + `+`”，没有以前占地方的“消息”大标题。
- [ ] 点击 `+` 能看到：`发起群聊 / 添加朋友 / 写笔记`；未开发项明确显示开发中，不是假按钮。
- [ ] 点击“添加朋友”能切到联系人区域。
- [ ] 会话列表密度、头像、未读、时间接近微信桌面端的紧凑布局。

## 3.2 会话右键

对一个会话点右键：

- [ ] 弹出的是紧凑浮层菜单，而不是大号 Bottom Sheet。
- [ ] 图标在左、文字在右，菜单项间距紧凑。
- [ ] 能看到置顶 / 取消置顶。
- [ ] 能看到免打扰相关操作。
- [ ] 有未读消息时能看到“标为已读”。
- [ ] 视觉和操作心智接近 Telegram 右键菜单，**不要出现 WhatsApp 式设计**。

## 3.3 消息右键

对自己和对方的文字消息分别右键：

- [ ] 回复。
- [ ] 复制文字。
- [ ] 自己的消息才出现“撤回”。
- [ ] “仅本地删除”使用红色危险操作样式。
- [ ] 对方消息不会错误出现“撤回”。
- [ ] 菜单位置靠近鼠标，不会跑到屏幕角落。

---

# 4. 联系人与设置页

## 4.1 通讯录

点击左侧“联系人”：

- [ ] 桌面端是**左侧通讯录目录 + 右侧详情**，不是移动端四个 Tab 横向放大。
- [ ] 左侧能看到搜索、通讯录管理、新的朋友、黑名单、星标朋友/联系人。
- [ ] 点击联系人后右侧显示头像、昵称、Handle、标签等详情。
- [ ] 搜索联系人不会明显卡顿。
- [ ] 360×640 Android 仍然保持适合触屏的单栏/Tab，不要被桌面重构破坏。

## 4.2 设置

进入“我的”→ 设置：

- [ ] Windows/Web 是左侧设置分类 + 右侧内容。
- [ ] 分类至少有：账号与存储、通用、快捷键、通知、插件、关于 DD。
- [ ] 资料、隐私开关、设备管理仍真实可操作。
- [ ] 设置页不再是一堆大 Material Card。

---

# 5. 头像【新增重点】

在“我的”页面点击自己的头像，选择：

```text
更换头像
```

选择一个 **JPEG / PNG / WebP，且小于 2 MiB** 的图片。

### 验收

- [ ] 上传成功后“我的”立即显示新头像。
- [ ] Windows 左侧导航头像立即刷新。
- [ ] 自己发出的聊天消息右侧显示新头像。
- [ ] 另一个账号重新进入会话/联系人后能看到新头像。
- [ ] 会话列表、聊天、联系人使用的是同一张头像，不会各显示各的。
- [ ] 重启客户端后头像仍存在。

然后测试：

```text
移除头像
```

- [ ] 移除后回退为首字占位。
- [ ] 重启后仍保持已移除状态。

负向测试只做一遍：

- [ ] 选择超过 2 MiB 图片时客户端明确拒绝。
- [ ] SVG 不应该被文件选择器当头像接受。

---

# 6. 连续发送 / 输入焦点【新增重点】

Windows 和 Web 分别测试。

点击输入框，连续执行：

```text
第一条 Enter
第二条 Enter
第三条 Enter
第四条 Enter
第五条 Enter
```

要求每条发送后**不要再用鼠标点输入框**。

- [ ] 第一条发出后光标仍在输入框。
- [ ] 五条可以连续只靠键盘发完。
- [ ] `Enter = 发送`。
- [ ] `Shift+Enter = 换行`。
- [ ] 中文输入法拼音还在候选态时按 Enter 只确认候选词，不提前把半截拼音发出去。
- [ ] 发送时输入框不会突然变灰/失焦再亮回来。

---

# 7. Android 输入框流畅度【新增重点】

Android 打开一个有较多历史消息的聊天。

重复 10 次：

```text
点输入框 → 键盘弹出 → 输入几个字 → 收起键盘
```

再快速输入连续发送 20 条短消息。

### 验收

- [ ] 第一次点击输入框不出现明显“卡一下才弹键盘”。
- [ ] 键盘升起/收起时消息区没有明显连续掉帧或闪跳。
- [ ] 输入文字时不卡顿。
- [ ] 发送后键盘不应被关闭再重新拉起。
- [ ] 连续发送 20 条过程中 UI 不冻结。
- [ ] 消息不重复、不乱序。

> 自动代码层已经移除发送时 TextField enabled 切换、减少草稿全局通知、移除聊天区 MediaQuery 宽度依赖并加入消息 RepaintBoundary。但 **60fps 是否真正达到必须以这条真机体验为准**，如果仍卡就继续做 Flutter Profile，不会凭单测宣布“性能完成”。

---

# 8. 聊天内语音 / 视频通话【新增重点】

Windows Alice 与 Android Bob 都保持登录在线。

## 8.1 语音通话

Alice 打开 Bob 聊天页，点击顶部电话图标。

- [ ] Alice 进入正式通话页，不出现房间 URL、identity 等 Debug 表单。
- [ ] Bob 不需要先进入旧“通话调试页”，也能自动收到来电界面。
- [ ] Bob 可接听。
- [ ] 接通后双方能听到声音。
- [ ] 静音 / 取消静音可用。
- [ ] 任意一方挂断，另一端同步结束。

## 8.2 视频通话

Alice 再点击聊天顶部摄像机图标。

- [ ] Bob 自动收到视频来电。
- [ ] 接听后能看到双方视频轨道。
- [ ] 摄像头开关可用。
- [ ] 麦克风开关可用。
- [ ] 挂断同步正常。

## 8.3 信令生命周期

- [ ] 登录后直接收来电，不要求先手动打开通话功能。
- [ ] 退出登录再登录，不出现重复来电弹窗。
- [ ] 关闭 Windows DD 后不会继续残留通话窗口或幽灵连接。

> 当前仍复用 P0 Call/LiveKit。公网 TURN/TLS、正式 P7 数据库存储、多设备忙线仲裁仍是后续任务。

---

# 9. 置顶 / 免打扰 / 已读跨端回归

Web 对某个会话：

```text
右键 → 置顶
```

- [ ] 会话立刻排到顶部。
- [ ] 刷新 Web 后仍置顶。
- [ ] Windows / Android 同步后能看到相同状态。

再取消置顶：

- [ ] 三端最终一致。

免打扰同样测试：

- [ ] 8 小时免打扰保存。
- [ ] 刷新后仍存在。
- [ ] 取消后跨端最终一致。

已读：

- [ ] Bob 打开 Alice 的聊天后，Alice 自己最后相关消息出现“已读”。
- [ ] Bob 关闭“已读回执”后，Alice 不再获得 Bob 的精确已读位置。

---

# 10. 本轮通过标准

下面全部通过才算这一轮真人验收完成：

```text
Windows 881×657 无边框窗口行为正常
桌面消息/通讯录/设置结构符合微信式心智
Telegram 式右键菜单可用
头像上传/读取/移除三端一致
发送后输入焦点不丢
Android 键盘交互主观无明显卡顿
聊天页直接发起语音/视频并能接来电
Web 置顶/免打扰跨端一致
已读回执符合隐私开关
```

如果某一项失败，直接在对应勾选项下面写现象即可，不需要重新跑已经通过的整份清单。
