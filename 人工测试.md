# DD 人工测试

> 当前批次：**晚间统一验收：未读/通知/Android IME + Windows 原生窗口 + 头像/资料 + LiveKit 通话 + P5 图片/GIF/表情/文件/语音媒体链**
> 更新时间：2026-08-08 14:46（UTC+8）
> 自动门禁：本轮 Flutter 61/61 PASS；Go test/vet PASS；OpenAPI recommended-strict PASS；git diff --check PASS；Web Release 与 Android Debug APK 构建 PASS。Windows Release 仍受现有 Flutter/Windows 中文路径/生成链问题影响，不能冒充通过。
> 原则：只测真实窗口、真实浏览器、真实输入法、真实设备和真实音视频。Go / Flutter / OpenAPI 能自动测的项目不要重复点。

---

## 0. 本轮明确不要测

以下仍属于后续阶段，不要当成本轮 Bug 重复报：

```text
独立“已送达”状态
群聊
朋友圈
扫码
正式杀进程 Push（当前仅进程存活时本地通知）
```

聊天内的**语音通话 / 视频通话**本轮已经接入，但当前仍复用 P0 Call API + LiveKit 媒体链，不代表 P7 正式持久化通话模块已经完成。

本轮新增可验收媒体链：**图片、GIF、本地图片表情、文件、语音条**。这些资源走私有对象存储预签名上传/完成校验/授权短期下载；文件客户端当前主动限制 256 MiB，因为现阶段仍是内存读取上传，避免按服务端 2 GiB 上限直接把客户端内存撑爆。

---

# 1. 启动环境

确保 Docker Desktop 已启动，然后普通 PowerShell。**截至 12:14 当前机器仍是 10:23 启动的旧 auth-dev，LiveKit 没有运行，所以这次必须先停旧环境再启动新版：**

```powershell
cd C:\Users\admin\Desktop\复刻微信

if (Test-Path .\.data\auth-dev.json) {
    powershell -ExecutionPolicy Bypass -File .\scripts\stop-auth-dev.ps1
}

powershell -ExecutionPolicy Bypass -File .\scripts\run-auth-dev.ps1
```

本轮继续复用两个已经是好友的账号，例如：

```text
Windows / Web = Alice
Android       = Bob
```

不要重新注册、不要重复加好友，除非旧数据已经没了。

---

# 2. Windows 原生窗口验收【新增重点】

双击根目录：

```text
DD-Windows.lnk
```

### 2.1 默认尺寸 / 外观

- [x] 第一次打开的默认客户端尺寸约为 **881×657**。
- [ ] 没有 Windows 系统默认的白色/黑色原生标题栏。
- [ ] 顶部 DD 自绘标题栏约 **28px**，比上一版更矮。
- [ ] Windows 11 下窗口四角有系统圆角，不是硬直角。
- [ ] 标题栏拖动/悬停/最大化过程中没有黑块、白块、主题色块覆盖。
- [x] 左侧导航约 60px，中间会话栏约 248px，右侧是聊天区。
- [x] 默认尺寸下不会退化成手机单栏布局。
- [x] 没有横向 overflow / BOTTOM OVERFLOWED。

### 2.2 窗口行为

依次操作：

- [x] 按住 DD 顶部空白处能拖动窗口。
- [x] 双击顶部能最大化，再双击能还原。
- [ ] 左 / 右 / 上 / 下四边都能缩放。
- [ ] 左上 / 右上 / 左下 / 右下四角都能斜向缩放，鼠标光标方向正确。
- [x] 最小化正常。
- [x] 最大化正常，并且**不会盖住 Windows 任务栏**。
- [x] 关闭按钮正常退出。
- [x] 顶部图钉按钮可以切换“窗口置顶 / 取消置顶”。
- [ ] 窗口缩小到最低尺寸附近时，主要 UI 不炸裂。

---

# 3. 桌面 UI 与右键菜单

## 3.1 消息页

- [x] 左侧是窄导航，不像手机底栏直接拉宽。
- [x] 会话栏顶部是“搜索 + `+`”，没有以前占地方的“消息”大标题。
- [x] 点击 `+` 能看到：`发起群聊 / 添加朋友 / 写笔记`；未开发项明确显示开发中，不是假按钮。
- [x] 点击“添加朋友”能切到联系人区域。
- [x] 会话列表密度、头像、未读、时间接近微信桌面端的紧凑布局。

## 3.2 会话右键

对一个会话点右键：

- [x] 弹出的是紧凑浮层菜单，而不是大号 Bottom Sheet。
- [x] 图标在左、文字在右，菜单项间距紧凑。
- [x] 能看到置顶 / 取消置顶。
- [x] 能看到免打扰相关操作。
- [ ] 有未读消息时能看到红色未读角标，并且右键能看到“标为已读”。
- [x] 视觉和操作心智接近 Telegram 右键菜单，**不要出现 WhatsApp 式设计**。

## 3.3 消息右键

对自己和对方的文字消息分别右键：

- [x] 回复。
- [x] 复制文字。
- [x] 自己的消息才出现“撤回”。
- [ ] “仅本地删除”使用红色危险操作样式。
- [x] 对方消息不会错误出现“撤回”。
- [ ] 菜单位置靠近鼠标，不会跑到屏幕角落。

---

# 4. 联系人与设置页

## 4.1 通讯录

点击左侧“联系人”：

- [x] 桌面端是**左侧通讯录目录 + 右侧详情**，不是移动端四个 Tab 横向放大。
- [x] 左侧能看到搜索、通讯录管理、新的朋友、黑名单、星标朋友/联系人。
- [x] 点击联系人后右侧显示头像、昵称、Handle、标签等详情。
- [x] 搜索联系人不会明显卡顿。
- [x] 360×640 Android 仍然保持适合触屏的单栏/Tab，不要被桌面重构破坏。

## 4.2 设置

进入“我的”→ 设置：

- [x] Windows/Web 是左侧设置分类 + 右侧内容。
- [x] 分类至少有：账号与存储、通用、快捷键、通知、插件、关于 DD。
- [x] 资料、隐私开关、设备管理仍真实可操作。
- [x] 设置页不再是一堆大 Material Card。

---

# 5. 头像【新增重点】

在“我的”页面点击自己的头像，选择：

```text
更换头像
```

直接选择普通手机原图即可，**源文件可以超过 2 MiB，也可以大于 2048×2048**。客户端会在后台处理并压缩成安全 JPEG 后再上传。为防恶意图片把设备内存打爆，仍保留很宽松的安全边界：源文件最大 64 MiB、解码画布最大约 70MP；这不是原来 2 MiB 的头像上传限制。

### 验收

- [x] 上传成功后“我的”立即显示新头像。
- [x] Windows 左侧导航头像立即刷新。
- [x] 自己发出的聊天消息右侧显示新头像。
- [x] 另一个账号重新进入会话/联系人后能看到新头像。
- [x] 会话列表、聊天、联系人使用的是同一张头像，不会各显示各的。
- [x] 重启客户端后头像仍存在。

然后测试：

```text
移除头像
```

- [x] 移除后回退为首字占位。
- [x] 重启后仍保持已移除状态。

额外测试：

- [ ] 选一张明显超过 2 MiB 的手机原图，能够自动压缩并成功上传，界面不会因压缩明显卡死。
- [ ] SVG 不应该被文件选择器当头像接受。
- [ ] 损坏/无法解析的图片应提示失败，不得崩溃。

---

# 6. 连续发送 / 输入焦点【新增重点】

Windows 和 Web 分别测试。

点击输入框，连续执行：

```text
第一条 Enter
第二条 Enter
第三条 Enter
第四条 Enter
第五条 Enter
```

要求每条发送后**不要再用鼠标点输入框**。

- [x] 第一条发出后光标仍在输入框。
- [x] 五条可以连续只靠键盘发完。
- [x] `Enter = 发送`。
- [x] `Shift+Enter = 换行`。
- [x] 中文输入法拼音还在候选态时按 Enter 只确认候选词，不提前把半截拼音发出去。
- [x] 发送时输入框不会突然变灰/失焦再亮回来。

---

# 7. Android 输入框流畅度【新增重点】

Android 打开一个有较多历史消息的聊天。

重复 10 次：

```text
点输入框 → 键盘弹出 → 输入几个字 → 收起键盘
```

再快速输入连续发送 20 条短消息。

### 验收

- [x] 第一次点击输入框不出现明显“卡一下才弹键盘”。
- [ ] 键盘升起/收起时消息区没有明显连续掉帧或闪跳；本轮已改为 `adjustNothing + 合成层 Transform`，不再让长消息列表跟着 IME 每一帧重新 layout。
- [x] 输入文字时不卡顿。
- [x] 发送后键盘不应被关闭再重新拉起。
- [x] 连续发送 20 条过程中 UI 不冻结。
- [x] 消息不重复、不乱序。
- [ ] 键盘打开后，点击聊天消息区空白位置可以直接收起键盘。
- [ ] 长按消息的 Android 菜单是紧凑圆角 Action Sheet，不再是默认大 Material 菜单。
- [ ] Android 的“仅本地删除”文字和图标为红色。
- [ ] Android 有未读会话时长按会话，可以看到“标为已读”。

> 自动代码层已经移除发送时 TextField enabled 切换、减少草稿全局通知、移除聊天区 MediaQuery 宽度依赖并加入消息 RepaintBoundary；第二轮进一步取消 Android 系统 `adjustResize`，键盘动画只通过合成层平移聊天表面。**60fps 是否真正达到仍必须以这条真机体验为准**。

---

# 8. 未读、资料页与通知【新增重点】

## 8.1 已读 / 未读失焦

Windows 停留在左侧“联系人”页，不要打开消息页；Android 给 Windows 账号发一条消息：

- [ ] Windows **不能**因为后台还挂着旧聊天 Widget 就自动把消息标为已读。
- [ ] Android 发送方此时不应看到错误的“已读”。
- [ ] Windows 左侧“消息”入口出现红色未读角标，会话本身也保留红色未读数。
- [ ] Windows 真正切回“消息”并打开对应会话后，才推进已读。

## 8.2 对方资料

- [ ] 点击聊天里对方头像会进入“个人资料”。
- [ ] 能看到头像、昵称、Handle、简介和当前关系。
- [ ] 返回聊天后正常继续收发消息。

## 8.3 软件内 / 系统通知

- [ ] DD 在前台，但当前不在该聊天时，新消息出现软件内提醒（发送人 + 消息预览）。
- [ ] Windows DD 最小化/切后台但进程仍运行时，新消息出现 Windows 系统通知。
- [ ] Android DD 切后台但进程仍运行时，新消息出现 Android 系统通知；首次运行会申请通知权限。
- [ ] 免打扰会话不会弹这类通知。

> **本轮不把“彻底杀掉 Android/Windows 进程后仍可靠推送”算完成。** 杀进程可靠通知需要 P10 Push Worker + Android FCM / 桌面后台推送链；当前本地通知依赖客户端进程/实时连接仍存活。

## 8.4 偶发“消息发送被服务端拒绝”回归

持续登录一段时间、切后台再回来后发送消息：

- [ ] 不再因为 Access Token 到期直接把消息标成“服务端拒绝”。
- [ ] Token 到期时消息应留在待发送队列，客户端自动刷新会话并用原 `clientMessageId` 重试。
- [ ] 真正因为拉黑/隐私/非法引用失败时，提示具体原因，而不是笼统“服务端拒绝”。

---

# 9. 聊天内语音 / 视频通话【新增重点】

**先完整重启一次本轮开发环境。** 当前 10:23 启动的旧 `auth-dev` 没有 LiveKit，不能继续拿旧进程测试：

```powershell
cd C:\Users\admin\Desktop\复刻微信
powershell -ExecutionPolicy Bypass -File .\scripts\stop-auth-dev.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\run-auth-dev.ps1
```

新脚本会同时启动 PostgreSQL / Mailpit / Redis / **LiveKit**，并把 LiveKit signal / RTC / TURN 暴露到自动检测到的局域网 IP。然后 Windows Alice 与 Android Bob 都保持登录在线。

## 9.1 语音通话

Alice 打开 Bob 聊天页，点击顶部电话图标。

- [ ] Alice 进入正式通话页，不出现房间 URL、identity 等 Debug 表单。
- [x] Bob 不需要先进入旧“通话调试页”，也能自动收到来电界面。
- [ ] Bob 可接听。
- [ ] 接通后双方能听到声音。
- [ ] 静音 / 取消静音可用。
- [ ] 任意一方挂断，另一端同步结束。
- [ ] 快速连续点两次挂断也不会再出现 `INVALID_CALL_STATE / 通话状态已变化`，挂断接口是幂等的。

## 9.2 视频通话

Alice 再点击聊天顶部摄像机图标。

- [ ] Bob 自动收到视频来电。
- [ ] 接听后能看到双方视频轨道。
- [ ] 摄像头开关可用。
- [ ] 麦克风开关可用。
- [ ] 挂断同步正常。

## 9.3 信令生命周期

- [x] 登录后直接收来电，不要求先手动打开通话功能。
- [ ] 退出登录再登录，不出现重复来电弹窗。
- [ ] 关闭 Windows DD 后不会继续残留通话窗口或幽灵连接。

> 当前仍复用 P0 Call/LiveKit。公网 TURN/TLS、正式 P7 数据库存储、多设备忙线仲裁仍是后续任务。

---

# 10. 置顶 / 免打扰 / 已读跨端回归

Web 对某个会话：

```text
右键 → 置顶
```

- [x] 会话立刻排到顶部。
- [x] 刷新 Web 后仍置顶。
- [x] Windows / Android 同步后能看到相同状态。

再取消置顶：

- [x] 三端最终一致。

免打扰同样测试：

- [x] 8 小时免打扰保存。
- [x] 刷新后仍存在。
- [x] 取消后跨端最终一致。

已读：

- [ ] Bob 打开 Alice 的聊天后，Alice 自己最后相关消息出现“已读”。
- [ ] Bob 关闭“已读回执”后，Alice 不再获得 Bob 的精确已读位置。

---

# 11. P5 媒体消息统一验收【新增】

任选 Windows / Web / Android 两端互发，另一端观察实时到达和重启后的历史恢复。

## 11.1 图片 / GIF / 图片表情

- [ ] `+ → 图片` 支持一次多选，最多 30 张；多张应按选择顺序逐张压缩/上传/发送，不乱序。
- [ ] 多图发送过程中输入区上方显示 `当前张/总张数 + 真实百分比`，不是假进度；连续大图时客户端不会因为同时解码多张而明显爆内存。
- [ ] 单张大图客户端压缩后发送，聊天中按原比例显示缩略图。
- [ ] 点击图片可查看大图；另一端能正常加载，不需要公开 Bucket。
- [ ] `+ → GIF` 可发送本地 GIF，另一端显示正常。
- [ ] `+ → 表情` 可发送本地 PNG/WebP/GIF 图片表情，气泡不出现多余白底。
- [ ] 断网发送/恢复后不会重复消息，失败项可重试。

## 11.2 文件

- [ ] `+ → 文件` 已不是“开发中”，可选择并发送普通文件。
- [ ] 上传文件时输入区上方出现真实百分比进度条，进度持续前进，不是假动画。
- [ ] 上传过程中点进度条右侧 `×` 可取消，取消后聊天页保持可操作，不会留下半条消息。
- [ ] 发送 300 MiB～1 GiB 大文件时客户端内存不会按文件大小等比例暴涨；当前已改为双遍流式 SHA-256 + 流式上传。
- [ ] 服务端 2 GiB 上限生效；超过 2 GiB 客户端明确拒绝。
- [ ] 对端文件气泡显示文件名和大小。
- [ ] 点击文件气泡能取得授权的临时下载地址并复制到剪贴板。
- [ ] 非会话成员无法通过旧 mediaId 获取下载地址。

## 11.3 语音条

- [ ] Android 长按麦克风开始录制，上滑取消可用；松开发送。
- [ ] Windows/Web 点击开始、再次点击结束并发送。
- [ ] 对端显示语音条和时长，点击可播放/暂停。
- [ ] 第一次播放后出现“已听”状态；完全退出并重新打开 DD 后该状态仍保留。
- [ ] 倍速切换可用，多条语音连续操作不会串音或同时播放。
- [ ] 麦克风权限拒绝时给出可理解错误，不出现白屏/卡死。
- [ ] 断网时语音消息进入可靠 pending，恢复后使用同一消息幂等重试。

---

# 12. 本轮通过标准

下面全部通过才算这一轮真人验收完成：

```text
Windows 881×657 无边框窗口行为正常
桌面消息/通讯录/设置结构符合微信式心智
Telegram 式右键菜单可用
头像上传/读取/移除三端一致
发送后输入焦点不丢
Android 键盘交互主观无明显卡顿，空白点击可收键盘
未读只在真正可见聊天时推进，红点/标为已读正常
软件内提醒 + 进程存活时的 Windows/Android 系统通知正常
头像大图本地压缩 + 点击对方头像进入资料页正常
图片 / GIF / 图片表情跨端发送与授权加载正常
文件消息可流式上传、显示进度、可取消、显示元数据并取得临时授权下载地址
语音条可录制、取消、可靠发送、播放和倍速
聊天页直接发起语音/视频并能进入 LiveKit 媒体房间
Web 置顶/免打扰跨端一致
已读回执符合隐私开关
```

如果某一项失败，直接在对应勾选项下面写现象即可，不需要重新跑已经通过的整份清单。
