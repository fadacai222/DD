# DD 开发进度跟踪｜当前权威快照

> 更新时间：2026-08-10
>
> 项目路径：`C:\Users\admin\Desktop\复刻微信`
>
> 产品原则：**UI/信息层级对标微信的克制；开放性、性能、即时响应、媒体体验对标 Telegram；目标是商业化产品，不接受“工程能跑即可”的妥协。**

---

# 1. 当前发布结论

当前状态定义为：

> **本轮代码实现与自动门禁已大范围闭环，已产出 Windows / Android / Web Release；仍需要按根目录 `人工测试.md` 做真实设备最终验收。未经真人验收的系统能力不得写成“最终通过”。**

本轮根目录发布物：

```text
DD-Windows.lnk
DD-Android.apk
DD-Web.lnk
```

发布物统一由：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\publish-client-artifacts.ps1
```

刷新。

Web 快捷方式通过：

```text
scripts/start-dd-web.ps1
```

选择一个本机 5 位数空闲端口启动 `build/web` 静态 Release，不再依赖旧手工路径。

---

# 2. 历史主线阶段概览

## P0｜基础通信与多端 POC

- ✅ Go API / WebSocket / Realtime 基础链。
- ✅ Windows / Android / Web 基础客户端。
- ✅ 注册登录、Pong、多端联测。
- ✅ WebRTC / LiveKit 通话 POC 主链。

## P2｜Auth / User / Device

- ✅ 邮箱注册登录。
- ✅ 验证码与密码重置。
- ✅ 失败限流。
- ✅ Access / Refresh 双令牌与刷新。
- ✅ 设备列表、远程退出、全部退出。
- ✅ 重置密码后旧设备失效。
- ✅ 用户资料、隐私设置。
- ✅ IDOR/越权边界测试。

## P3｜好友关系链

- ✅ DDID 精确搜索。
- ✅ 联系人/好友申请基础关系模型。
- ✅ Block / Unblock。
- ✅ 联系人备注、标签、星标。
- ✅ DIRECT 会话与联系人关系解耦，删除联系人不删除历史聊天。

## P4｜可靠消息与生产力功能

- ✅ 文本消息、Pending Queue、Realtime Sync。
- ✅ 已读/未读、角标、标为已读。
- ✅ 回复、编辑、撤回、本地删除。
- ✅ 转发、收藏、置顶、搜索。
- ✅ 置顶会话、免打扰、归档。
- ✅ 图片/GIF/Sticker/File/Voice/Video 消息协议基础。

## P5｜产品体验持续收敛

- ✅ Windows 原生无边框/圆角窗口与四边四角 resize 基础。
- ✅ Android 输入/键盘/长按菜单多轮优化。
- ✅ 通话计时、结束自动退出、通话消息等基础体验。
- ✅ 媒体授权、本地缓存、下载 grant 等多轮稳定性工作。

---

# 3. 2026-08-10 `bug与需求反馈-todolist.md` 本轮执行状态

## R00｜Windows DD 现代化 UI

状态：**✅ 代码闭环 / ⏳ 真人视觉验收**

本轮：

- 移除固定 `#2E2E2E` 大黑 Rail 的视觉来源。
- 新增 `DdDesktopTokens`：
  - navigationSurface
  - sidebarSurface
  - contentSurface
  - hoverSurface
  - selectedSurface
  - borderSubtle
  - titleBarSurface
  - navigationWidth
  - sidebarWidth
  - titleBarHeight
  - chatHeaderHeight
  - listRowHeight
  - compactControlHeight
- Windows 全局 Rail：
  - 中性 light/dark surface；
  - 局部圆角 selected；
  - 3px DD 绿色 active indicator；
  - Hover 与 Selected 分离；
  - badge 重排。
- 会话侧栏：
  - 统一 264px desktop token；
  - 圆角 row；
  - pinned/selected/hover 层级重做。
- 联系人侧栏同步同一视觉语言。
- Desktop Chat Header 收进同一 token 系统。
- Flutter 自定义 TitleBar 收进同一 title surface/border token。
- 原生 Windows runner 复核：
  - `WS_OVERLAPPEDWINDOW` 能力保留；
  - `WM_NCHITTEST` 四边四角 resize；
  - `WM_DPICHANGED`；
  - Windows 11 DWM round corner。

自动证据：

- 会话/联系人 R00 定向 Widget Test 多轮全绿。
- 881×657 下 sidebar/header/footer 几何已进入测试。
- R00 相关 `dart analyze --fatal-infos` 0 issue。

真人仍需：125%/150% DPI、连续 Hover 手感、真实 Windows 圆角/边缘拖拽视觉。

---

## R01｜Android 媒体“复制”动作

状态：**✅ 代码闭环 / ⏳ Android 真机验收**

- Android 图片/GIF/Sticker/视频媒体 UI 不再提供无意义“复制媒体”。
- 普通文字复制保留。
- 保存/下载保留。

---

## R02｜头像自由裁剪

状态：**✅ 代码闭环 / ⏳ 真机手感验收**

旧固定圆框模型已推翻，改为：

```text
imageRect + cropRect
```

- 1:1 crop。
- 8 锚点。
- 图片拖动/缩放。
- crop 覆盖约束。
- 90° 旋转。
- 还原 / 完成 / 取消。
- UI crop 坐标与旋转后最终 JPEG 像素坐标统一。

自动覆盖：

- 1080×2400 长图。
- 2400×1080 横图。
- 方图。
- 8 锚点。
- clamp。
- 90° 像素映射。
- 四次旋转回 0。
- 取消无输出。

相关头像裁剪/图像处理测试：11 项全绿。

---

## R03｜联系人“发消息”统一路由

状态：**✅ 已闭环**

根因：旧代码拿到 DIRECT conversation 后又 `Navigator.push(TextChatPage)`，导致 Windows 另开错误全窗聊天。

现实现：

```text
资料 Route 退出
→ MainShell 切到消息
→ ConversationsPageController 选中 DIRECT
→ Desktop 主内容区原地显示聊天
```

不破坏 Android 正常聊天导航。

---

## R04｜Android 通知小图标

状态：**✅ 代码闭环 / ⏳ ROM 真机验收**

- `POST_NOTIFICATIONS` 存在。
- App 首次启动通知初始化默认会请求权限。
- 每一条 Android 通知显式指定 `ic_stat_dd`，不只依赖初始化默认值。
- 资源合同测试存在。

真人需验证不同 ROM 状态栏实际 monochrome 显示。

---

## R05｜消息提示音 / 通话音

状态：**✅ 代码闭环 / ⏳ 双端听感验收**

声音全部为 DD 自生成确定性 PCM WAV，不包含微信/Telegram 第三方受版权音频素材。

包含：

- 消息提示音。
- 来电铃声。
- 去电等待音。
- 接通音。
- 挂断音。

重要根修：

- Windows `AppSoundBackend` 不再继续依赖曾经出现 MediaEngine 错误的 audioplayers 文件播放链；
- Windows DeviceFileSource 分支改为 `media_kit.Player`；
- Android/Web 等仍保留轻量 audioplayers 内存播放路径。

Sound/Voice 相关自动测试全绿。

---

## R06｜Windows 语音条播放

状态：**✅ 代码闭环 / ⏳ Android ↔ Windows 真音频验收**

真人日志历史根因：

```text
PlatformException(4, MediaEngine error, 指定的任务名称无效。)
```

处理：

- Windows 语音播放改 `media_kit`。
- Windows 第一次播放时懒初始化 Player，不进聊天就占 native 资源。
- 播放前按真实文件头识别：
  - WAV
  - AAC
  - MP3
  - M4A/MP4 audio
  - OGG
- 不再完全相信历史消息里可能错误的 MIME/extension。

Voice playback / voice format / sound 定向测试均已通过。

---

## R07｜视频发送/播放/缓存链

状态：**✅ 代码大链闭环 / ⚠️ 四格式真机端到端仍须人工最终验证**

### 已修根因 1：反逻辑 remote probe

旧链：

```text
上传大视频
→ 再拿签名 URL 下载回来
→ 读视频信息 / poster
→ 超时则整条发送失败
```

新链：

```text
本地 XFile
→ 本地 probe 宽高/时长/poster
→ probe 成功
→ 主视频 stream upload
→ poster 独立 reservation/upload
→ 发送 VIDEO message
```

### 已修根因 2：upload cancel 只停 UI

现在：

- 每一次对象存储 PUT 使用独立 `http.Client`。
- `MediaUploadCancellation.cancel()` 会关闭这一次 transport，真正 abort 请求。
- 不再只停止向 sink 写数据后继续挂着 socket。

### Upload Retry

- Timeout / ClientException / 408 / 429 / 5xx：最多一次受控 retry。
- PUT 重试使用**新 reservation**。
- complete 阶段复用同一 uploadId 做幂等 retry。
- 4xx 契约错误不无脑重试。

Media API 自动测试：6 项全绿，包括 active transport cancellation 和 fresh reservation retry。

### Persistent Video Cache

新增：

```text
video_file_cache.dart
video_file_cache_backend_io.dart
video_file_cache_backend_stub.dart
```

语义：

- 第一遍：签名 URL 立即流媒体播放。
- 同时后台按 stream 落完整本地文件。
- 第二次打开：优先 `file://` 本地完整缓存。
- expected size 校验。
- `.part` 临时文件。
- 失败删除残片。
- 约 4 GiB 上限。
- 30 天 age/LRU 清理。
- Web 不落无限磁盘缓存，继续 streaming。
- cache 下载 401/403/410 自动刷新 grant 一次。
- 保存/复制重新取远端 URL，不会因为播放器使用 file:// 而坏掉。

### Server orphan cleanup

worker 会回收超过保留期且没有任何 `message_media/custom sticker/Telegram sticker item` 引用的 READY chat media，覆盖：

- 视频已上传但 poster 失败。
- poster 已上传但 message send 失败。
- 文件/图片/语音上传成功后业务失败。
- 自定义表情上传成功但建立库记录失败。

### Real-format gate

新增 `video_media_probe_real_sample_test.dart`，通过环境变量接收真实 MP4/MOV/WebM/MKV 样本。

本机已用 FFmpeg 8.1.1 在系统临时目录生成 4 种真实容器样本。`flutter test` 宿主需要额外 libmpv native PATH，而且其 headless native video state 与 Windows Release App 不完全等价，因此**不能拿测试宿主失败冒充产品不支持，也不能冒充真机已通过**。最终按 `人工测试.md` 在 Release App 真实发送/播放。

---

## R08｜附件面板统一“相册”

状态：**✅ 代码闭环 / ⏳ 系统 Picker 真人验收**

- 删除独立“图片”。
- 删除独立“GIF”。
- 删除独立“视频”。
- 合并唯一“相册”。
- 文件继续独立。
- 旧“更多 → 表情文件”入口删除；表情统一走 R10 表情库。
- picker 回来按实际扩展/MIME 分流 IMAGE/GIF/VIDEO。

相关附件/聊天回归测试已通过。

---

## R09｜详细资料 Telegram 式信息层级

状态：**✅ 代码闭环 / ⏳ 视觉真人验收**

- 居中大头像。
- 昵称主标题。
- DDID 次级信息。
- 关系状态胶囊。
- DDID / 昵称或备注 / 个性签名 / 关系 / 标签卡片分层。
- 发消息 / 语音 / 视频横向快捷动作。
- 桌面最大内容宽度。
- 360×640 / 881×657 边界测试。
- stable userId 加载。
- deleted/inactive/不可见：`该用户当前不可用`。
- `NONE` 才显示添加联系人。
- `PENDING_OUTGOING` 不重复申请。
- `PENDING_INCOMING` 真实查询 incoming request 并可直接通过。
- Block 关系不做隐私泄露。

R09 相关联系人/Profile/API 自动测试：15 项全绿。

---

## R10｜Emoji + 自定义表情 + Telegram Sticker Pack

状态：**✅ 代码链闭环 / ⏳ Telegram 真凭据/真包人工验收**

### PostgreSQL

新增 migration：

```text
000016_stickers.up.sql
000016_stickers.down.sql
```

包含：

- custom_stickers
- telegram_sticker_packs
- telegram_sticker_items
- user_sticker_packs
- sticker_rate_events

真实 `dd-dev-postgres-1` 已执行：

```text
up
→ 幂等 up
→ down
→ 再 up
```

Migration roundtrip 通过。

### 自定义表情

- owner-only。
- READY STICKER media 才可加入库。
- PNG / WebP / GIF。
- 10 MiB 客户端上限。
- 4096×4096 客户端尺寸上限。
- 同账号多设备服务端同步。
- 另一个账号隔离。
- ❤️ tab。
- `+` 上传。
- 管理页：关闭 / 整理 / 多选 / 批量删除 / 二次确认。
- 已发送消息不因库删除而失效。

### Telegram Sticker Relay

客户端：

- 支持合法 `https://t.me/addstickers/<setName>`。
- 支持 Telegram 常用域名/deep link。
- 只输出严格 setName。
- 非 Telegram host、额外 URL/query、非法字符均拒绝。

服务端：

```text
getStickerSet
→ getFile
→ Telegram file download
→ DD object storage
→ stable mediaId
```

- Bot token 只从 `TELEGRAM_BOT_TOKEN` / secret file 注入。
- token 不写死、不透传客户端、不返回 provider 错误原文。
- server API 只接受 setName，不接受 arbitrary URL，切断 SSRF 面。
- 每用户 import rate limit。
- pack/item/file 上限。
- 同 pack 全局 cache，多用户复用。
- 用户订阅与全局资源分离。
- 用户 A 移除不影响用户 B。
- 当前稳定支持 Telegram 静态 WebP。
- TGS / WebM 动画/视频 sticker 当前明确计入 unsupported，不渲染空白、不假装支持。

### 权限边界

真实 PostgreSQL 测试证明：

- Alice 已订阅 pack → 可发送 sticker。
- Bob 仅仅在聊天里收到/知道同 mediaId → 新发送 `ErrForbidden`。
- Bob 正式订阅后 → 可重新发送。

### 缓存回收

worker：

1. 30 天无人订阅 pack → 删除 pack metadata/items；
2. 再回收真正无 message/library/pack 引用的 DD STICKER object。

### Flutter 面板

统一表情面板：

```text
Emoji tab
❤️ Custom tab
Pack 1 tab
Pack 2 tab
...
```

- tabs 可横向滚动。
- pack 添加后立即出现。
- 移除后立即消失。
- pack cover/first sticker 可作为 tab icon。
- 大量自定义表情 Grid 可滚动。
- GIF custom grid 不让 200 个动画同时播放，使用轻量 GIF 占位。
- 选 custom/pack sticker 发送稳定 DD mediaId。

R10 Flutter 组合回归：35 项全绿。

OpenAPI 已增加完整 Stickers tag / path / schema，OpenAPI lint 通过。

---

## R11｜启动第一屏 / 自动恢复会话

状态：**✅ 代码闭环 / ⏳ 冷启动真人验收**

- `ImClientApp` 不再直接 `home: AuthPage()`。
- 新增 `AuthBootPage`。
- 有 vault：Boot → refresh → Shell，登录表单不参与第一帧构建。
- vault 为空：Boot 判定后才进入 Auth。
- refresh 401：清无效本地会话 → 登录。
- 503/临时网络：停在 Boot + 重试，不误清 refresh token。
- 运行中 refresh 401 也会退出失效 session，不再 30 秒死循环。
- Android 原生 splash 已有 DD logo，Flutter Boot 接第一帧。

Boot/Auth 定向测试：7 项全绿。

---

## R12｜用户提及 `@username`

状态：**✅ 协议/服务端/Flutter 全链闭环 / ⏳ 真人输入手感验收**

### 权威模型

```text
text + server authoritative MessageEntity + stable userId
```

- 客户端伪造 entity/userId 被服务端清掉。
- offset/length 固定 UTF-16 code unit。
- O(n) scanner。
- 中文/Emoji/标点/大小写。
- 排除 email / @@ / 非法边界。
- 64 entity / 32 unique user 上限。
- 一次 SQL 批量解析 ACTIVE users。

### Send / Edit / Forward

- 普通发送服务端解析并写 content_json。
- Edit 删除旧 entities 后重建。
- Forward 保留历史 text + stable userId，不按新 handle 重新绑定。
- Saved / Sync / history JSON 走同一 entities。

### Stable user API

```text
GET /api/v1/users/{userId}
```

- 公开资料 only。
- blocked/missing 不泄露隐私。
- PeerProfile 通过 stable userId 加载。

### Suggestion API

```text
GET /api/v1/users/mention-suggestions?q=al&conversationId=...&limit=8
```

- q 2～32。
- limit <= 8。
- DIRECT 对方优先。
- 联系人其次。
- 公开 prefix 再次。
- 双向 block filter。
- rate limit。
- 防空 query/huge limit 枚举。

### Composer

- 220ms debounce。
- request sequence 防乱序。
- 内存缓存。
- IME composing 不强制替换。
- email / @@ 不触发。
- Windows ↑↓ / Enter / Esc。
- Android 点击候选保持输入框 Focus 和键盘。
- Overlay 不改变消息列表高度。

### Render / Tap

- MentionRichText 独立组件。
- 越界/overlap/mismatch entity 安全忽略。
- stable userId 点击。
- handle 以后修改，旧消息仍指原 user。
- 不可用 user 显示安全状态。

### `@all`

- 只在 GROUP 解释为 MENTION_ALL。
- OWNER / ADMIN 才生成。
- MEMBER 手输只是普通文本。
- DIRECT 手输只是普通文本。

### 通知

- 不制造第二条重复通知。
- Mention 可显示“某某提到了你”。
- 不绕过 mute。

服务端 contacts/httpapi/messaging 多轮全绿；Flutter Mention + chat/contacts 大回归多轮全绿。

---

## R13｜文档与人工测试

状态：**✅ 本轮已更新**

- `开发进度跟踪.md`：当前文件已重写为本轮权威快照。
- `人工测试.md`：已按 R00～R14 + 核心回归重新生成，包含 `[ ] / ✅ / ❌ / 测试反馈`。
- 未真人验证项保持 `[ ]`，不会自动假打勾。

---

## R14｜Android Telegram 风格悬浮 Footer

状态：**✅ 代码闭环 / ⏳ Android 真机视觉验收**

- 自定义 `DdFloatingNavigationBar`，不是 Material NavigationBar 套圆角。
- 左右约 12dp 留白。
- 完整 pill 外轮廓。
- DD 品牌 selected capsule。
- 轻边框/轻阴影。
- light/dark tokens。
- 未读 badge。
- 180ms 切换动画。
- Shell 上层读取 IME 状态；键盘弹出 Footer 隐藏，避免被 Scaffold 内部 viewInsets 消耗后误判。
- Windows `_DesktopRail` 独立，不受移动 Footer 修改影响。

组件自动测试已通过。

---

# 4. 本轮安全/稳定性重点

## 4.1 Sticker SSRF

客户端只把 Telegram URL 解析成 setName；服务端 API 根本不接任意 URL。

## 4.2 Sticker 越权

“看过某 sticker mediaId”不等于“拥有这个 sticker”。新发送必须：

- 自己拥有 STICKER media；或
- 在自己 custom_stickers；或
- 自己订阅对应 Telegram pack。

## 4.3 Media orphan

READY media 不再永久泄漏：超过保留期并且无任何业务引用时由 worker 删除对象 + DB row。

## 4.4 Upload Cancel

取消不再只是 UI flag；对象存储 HTTP transport 会真正 close。

## 4.5 Mention 伪造

服务端不信任客户端 entity offset/userId；所有普通发送都按正文重新解析。

## 4.6 Block 隐私

曾在 Mention stable profile 重构中发现过“复用 relationship helper 可能泄露 BLOCKED_BY_PEER”的回归风险，已恢复成 blocked/missing 对外不可区分语义，不因 Mention 破坏旧隐私模型。

---

# 5. 自动门禁记录

本轮已实际执行过的门禁包括：

```text
Dart analyzer（定向 + 全量）
Flutter Widget/Unit Tests（定向 + 全量）
Go package tests（定向 + go test ./...）
OpenAPI contract test
OpenAPI lint
PostgreSQL migration up / idempotent up / down / up roundtrip
Sticker real PostgreSQL integration
Sticker messaging authorization real PostgreSQL integration
Windows Release build
Android Release APK build
Web Release build
```

已多次明确通过的定向批次：

```text
R02 avatar crop/image processing       11 tests
R03 contacts/conversation routing      16 tests（相关批次）
R09 contacts/profile/API               15 tests
R10 sticker Flutter combined           35 tests
R11 Boot/Auth                            7 tests
R12 Mention/Chat/Contacts               多轮回归，最后相关大批次 59 tests
R14 floating footer                     2 tests
Media upload cancel/retry               6 tests
Voice/Sound                             8 tests
R00 desktop conversation/contacts      13 tests；后续 chat 合并回归 39 tests
```

> 真实 MP4/MOV/WebM/MKV 的 `VideoMediaProbe` 集成门禁已建立，但 Flutter test headless native media_kit 环境不等价于 Windows Release VideoOutput；最终格式支持以 `人工测试.md` 的真实 Release 双端发送/播放为准。

---

# 6. 本轮新增的重要文件

```text
server/migrations/000016_stickers.up.sql
server/migrations/000016_stickers.down.sql
server/internal/stickers/models.go
server/internal/stickers/service.go
server/internal/stickers/telegram_provider.go
server/internal/stickers/telegram_provider_test.go
server/internal/stickers/service_integration_test.go
server/internal/httpapi/stickers.go
server/internal/httpapi/stickers_test.go
server/internal/messaging/sticker_authorization_integration_test.go

clients/app/lib/features/messaging/domain/sticker_models.dart
clients/app/lib/features/messaging/domain/telegram_sticker_link.dart
clients/app/lib/features/messaging/data/sticker_api_client.dart
clients/app/lib/features/messaging/presentation/sticker_library_sheet.dart
clients/app/lib/features/messaging/data/video_file_cache.dart
clients/app/lib/features/messaging/data/video_file_cache_backend_io.dart
clients/app/lib/features/messaging/data/video_file_cache_backend_stub.dart

clients/app/test/features/messaging/sticker_api_client_test.dart
clients/app/test/features/messaging/sticker_library_sheet_test.dart
clients/app/test/features/messaging/telegram_sticker_link_test.dart
clients/app/test/features/messaging/video_media_probe_real_sample_test.dart

scripts/start-dd-web.ps1
scripts/publish-client-artifacts.ps1
```

---

# 7. 下一步只剩什么

代码层继续开发不能替代以下真人系统能力：

1. Windows 125%/150% DPI 与实际边缘 Resize 手感。
2. Android ROM 状态栏真实 DD small icon。
3. Windows/Android 消息音、来电、回铃、接通、挂断的真实听感。
4. Android ↔ Windows 真语音条播放。
5. Android ↔ Windows MP4/MOV/WebM/MKV 真视频发送、Poster、播放、缓存、保存。
6. Telegram Bot Token 配置后的真实公开 Sticker Pack Relay。
7. Android 悬浮 Footer 在不同屏幕尺寸/SafeArea/软键盘上的实际视觉。
8. 完整通话与 30 分钟稳定性回归。

这些全部已写进根目录：

```text
人工测试.md
```

**在人工测试全部通过前，项目状态只能写“代码/自动门禁闭环”，不能写“最终上线验收完成”。**
