# DD 开发进度跟踪

> 最后更新：2026-08-08 14:46（UTC+8，P5 文件流式上传/进度取消 + 过期媒体清理 Worker 推进）
> 当前阶段：**P4 可靠消息核心已落地；本轮问题修复代码、自动回归和 Windows/Web/Android 构建均已收口。下一步不再继续堆功能，先重启旧 auth-dev，让主开发环境真正启动 LiveKit，然后完成真人跨端/通知/通话/Windows 原生窗口/Android Profile 验收。**
> 当前运行环境提醒：截至 12:14，`.data/auth-dev.json` 仍是 10:23 启动的旧环境，状态文件没有 `livekitStartedByScript`，`docker compose ... ps livekit` 为空；不重启环境直接测通话会得到假失败。
> 统计原则：只有代码、自动测试、真实集成、构建或人工验收能支撑的内容才计入；未接入的跨模块能力绝不提前算完成。

---

## 1. 当前一句话结论

项目已经从“账号体系刚起步”推进到：

```text
P0 实时/通话 PoC 已完成当前三端人工批次
P1 正式工程底座基本形成
P2 正式账号、密码恢复、资料、隐私、多设备会话生命周期已落地
P3 正式好友关系链、拉黑、联系人管理、Flutter UI 已落地
P4 正式文字消息、事务 sequence、Durable Outbox、Sync cursor、正式鉴权 WebSocket、Flutter 离线队列与聊天可靠性主链已经落地；Windows 无边框主壳、桌面消息/通讯录/设置、聊天主区第二轮已落地，当前进入真人 UI/性能收口
```

现在已经具备一条正式业务链：

```text
注册
→ 登录
→ 多设备会话
→ 密码找回
→ 资料 / 隐私
→ 精确 Handle 搜索
→ 好友申请
→ 建立双向好友
→ 创建 / 复用私聊会话
→ 联系人备注 / 标签 / 星标
→ 删除 / 拉黑
→ 私聊会话
→ 文字消息
→ 断网待发送 / 原 clientMessageId 重试
→ Durable Outbox
→ Cursor Sync 补偿
→ 已读 / 未读
→ 撤回 / 本地删除
```

但项目仍然**不是完整 IM 1.0**。本轮重点不是继续堆功能，而是把真人测试抓出的根因收掉：`IndexedStack` 隐藏聊天不再继续推进已读，左侧消息入口/会话未读红点能保留；Android 改成 `adjustNothing + viewInsets + Transform` 避免键盘动画驱动整条消息列表反复 layout，并支持点击聊天空白收键盘；移动端消息/会话菜单统一紧凑 Action Sheet，危险删除红色并补“标为已读”；头像大图在独立 isolate 本地压缩后上传，点击对方头像进入资料页；Access Token 增加主动轮换，401 保留 pending 后刷新，不再笼统报“服务端拒绝”；Windows 标题栏缩至约 28px、请求 DWM 圆角并加 8 方向原生 resize；最关键的是主 `run-auth-dev.ps1` 之前根本没有启动/配置 LiveKit，本轮已经把 LAN LiveKit/RTC/TURN 合并进主环境并让 hangup 幂等。仍未完成的是杀进程 Push、Sticker/GIF、图片、语音条、独立“已送达”、完整 SQLite、正式 P7 持久化通话模型和真机 Profile 性能量化。

---

## 2. 阶段进度

> 百分比只是位置判断，不是精确工时。

| 范围 | 当前判断 | 说明 |
|---|---:|---|
| 产品 / 架构 / 安全 / API / 测试文档 | 🟢 约 88% | 主设计完整，新增 P3 正式设计；最终许可证、部分 ADR、1.0 E2EE 范围仍待冻结 |
| P0 技术路线验证 | 🟢 约 80%～85% | Windows/Web/Android 当前人工批次通过；公网 TURN/TLS、跨运营商、iOS/macOS/Linux 仍未验收 |
| P1 工程基础 | 🟢 约 82%～88% | Monorepo、Go、Flutter、Admin、OpenAPI、迁移、CI、Compose 均已存在；Secret/依赖/许可证质量门仍需加强 |
| P2 Auth / User / Device | 🟢 约 90%～93% | 主体账号生命周期已完成；头像上传/读取/移除、客户端大图后台压缩、统一头像展示和对方资料页已接；Access Token 已主动轮换；正式 P5 对象存储迁移、Admin、2FA、最终真人跨端验收仍未结束 |
| P3 好友关系链 | 🟢 约 84%～88% | 搜索、申请、双向好友、会话复用、联系人元数据、删除、拉黑、Flutter UI、真库并发测试已完成；P4 消息拉黑联动已接入，P7/P8 联动和 Admin 风控配置待后续 |
| P4 会话 / 文字聊天 / 同步 | 🟡 约 88%～92% | 可靠消息、Outbox/Sync、Redis 跨节点、未读/已读可见性、红点、移动标为已读、401 pending 恢复、Telegram 式菜单与 Android IME 第二轮已落地；剩 Sticker/GIF、已送达、完整本地库、跨端人工回归与 Profile 性能收口 |
| P5 媒体 / 文件 / 语音 | 🟡 约 61%～67% | 私有对象存储媒体表、预签名上传、SHA/大小/MIME 完成校验、配额预占、授权短期下载已进入主链；文件已升级双遍流式 SHA + 流式上传、进度/取消和 2 GiB 上限，并接系统保存位置、真实下载进度和下载取消；图片支持单批最多 30 张、逐张压缩上传和真实进度；Worker 已回收过期未完成上传。剩恶意文件扫描、孤儿派生清理、断点续传、拖拽/粘贴、超大下载直接落盘与音频策略 |
| P6～P14 | 🔴 整体仍低 | 群聊、正式通话持久化、朋友圈、扫码、E2EE、生产运维等仍在后续；P7 已复用 PoC 通话链进入聊天入口，P10 已有“进程存活时”的 Windows/Android 本地通知基线，均不能冒充对应阶段正式完成 |
| **完整 1.0 总路线** | **约 41%～47%** | 账号、关系链、可靠消息底座和第二轮客户端骨架已形成；P5 已从“能发媒体”推进到具备流式大文件和后台垃圾回收的正式主链，但群聊/朋友圈/正式通话/生产级 Push/E2EE 仍是大块 |

---

## 3. P0 当前结论

根目录旧 `人工测试清单.md` 作为 P0 验收归档，不再重复测试。

当前 Windows / Web / Android 人工批次已经验证：

- REST / WebSocket PoC。
- Windows ↔ Web 音视频。
- Windows ↔ Android 音视频。
- Web ↔ Android 音视频。
- 来电、接听、拒绝、取消、45 秒超时、挂断同步。
- TURN/UDP relay-only。
- Android 摄像头、静音、视频开关。
- Android 后台 / 锁屏 / 短时断网恢复。
- Android / Windows 蓝牙音频有设备场景。
- 窄屏和窗口缩放。

仍保留后续专项：

- 正式 `wss://`。
- 可信证书。
- TURN/TLS。
- 公网 NAT / CGNAT / 4G/5G 跨运营商。
- iOS 真机。
- macOS / Linux 正式构建和验收。

P0 Call 身份和状态仍是 PoC，不能直接冒充 P7 生产通话模块。

---

## 4. P1 工程基础

| 能力 | 状态 | 当前证据 |
|---|---|---|
| Monorepo | ✅ | `clients/ server/ packages/ infra/ scripts/ docs/ tasks/ admin/` |
| Go 服务 | ✅ | `cmd/api`、`cmd/worker`、`cmd/migrate` |
| Flutter | 🟢 主体底座 | Foundation/Auth/Contacts/Realtime/Calls 均有模块和自动测试 |
| React Admin | 🟡 | TypeScript + Vite 生产构建存在；正式 RBAC/审计业务仍待 P10 |
| OpenAPI | ✅ | `/api/v1` Discovery/System/Auth/Contacts/Messaging/Sync 正式合同通过 recommended-strict lint |
| Realtime Schema | ✅ | Go / Dart contract fixture tests |
| 配置 / Secret | 🟢 | 环境变量、`*_FILE`、危险配置校验、开发随机 Secret |
| 日志 / Request ID | 🟡 | HTTP 已有；Worker/WSS/后台任务仍要统一 |
| Health | ✅ | live / ready / version 分离 |
| Dev Compose | ✅ | PostgreSQL / Redis / MinIO / Mailpit / LiveKit |
| Migration | ✅ | 当前 000001～000006；`000005_messaging` 已完成可靠消息真库链，`000006_profile_avatars` 已于 2026-08-08 在当前开发 PostgreSQL 实际 `up` 成功 |
| CI | 🟢 主要路径 | Go format/test/vet、Auth 真库、Contacts 真库并发、Messaging 真库可靠性、OpenAPI、Flutter、Admin、Compose、E2EE PoC 等 |

本轮额外清掉了上一轮遗留的 5 个 Go `gofmt` 问题，并把 `gofmt` 正式加入 CI，避免“本地能编译但 CI 格式门禁才炸”。

---

## 5. P2 Auth / User / Device 当前状态

### 5.1 已正式落地接口

```text
POST /api/v1/auth/register/email/send-code
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/token/refresh
POST /api/v1/auth/password/reset/send-code
POST /api/v1/auth/password/reset
POST /api/v1/auth/logout-all

GET   /api/v1/me
PATCH /api/v1/me

GET    /api/v1/devices
DELETE /api/v1/devices/{deviceId}
```

### 5.2 已落地能力

- 邮箱 Unicode / IDNA 归一化。
- Handle 规则和保留词。
- 6 位邮箱验证码。
- 验证码 HMAC 哈希存储。
- 发送冷却、窗口限流、尝试次数、过期和消费。
- Mailpit 真 SMTP 联调。
- Serializable 注册事务。
- Argon2id + NeedsRehash。
- 不存在账号 / 错误密码统一错误。
- dummy Argon2 降低明显时序枚举。
- 登录失败 15 分钟累计 10 次限流。
- Auth 安全审计事件。
- 15 分钟 Access Token。
- Refresh Token 哈希存储、轮换、Family、重放整族撤销。
- Access Token 每次同时验证用户和设备有效性。
- 密码找回和密码重置。
- 重置密码后撤销全部旧设备 / Refresh Token。
- `/me` 资料读取和修改。
- 隐私设置读取和修改。
- 设备列表。
- 指定设备远程退出。
- 全设备退出。
- 设备 IDOR 防护。

### 5.3 Native / Web 会话

Native：

- `flutter_secure_storage` 存 Refresh Token。
- App 启动自动 Refresh 恢复登录。
- “退出本机”撤销服务端设备，不只是清 UI。

Web：

- JSON 不返回 Refresh Token。
- `dd_refresh` 使用 HttpOnly Cookie。
- `SameSite=Lax`。
- HTTPS 自动 Secure。
- Cookie Path `/api/v1/auth`。
- `BrowserClient.withCredentials=true`。
- 页面启动可通过 Cookie 自动恢复登录。

### 5.4 Flutter P2 UI

已有：

```text
注册
登录
密码找回
资料
隐私
设备管理
远程退出
全部退出
```

真正仍需要人验收的是系统安全存储、真实浏览器 Cookie、多端远程退出和 Android 真机布局，这些已经放到新的 `人工测试.md`，不再把 Token replay / SQL 事务这种机器能测的项目交给人点。

### 5.5 P2 剩余

- Admin 用户只读列表 / RBAC。
- TOTP / 2FA（1.0 是否强制仍需产品决定）。
- 更完整 SMTP 故障 / 验证码轰炸负向矩阵。
- 并发同邮箱 / Handle 注册专项进一步加强。
- Windows 当前机器的**干净 Release Build 环境依赖**：Visual Studio Build Tools 缺 ATL；这不是业务代码错误。

---

## 6. P3 好友关系链本轮大批量开发

本轮新增正式模块：

```text
server/internal/contacts/
clients/app/lib/features/contacts/
server/migrations/000004_contacts.*
docs/11-P3好友关系链.md
```

### 6.1 数据表

新增：

```text
contact_requests
contacts
contact_tags
blocks
relationship_rate_events
conversations
conversation_members
```

其中 Conversation 基础提前进入 P3，是为了保证接受好友时可在一个数据库事务里“建好友 + 建/复用私聊会话”。消息表和可靠同步仍属于 P4。

### 6.2 精确 Handle 搜索

```text
GET /api/v1/users/by-handle/{handle}
```

安全边界：

- 必须 Bearer Access Token。
- 精确 Handle，不提供无界全库昵称模糊搜索。
- 不返回邮箱。
- 任一方向拉黑时统一 404，不暴露“谁拉黑谁”。
- 每用户 10 分钟默认最多 60 次。
- 限流记录落 PostgreSQL。

### 6.3 好友申请

支持：

```text
发送
接收列表
发出列表
接受
拒绝
撤销
过期
```

默认：

```text
申请有效期 30 天
每用户每天最多 30 次新申请
```

同方向重试幂等，不制造重复 PENDING。

如果双方几乎同时互发申请：

```text
自动收敛为 ACCEPTED
```

### 6.4 真并发问题已实际抓到并修复

第一次真实 PostgreSQL 并发测试复现：

```text
SQLSTATE 40001
could not serialize access due to read/write dependencies among transactions
```

根因是 PostgreSQL `SERIALIZABLE` 在双向同时申请时允许事务主动中止，需要应用层重试。

已补：

```text
好友申请事务自动重试
接受好友事务自动重试
```

第二次同样真库并发测试通过。

### 6.5 接受好友原子事务

一次完成：

```text
双向 contacts
+ DIRECT conversation 创建 / 复用
+ 2 个 conversation_members
+ request ACCEPTED
```

同一用户对只能有一个逻辑私聊。

### 6.6 联系人管理

已支持：

- owner 自己的备注。
- owner 自己的标签。
- owner 自己的星标。
- 标签 NFKC 归一化和大小写去重。
- 最多 20 个标签。
- 删除好友时删除双方 contact，但保留历史会话语义。

### 6.7 拉黑

当前 P3 立即执行：

```text
写 block
→ 删除双方好友关系
→ 取消双方 PENDING 申请
→ 搜索对方时不可见
→ 再发好友申请被阻断
```

同时暴露服务层 `IsBlockedBetween` 给后续正式模块使用。

**尚未提前声称完成**：P4 正式消息、P7 正式通话、P8 朋友圈现在还没全部存在，所以“拉黑立即阻断这些正式模块”的总验收仍保持未完成。

### 6.8 Flutter P3 UI

P3 关系链能力现已收进正式产品主壳的“联系人”入口，不再以“好友 / 关系链 · P3”调试页作为登录后主入口。

联系人页当前四个功能页：

```text
添加朋友
新的朋友
联系人
黑名单
```

已接：

- 精确 Handle 搜索。
- 申请理由。
- 加好友。
- 接受 / 拒绝。
- 撤销发出的申请。
- 联系人列表。
- 备注 / 标签 / 星标。
- 删除好友。
- 拉黑并删除。
- 黑名单列表。
- 解除拉黑。

已有 360×640 Android-like Widget Test，专门防 `BOTTOM OVERFLOWED`。

---

## 7. 本轮自动化与真实验证

### P4 可靠消息新增验证

真实 PostgreSQL `000005_messaging` 已迁移并执行完整生命周期测试：

```text
100 并发创建同一 DIRECT conversation：PASS，仅 1 个逻辑会话
100 并发重复同一 senderDeviceId + clientMessageId：PASS，仅 1 条逻辑消息
sequence 连续分配 / reply 同会话约束：PASS
历史 beforeSequence 游标分页：PASS
lastReadSequence 单调推进 / 越界拒绝：PASS
Durable Outbox -> 双方 sync_events：PASS
重复 Sync cursor：PASS，无重复事件
拉黑后禁止新写、旧历史仍可读：PASS
不限时撤回：2 分钟硬限制已从服务端和 OpenAPI 删除；测试用例已改为超过 24 小时自己的消息仍可撤回、他人仍不可撤回（本轮普通 `go test ./...` 通过；真 PostgreSQL 集成用例由 CI/真库环境执行）
本地删除只影响当前用户视图：PASS
陌生消息默认拒绝 / 隐私开启后允许：PASS
```

这轮并发测试实际抓出了两个只靠普通单测很难发现的问题，并已修复：

- 100 并发建私聊时，单纯 `Serializable + 3 次重试` 会出现 `SQLSTATE 40001`；现改为同一 user pair 的 PostgreSQL advisory transaction lock + 唯一键双保险。
- 100 并发重放同一 `clientMessageId` 时，先查再插仍存在竞争窗口；现对 `deviceId:clientMessageId` 使用 advisory transaction lock，重复请求稳定返回同一 messageId。

Flutter 新增可靠性 / 交互自动测试：

```text
首次发送网络失败 → pending 持久化：PASS
恢复后重试继续使用完全相同 clientMessageId：PASS
服务端成功后才删除 pending：PASS
Sync cursor 持久化并禁止回退：PASS
当前聊天页收到 Sync 新消息后自动推进已读：PASS
会话置顶 / 免打扰返回后立即本地 upsert 且保留其他偏好：PASS
自己的消息按 peerLastReadSequence 显示“已读”：PASS
Windows Enter 发送：PASS
Shift+Enter 不误触发送：PASS
发送后输入框 FocusNode 保持焦点 + 系统文本输入仍激活：PASS
881×657 登录后主壳使用桌面导航/会话结构且无 overflow：PASS
头像 Auth API Client 使用 Bearer + 原始二进制 PUT：PASS
2400×1800 大头像本地异步缩放/压缩到 <=2 MiB / <=1536 边：PASS
隐藏桌面聊天 hostVisible=false 不推进已读，真正可见后才 markRead：PASS
Android 未读会话长按出现“标为已读”并真实清零：PASS
Android 点击聊天空白区域释放输入焦点：PASS
Android “仅本地删除”危险操作红色：PASS
Access Token 401 时 pending 不失败、触发刷新且保留原消息：PASS
登录成功进入正式产品主壳：PASS
联系人窄屏 / 申请流程：PASS
```

正式 Realtime 新增自动验证：

```text
/api/v1/realtime 无 Token：拒绝
错误 protocolVersion：拒绝
Dart hello 携带 accessToken + protocolVersion：PASS
半开 WebSocket 心跳重连：PASS
旧 /ws P0 兼容链：PASS
Redis 两 API 节点跨节点 event_available：PASS
CLIENT KILL 强杀 Pub/Sub 后自动恢复：PASS
200 条正式鉴权 WebSocket 同时在线并全部 Pong：PASS
```

跨节点路由没有把“在线设备列表”做成 Redis 里的第二份真相，而是采用无状态 fan-out：每个 API 节点订阅同一用户事件总线，收到提示后只检查自己的本机 Socket Hub。这样 Redis 重启不需要重建 Presence；最坏只是漏掉瞬时 wake-up，客户端仍由 PostgreSQL Durable Outbox + `/api/v1/sync?cursor=` 补账。

专项可靠性/负载脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\benchmark-p4.ps1
```

覆盖：

```text
真实 PostgreSQL >= 100 msg/s
200 条正式鉴权 WebSocket 同时在线 + Ping/Pong
Redis 跨节点投递 + 强杀 Pub/Sub 后恢复
```

性能测试不塞进普通共享 Runner CI，避免机器抖动制造伪失败；Redis 功能正确性测试仍进入 CI。

### 服务端

已通过：

```text
gofmt：当前 server 全部通过
go vet ./...：PASS
go test ./...：PASS
Go API / Worker / Migrate build：PASS
Docker Linux API image build：PASS
```

新增 HTTP / 账号安全测试覆盖：

```text
未登录好友搜索 = 401
精确搜索不返回 email
被拉黑搜索 = 404
好友申请 IDOR = 403
pageSize > 100 = 400
错误 Method = 405 + Allow
CORS 允许 GET/POST/PUT/PATCH/DELETE/OPTIONS：PASS
头像只接受 JPEG/PNG/WebP，伪造 PNG / SVG 拒绝：PASS
头像限制最大 2048×2048 / 约 4MP，避免小文件超大画布图片炸客户端内存：PASS
头像文件上限由 HTTP MaxBytesReader + DB 约束双层限制为 2 MiB
```

### PostgreSQL 真库

本轮头像新增 `000006_profile_avatars` 已在当前开发 PostgreSQL 实际执行 `up`，日志确认 `migrations applied count=1`。完整 `down -> up` round-trip 因工具安全层阻止潜在破坏性 `down` 未强行绕过。

此前使用临时 `postgres:18.4-alpine` 真库，Migration 000001～000004 后运行：

```text
TestRelationshipLifecycleWithPostgres：PASS
```

覆盖：

```text
搜索
申请
重复提交幂等
反向申请自动接受
双向联系人
DIRECT conversation
owner 联系人元数据隔离
拉黑
解除拉黑
IDOR
拒绝
双方并发申请
搜索限流
```

测试容器执行完自动删除，没有留下 `dd-dev` 或 P3 临时容器。

### OpenAPI

```text
JSON：PASS
Redocly recommended-strict：PASS
```

### Flutter / 本轮最终静态与构建快照

```text
Dart format：74 files / 0 changed
Flutter 全量测试：51/51 PASS
Realtime package：4/4 PASS
Go vet ./...：PASS
Go test ./...：PASS
PowerShell run/stop/firewall 脚本语法解析：PASS
Docker Compose LiveKit 配置静态解析：PASS
git diff --check：PASS
Windows 881×657 桌面主壳 Widget 回归：PASS
发送后输入焦点保持回归：PASS
Windows Release（含无边框 C++ runner）：PASS
Web Release：PASS
Android Debug APK：PASS
```

本轮 Android 输入卡顿专项第二轮不再只减少 rebuild：Activity 由 `adjustResize` 改为 `adjustNothing`，Flutter 直接监听 `viewInsets`，通过合成层 `Transform.translate` 把已完成布局的聊天表面随键盘平移，避免 IME 每一帧改变可用高度后重新 layout 长历史；保留消息 `RepaintBoundary`、700ms 草稿 debounce、TextField 不 disable、轻量 InkRipple。真实 Android Profile FPS 仍需人工/性能批次量化，当前不提前声称已达到 60fps。

> 本轮 `flutter analyze --fatal-infos` 在中文项目路径下触发 Flutter 3.44 Analysis Server 的 LSP JSON 截断异常（analysis server 自身退出，不是 Dart 诊断）。因此没有把这次 analyze 结果伪装成通过；改用全量 Flutter Test + 三端实际构建作为本轮编译门禁。现有 `build-client.ps1` 的 Windows 构建继续使用 `subst` 短路径规避同类路径工具链问题。

本轮还修复了两个旧测试稳定性问题：

- Calls Widget Test：`testWidgets` 的 FakeAsync 环境误用了 `pumpEventQueue()`，导致“短屏来电无 overflow”测试长期挂住并表现为工具通道 502/超时。已改为 `tester.pump()`，并新增“shutdown 不挂死”回归；随后整套 Flutter Test 36/36 一次性通过。
- Realtime 半开 WebSocket 重连测试：服务端测试代码在 `await WebSocketTransformer.upgrade()` 之后才递增连接计数，和客户端 `channel.ready` 存在调度竞态，导致 CI 偶发 3 秒超时。已把连接计数移动到等待 upgrade 之前，并连续重复运行 10 次全部通过，最终总质量脚本再次 5/5 PASS。

### 总质量门禁 / 当前构建

项目统一入口：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-client.ps1
```

本轮 P4 总门禁持续保持 **5/5 PASS**：Go format/vet/test、Realtime package、Flutter 全量测试、REST + WebSocket Live Smoke 和残留端口检查全部纳入 `scripts/test-client.ps1`。Smoke 使用动态 5 位空闲端口并在结束时校验释放，不占用人工测试环境。

总检期间还抓到 `test-client.ps1` 的一个间歇性问题：Windows 自动分配空闲端口偶尔会返回四位端口（本次为 `5703`），但项目配置明确要求 `IM_PORT >= 10000`，导致临时 API 拒绝启动。现已把 smoke 端口选择收紧为 `10000~65535` 的随机空闲端口，不再靠运气。

此外已从**完全无运行容器**状态实际启动 `run-auth-dev.ps1 -SkipClientBuild -SkipWeb` 验证开发环境：PostgreSQL / Redis / API `18473` / Messaging Worker 均成功启动，`/api/v1/system/ready` 同时确认 `postgres=ok`、`redis=ok`；随后 `stop-auth-dev.ps1` 已确认停止 API / Worker 和本轮脚本自己启动的基础设施，`18473` 已释放且 `.data/auth-dev.json` 已清理。启动脚本同时修复了两个开发环境问题：

- `run-auth-dev.ps1` 现在会启动 PostgreSQL / Redis / Mailpit / **LiveKit**，并构建启动 API + `cmd/worker`；LiveKit 使用检测到的 LAN IP 暴露 signal `17880`、RTC TCP `17881`、RTC UDP `17882`、TURN UDP `13478`，防火墙只放行 Private + LocalSubnet。**截至 2026-08-08 12:14，当前机器仍在运行 10:23 启动的旧 auth-dev，状态文件没有 `livekitStartedByScript`，且 `livekit` service 为空，因此真人通话必须先 stop/start 一次新脚本。**
- Go 查找改为 PATH 优先、`C:\Program Files\Go\bin\go.exe` 兜底，避免非交互 PowerShell 明明装了 Go 却启动失败。

Migration 辅助脚本还修复了 PowerShell URL 插值：`$database?sslmode=disable` 改为 `${database}?sslmode=disable`，否则 `$database?sslmode` 会被当成错误变量名。

三端构建验证：

```text
Windows Release：PASS（im_client.exe）
Web Release：PASS（build/web）
Android Debug APK：PASS（app-debug.apk）
```

```text
Flutter Windows Release：PASS
Flutter Web Release：PASS
Android Debug APK：PASS
Docker Linux API image：PASS
```

Windows ATL 已安装完成，`flutter_secure_storage_windows` 的 `atlstr.h` 构建阻塞解除；`build-client.ps1` 仍保留 ATL 预检，换新机器时如果缺组件会在构建前直接给出安装提示。

Android 在 Flutter 3.44.9 + AGP 9 下会针对当前 `livekit_client 2.10.0 -> flutter_webrtc 1.6.0 / device_info_plus 12.4.0` 上游依赖栈提示传统 KGP 未来兼容性警告。当前这些版本已经是本项目可解析的最新 LiveKit 组合，APK 构建正常。`build-client.ps1` 只在锁文件仍精确匹配这组版本时过滤这一段已知冗长提示；只要依赖版本或插件集合变化，警告会重新显示，避免把新的兼容问题静默隐藏。

---

## 8. CI 更新

CI 现在额外包含：

```text
Go format check
Contacts PostgreSQL concurrency integration
Messaging PostgreSQL reliability integration
```

Messaging CI 会在真实 PostgreSQL 上重复执行 P4 会话/消息幂等、Outbox/Sync、拉黑、撤回、本地删除等生命周期验证；后续修改若重新引入重复消息或并发会话问题，会直接阻断。

---

## 9. P4 当前主体结果与剩余收口

本轮已经把以下**可靠性底座**一次性落下：

```text
000005 messages / outbox_events / sync_events
DIRECT conversation 并发幂等
conversation sequence
senderDeviceId + clientMessageId 幂等
消息历史 sequence cursor
已读序号 / 未读数后端状态
Durable Outbox Dispatcher
/api/v1/realtime 正式鉴权
/api/v1/sync 恢复接口
Flutter sync cursor + pending 持久化
断网待发送 / 原 ID 重试 / 单条重试 / 取消发送
草稿持久化
文字消息
回复 / 复制
撤回事件链
本地删除
Redis 跨 API 节点实时唤醒
200 WSS / >=100 msg/s 专项基线
P3 拉黑 -> P4 新消息阻断
```

### 9.1 2026-08-08 真人验收问题 → 本轮处理状态

```text
Web 端置顶无效            → 已定位 CORS PATCH 预检根因并修复，待真人跨端回归
Web 端免打扰无效          → 同上，且客户端成功后立即更新本地 UI
聊天气泡无“已读”          → 已接 peerLastReadSequence + 隐私开关，待双端真人回归
PC/Web 无 Enter 发送       → 已实现 Enter 发送 / Shift+Enter 换行 / composing 防误发
Emoji 缺失                → Unicode Emoji 基础选择器已实现；最近使用仍待做
图片表情 / Sticker         → 未完成
GIF                       → 未完成
撤回 2 分钟硬限制          → 服务端 / OpenAPI / 客户端文案均已移除，待多端最终一致验收
图片发送                   → 未完成
语音条                     → 未完成
整体偏 WhatsApp / Material → Windows 无边框、60px 导航、248px 会话栏、通讯录/设置双栏和聊天页第二轮已落地；登录/发现/我的及像素细节仍继续收口
Windows 默认窗口/系统标题栏 → 默认 881×657；标题栏约 28px，WS_OVERLAPPEDWINDOW + WM_NCCALCSIZE 自绘，DWM 圆角/去 accent border，Flutter 8 方向 Win32 resize handle；真人色块/缩放待复测
头像缺失/大图限制          → PUT/DELETE /me/avatar + GET /avatars/{userId}；普通大图在后台自动旋转/缩放/压缩，源文件 64 MiB / 约 70MP 安全上限防内存 DoS；主导航/会话/聊天/联系人/我的统一显示；点击对方头像进入资料页
发送后输入光标丢失         → 不再 disable TextField，发送前后 requestFocus；自动回归 PASS
联系人页仍自动已读/无红点    → 已读推进要求 hostVisible + resumed + 当前 route；主消息入口/会话保留红色未读角标；自动回归 PASS
Android 无“标为已读”/菜单丑 → 长按会话/消息统一紧凑圆角 Action Sheet；未读会话补“标为已读”；仅本地删除红色；自动回归 PASS
软件内外无通知             → 前台非当前会话显示软件内提醒；Windows/Android 进程存活后台接本地系统通知；杀进程可靠 Push 明确保留 P10
偶发“服务端拒绝”           → 定位到 Access Token 无主动轮换可触发 401；现到期前刷新，401 保留 pending 并触发 refresh；业务 4xx 显示具体原因
聊天内语音/视频无法进房间    → 根因是旧 run-auth-dev 明确清空 LiveKit 且未启动容器；新脚本已合并 LAN LiveKit/RTC/TURN，需 stop/start 当前旧环境后真人复测
通话状态变化/挂不断          → accept/reject/hangup 同状态重复动作幂等；客户端 INVALID_CALL_STATE 自动恢复/清理最新状态
Android 键盘升降掉帧         → 第二轮改 adjustNothing + viewInsets + 合成层 Transform，避免长消息跟 IME 每帧重新 layout；空白点击收键盘；真人 Profile 待测
右键菜单风格不合适          → 桌面消息/会话 Telegram 式紧凑浮层、左图标、危险操作红色；明确不采用 WhatsApp 菜单心智
```

已修项不再列作“代码已知缺陷”，但在 Web/Windows/Android 真人回归前仍不提前关闭对应任务。

### 9.2 新产品体验基线

正式冻结：

```text
UI / 布局 / 交互习惯 → 参考微信
功能丰富度 / 开放性 / 限制策略 → 参考 Telegram
WhatsApp → 不作为设计参考
```

详细标准见 `docs/12-产品体验与UI功能基线.md`。

现在 **还不能把 P4 写成 100%**，剩余明确是：

```text
P4-019：继续登录/发现/我的像素细节、三端截图验收 / Golden 基准；Windows/消息/通讯录/设置第二轮已落地
P4-024：Android/Windows/Web Profile 帧耗时量化、50 条快速发送、500+ 历史性能验收
P4-015：Web 置顶/免打扰跨端真人回归，确认刷新和另一端同步
P4-021：补独立“已送达”机制，并验收关闭已读回执场景
P4-022：Web + Windows 中文输入法真人回归后关闭
P4-017：超过 24 小时撤回的真库 / 多端最终一致验收后关闭
P4-020：Emoji 最近使用 + Sticker + GIF
P5-008：图片消息
P5-010/P5-011：语音条录制与播放
P4-012：完整 Flutter 本地会话/消息/联系人数据库与升级迁移
P4-013：真人断网 / 杀进程 / 双端 Sync Checkpoint
P4-023：Telegram 式消息操作扩展预留
```

本轮已更新 `人工测试.md`，只保留自动化无法替代的 Windows 原生窗口、头像跨端/大图体验、真实通知、真实 LiveKit 通话、Android 键盘 Profile、Web/Windows 中文输入法、跨端未读/偏好检查，不再让人工重复跑 **51 项 Flutter 单测**或 Go 单测。

---

## 10. 当前人工测试策略

继续沿用你要求的批次策略：

> **大范围开发 → 能自动测的全部自动测 → 最后只把真实设备 / 浏览器 / 多端交互交给人工。**

新的人工验收入口：

```text
人工测试.md
```

旧：

```text
人工测试清单.md
```

继续冻结为 P0 归档，不再重复跑。

---

## 11. 当前明确未冻结的长期决策

仍包括：

- 最终开源许可证。
- ADR 最终 Accepted / Superseded 状态。
- 1.0 私聊 E2EE 精确范围和恢复模型。
- iOS APNs 生产方案。
- 公网 TURN/TLS 拓扑。
- P3 搜索 / 申请限流是否开放给管理员配置。

这些不阻碍当前 P4 真实多端验收和剩余工程收口。
