# DD 开发进度跟踪｜当前权威快照

> 更新时间：2026-08-12 04:xx
>
> 项目路径：`C:\Users\admin\Desktop\复刻微信`
>
> 事实来源优先级：当前源代码 / migration / runtime route / 自动测试 > `docs/` 当前状态文档 > 历史需求与旧 Todo。

## 1. 当前结论

DD 当前处于 **大功能扩展后的 Alpha/Beta 收敛期**。

已经形成正式代码主链：

```text
P0  Realtime foundation
P2  Auth / User / Device
P3  Contacts / Relationship / Block
P4  Reliable Messaging / Outbox / Cursor Sync
P5  Media / Voice / Video / Sticker / productivity
P6  Groups
P7  Formal Calls
P8  Moments
P9  QR
P10 Push
```

当前不能宣称 Stable 1.0，原因不是“前面模块还没写”，而是：

- **当前 Flutter 全局自动门禁已恢复**：2026-08-12 本轮重新执行 ASCII 短路径 `dart analyze --fatal-infos` 为 0 issue，Flutter 全量测试 361 PASS / 5 SKIP（真实视频样本环境变量未提供），服务端 `go test ./...` 通过；当前仍不能写成 release-green 的原因转为真人多端/真机/生产发布链验收债务；

- P6～P10 仍有真人多端/真机/弱网/公网环境验收债务；
- 2026-08-12 多个用户反馈已有代码修复，但仍处于 `FIXED-PENDING-RETEST`；
- CI 仍缺独立 Secret Scan 与 Moments/QR 指定 PostgreSQL integration step；
- P12 Admin / Abuse / Data Rights 尚未实现；
- P13 Production Self-host 尚未形成生产部署、备份恢复、升级回滚和可观测性闭环；
- P14 iOS/macOS 尚未正式交付。

> **根目录 `未开发任务.md` 只列真正还缺代码/闭环的事项；已有代码但真人未复测的功能不能重新列成“未开发”。**

## 2. 当前阶段表

| 阶段 | 状态 | 当前事实 |
|---|---|---|
| P0 | `IMPLEMENTED` | REST/WebSocket/Realtime、LiveKit/TURN PoC 基础已形成 |
| P2 | `IMPLEMENTED + AUTO-VERIFIED` | 邮箱 Auth、验证码、密码重置、Token、设备、资料隐私 |
| P3 | `IMPLEMENTED + AUTO-VERIFIED` | DDID、联系人、好友申请、Block、备注/标签/星标 |
| P4 | `IMPLEMENTED + AUTO-VERIFIED` | Outbox、Cursor Sync、Pending、已读、回复/编辑/撤回/转发/搜索/归档/置顶 |
| P5 | `IMPLEMENTED / MODULE-AUTO-EVIDENCE / GLOBAL-GATE-PASS` | 图片/GIF/Sticker/File/Voice/Video、缓存、Emoji、自定义 Sticker、Telegram Sticker Relay；当前全 App analyzer/test 已恢复通过 |
| P6 | `IMPLEMENTED / MODULE-AUTO-EVIDENCE / GLOBAL-GATE-PASS / HUMAN-PENDING` | 正式 Group domain/API/Flutter/权限/群头像/群通话入口 |
| P7 | `IMPLEMENTED / MODULE-AUTO-EVIDENCE / GLOBAL-GATE-PASS / HUMAN-PENDING` | PostgreSQL Calls、Bearer Principal、多设备接听仲裁、LiveKit token |
| P8 | `IMPLEMENTED / MODULE-AUTO-EVIDENCE / GLOBAL-GATE-PASS / HUMAN-PENDING` | Moments Feed、媒体、隐私、互动、持久未读、互动焦点 |
| P9 | `IMPLEMENTED / QR-DIRECTED-AUTO-EVIDENCE / GLOBAL-GATE-PASS / HUMAN-PENDING` | 个人码、群二维码、扫码登录；P9 定向证据历史有效，当前全 App analyzer/test 已恢复通过 |
| P10 | `IMPLEMENTED / PUSH-DIRECTED-AUTO-EVIDENCE / GLOBAL-GATE-PASS / HUMAN-PENDING` | Push job/Worker、FCM/APNs/UnifiedPush、Flutter token、Android data-only FCM；当前全 App analyzer/test 已恢复通过 |
| P11 | `OUT-OF-SCOPE` | V1 明确不开发 Production E2EE |
| P12 | `PLANNED` | Admin / Abuse / Data Rights |
| P13 | `PARTIAL INFRA` | 只有 dev infra 基础，生产闭环未完成 |
| P14 | `PLANNED` | iOS/macOS 正式交付未开始 |

## 3. 2026-08-12 本轮新增/修复事实

### 3.1 Android Push 头像与小标

状态：`FIXED-PENDING-RETEST`

已落代码：

- Android 用户可见 FCM 改为 **data-only + HIGH priority**，避免后台被系统顶层 `notification` 绕过 DD 自定义通知链。
- 后台 isolate 注册 `FirebaseMessaging.onBackgroundMessage`。
- 前台/后台通知统一进入 `AppNotificationService`。
- 支持 sender avatar large icon、DD monochrome small icon。
- 点击本地通知可恢复目标 `conversationId`。
- 服务端使用 `AUTH_TOKEN_SECRET` 生成 24 小时 HMAC sender-avatar capability URL。
- `HIDDEN` 预览不下发 avatar capability。
- Push payload 不放 Access/Refresh Token。

仍需真人：安装最新 APK 后验证前台、后台、系统杀进程三态的头像、小标和点击恢复。

### 3.2 Telegram Sticker Pack / 动态与视频表情

状态：`IMPLEMENTED / DIRECTED-AUTO-EVIDENCE / GLOBAL-GATE-PASS / HUMAN-PENDING`

当前已不是“只支持静态 WebP”：

- Telegram Relay 支持静态 WebP/PNG。
- 动态 TGS 使用 `application/x-tgsticker`，客户端 gzip + Lottie 循环渲染。
- 视频 WebM 使用静音循环视频 Sticker 播放链。
- 合法 TGS/WebM 不再计入 `unsupported_sticker_count`。
- 旧 partial pack 再次导入会强制刷新。
- Sticker 面板打开即从服务端拉用户 pack，并每 4 秒静默同步，修复同账号多端长期不一致的代码路径。
- 整包分享使用正式 `STICKER_PACK` 消息，并携带排序第一项真实缩略图；接收方未订阅也能看到预览并点击导入，普通转发保留预览/导入能力。

仍需真人：Windows ↔ Android 同账号导入/移除/刷新/重启后的真实一致性，以及真实公开 TGS/WebM Pack。

### 3.3 视频 Sticker 缓存

状态：`FIXED-PENDING-RETEST`

- `LoopingVideoSticker` 不再只依赖临时签名 URL。
- 首次读取后进入统一 `VideoFileCache` 持久缓存。
- 聊天气泡、表情候选、分享预览按 `mediaId + user namespace` 复用本地文件。
- 缓存受统一容量预算/LRU 与 30 天年龄限制。
- 地址解析/播放器打开各有超时失败态，不再永久转圈。
- BottomSheet 入场动画期间持续重算可见性，修复“刚上传可播，退出再打开永久转圈”。

### 3.4 Android 大视频选择 OOM

状态：`FIXED-PENDING-RETEST`

真机 ADB Logcat 已定位旧根因：Android 文件选择链把 128 MiB / 503 MiB 视频 materialize 成同量级 `byte[]`，在约 256 MiB Java heap growth limit 下直接 OOM。

当前修复：

```text
ACTION_OPEN_DOCUMENT
→ Android 后台 64 KiB 流式复制到 cacheDir/dd_picker
→ MethodChannel 只返回路径/名称/MIME/大小
→ poster 继续原生 scaled frame
→ upload 继续 uploadStream
```

聊天相册、文件、自定义表情 Android 入口均改走该链。仍需真人用约 100 MiB / 500 MiB 样本复测。

### 3.5 性能设置

状态：`IMPLEMENTED / DIRECTED-AUTO-EVIDENCE / GLOBAL-GATE-PASS / HUMAN-PENDING`

已新增：

```text
AppPerformanceStore
PerformanceSettingsPage
```

包含：

- 节能模式；
- 减少动画；
- 视频自动预览；
- 硬件加速视频解码。

设置持久化到设备本地；`MaterialApp`、Inline Video、Viewer、Poster/Sticker 播放链已经读取对应开关。Windows Runner 保留 `HighPerformancePreference`。

### 3.6 朋友圈互动与视频

状态：`FIXED-PENDING-RETEST`

- 新增 migration `000028_moment_activity`。
- `GET /api/v1/moment-activity` 返回未读数 + 最近互动明细。
- `POST /api/v1/moment-activity/read` 写已读状态。
- Android Footer、Windows Rail、“发现 → 朋友圈”入口显示红色 `1..99 / 99+`。
- 自己的朋友圈封面下新增“互动焦点”，点开可看谁点赞/评论、评论正文和时间。
- mark-read 不删除历史互动。
- Moment 单视频从真实授权源 probe poster/时长，并在可视区静音循环自动预览；单击进入完整播放器。

### 3.7 Windows / Android UI 与交互收口

当前代码已包含：

- Windows 系统托盘：×/Alt+F4/任务栏关闭隐藏到托盘；普通最小化仍留任务栏；托盘右键退出。
- Windows 联系人详情按参考图重做。
- Mention 资料改为 Telegram 式居中悬浮窗。
- Windows 通讯录“群聊”改为同页分栏，不再 Push 独立整页。
- 空闲群通话入口统一移入 `+` 菜单；进行中才显示紧凑加入入口。
- Android 语音输入态隐藏表情图标。
- Android 二级 Action Sheet 点击外部空白可退出。
- 文件暂停后提供“继续 + 取消”。
- Android Markdown/普通文档可走 FileProvider 打开/分享，APK 走系统安装器链。
- 图片/视频上传进度迁移到消息流缩略图中央圆环，不再占 Composer 顶部横条。
- 全局 SnackBar 预留底部安全区；Dialog/Input 统一中性视觉。
- Sticker 与普通图片缩略图尺寸分离；长按/右键收到的 Sticker 可加入我的表情。

以上真实平台视觉仍需统一真人复测，不能仅凭 Widget Test 标记 `HUMAN-PASS`。

## 4. 当前数据库 / API 快照

migration 当前推进到：

```text
000026_push_product_chain
000027_custom_sticker_video
000028_moment_activity
000029_telegram_sticker_dynamic_video
000030_sticker_pack_share
```

正式 API 已覆盖：

- Auth / User / Device
- Contacts / Relationships
- Messaging / Media / Stickers
- Groups
- Formal Calls
- Moments / Moment Activity
- QR / QR Login / Group QR
- Push preferences / endpoints / test
- Media upload cancel

旧实验 `/api/calls...` 只属于历史兼容面；新开发以 `/api/v1/calls` 为准。

## 5. 当前 CI / 自动门禁事实

`.github/workflows/ci.yml` 当前包含：

```text
server-contracts:
  gofmt
  go test ./...
  go vet ./...
  migrations
  Auth PG + Mailpit
  Contacts PG
  Groups PG
  Calls PG
  Messaging PG
  Push PG lifecycle + worker
  Redis realtime cross-node
  OpenAPI strict
  compose config

admin-web:
  typecheck
  build

flutter-client:
  analyze
  test
  Web Release
  Android Release
  artifact upload

windows-release:
  Windows Release
  real-engine MP4 poster smoke
  artifact upload
```

P11 E2EE PoC CI job已从当前主产品 CI 移除，与“V1 不开发 E2EE”的产品决策一致。

当前仍缺：

1. 独立 Secret Scan。
2. Moments 指定 PostgreSQL integration step。
3. QR 指定 PostgreSQL integration step。
4. iOS/macOS runner。
5. 真 FCM/APNs 设备级 delivery/click 只能在真实设备/受控环境验收，不能用普通 mock CI 冒充。

## 6. 仅待真人验收的重点

这些有代码，不应重复开发：

1. Android Push：前台/后台/杀进程头像、小标、点击恢复。
2. Android 100 MiB / 500 MiB 级大视频选择、poster、上传、取消/重试。
3. Telegram Pack Windows ↔ Android 同账号同步、TGS/WebM 真实公开包、整包分享首图。
4. Windows 托盘、Alt+F4、Explorer 重启、125%/150% DPI、四边四角 Resize。
5. P6 群聊多端/500 人边界/弱网。
6. P7 真公网 TURN/TLS、30 分钟语音/视频、多网络切换。
7. P8 真图片/视频连续发布、互动角标与焦点多端同步。
8. P9 真机相机扫码、群二维码与跨设备 PC/Web 登录。
9. Android 文件打开/分享/APK 安装器链、SnackBar/Dialog/Input 等本轮视觉。

## 7. 下一步开发主线

真正还需要继续写代码的事项，以 `未开发任务.md` 为唯一清单：

```text
当前真人回归暴露问题 Stop-the-line 修复
→ CI Secret Scan
→ Moments / QR 指定 PostgreSQL CI 门禁
→ P12 Admin / Abuse / Data Rights
→ P13 Production Self-host
→ P14 iOS / macOS
```

任何真人回归暴露的新 `KNOWN-FAILURE` 都应临时提升为最高优先级 Stop-the-line 修复。
