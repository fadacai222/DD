# AI04 Bugfix Report

状态：`FIXED-PENDING-RETEST`

基线：`396e672339429100ae5288f3408e20f9d3858dcd`

## B09：首页会话左右滑连键

- `_ConversationSwipeActions` 新增显式 `closed / leadingOpen / trailingOpen` 离散状态。
- 从关闭态可以按原有阈值/速度打开任一侧。
- 已打开一侧后，本次反向 gesture 的 offset 被限制到 `0`，最多只能关闭，不能继续穿越并打开另一侧。
- 回到 `closed` 后，下一次新的反向 gesture 才能打开另一侧；两个方向规则完全对称。
- action 成功或失败都通过 `finally` 回到 `closed`。
- busy 期间不接受拖动，并把 offset 收敛到当前离散状态，避免半开。
- drag cancel 回到本次 gesture 开始前的离散状态，不跳到另一侧。
- 原有置顶/已读、静音/归档/删除操作保持不变。

## B10：朋友圈评论颜色合同

- 评论作者名和 `回复@xxx` 回复对象继续固定使用 `0xFF576B95` 蓝色。
- 评论正文与分隔文本显式使用 DD 普通正文色，不再继承可能为 danger/red 的 `DefaultTextStyle`。
- 评论按钮继续使用蓝色。
- 已点赞红心继续允许 `DdColors.danger`。
- 删除动作与 error/danger 视觉语义未修改。

## 自动验证

- `dart format --output=none --set-exit-if-changed`（4 个 Dart 目标文件）：PASS，0 changed。
- `flutter test test/features/messaging/conversations_page_test.dart test/features/moments/moments_feed_page_test.dart`：PASS，20/20。
- `dart analyze --fatal-infos`：PASS，0 issue。
- B09 回归覆盖双方向完整序列、反向手势不能穿越、drag cancel、action 后关闭。
- B10 回归用红色 body text theme 主动污染环境，验证评论富文本仍保持正文色/蓝色，并验证点赞后的红心仍为 danger。

## 人工复测项

- Android 真机：连续执行“左开 → 右关 → 新右开 → 左关”及反向序列，确认手感和动画无跳边。
- Windows/其他现有客户端：确认指针拖动语义未出现回退。
- 朋友圈明/暗主题：确认评论正文、作者/回复名、评论按钮视觉正确，点赞红心和危险操作仍保持红色语义。

## 明确未触碰

- `text_chat_page.dart`
- Messaging server/API
- Push / Calls
- 全局主题重构
- master 合并/rebase
