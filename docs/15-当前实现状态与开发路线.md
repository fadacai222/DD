# DD 当前实现状态与开发路线

> 更新时间：2026-08-15（Wave2 集成 `6ac4e6e`；Wave3 真人修复已合流；Admin Relay 已提交；真人复测待完成）
>
> **后续每个 AI / 开发者开始工作时，在 `docs/README.md` 之后首先读取本文件。**
>
> 本文件只回答：**现在真实做到哪、当前哪里坏、下一步先做什么。**

---

# 1. 当前版本判断

DD 已经跨过“基础聊天 Demo”阶段，当前属于 **大功能快速扩展后的 Alpha/Beta 收敛期**。

Wave1 已完成物理合流并继续形成 Wave2 集成分支 `integrate/2026-08-14-wave2 @ 6ac4e6e6c2d37a234cde3219bb4ce5685a0c5298`。Wave2 已包含注册倒计时、Realtime resume 恢复、群通话生产配置、Android killed-process Push foundation、会话 Swipe/Moments 修复、Telegram Sticker 错误闭环，以及 viewer-relative 备注名、durable group mentions、Voice 播放/STT foundation、Windows clipboard image adapter、iOS Live Photo paired media。

2026-08-14 真人复测新增 8 个明确失败后，Wave3 单写者已继续处理用户可见热点：群昵称弹窗 framework 红屏、置顶会话透明透出快捷按钮、备注保存后的会话缓存刷新、语音转文字改为长按菜单、Android CALL 类别通知，以及 @提醒/Voice/Clipboard/Live Photo/B17 UI 接线。当前自动证据为 Flutter 530 PASS / 5 条件 SKIP / 0 FAIL、analyzer 0，相关 server 定向 test/vet PASS；所有真人未复测修复仍只能标 `FIXED-PENDING-RETEST`。

当前 stop-the-line 阻塞集中在生产链：Android 真后台 Push 依赖 FCM Worker 正式投递、STT endpoint/model 尚未配置、群通话需在 Wave2 服务端版本上重新真人验证。生产 FCM service-account 已确认属于 `dd-chat-3701e`，LiveKit runtime secret 校验通过；生产 API/Worker 仍需从 `0.4.0-cloud.3` 升级到 Wave2 后复测。Admin Telegram Sticker Relay 已提交到 `master @ 195eae7`，并通过独立 `integrate/2026-08-15-admin` 接入 Wave2 基线。

已经有正式业务主链：

```text
P0  Realtime foundation
P2  Auth / User / Device
P3  Contacts / Relationship / Block
P4  Messaging / Outbox / Cursor Sync
P5  Media / Voice / Video / Sticker / productivity
P6  Groups
P7  Formal 1:1 Calls
P8  Moments
P9  QR server + Flutter implementation（定向自动证据已恢复，真人待验收）
P10 Push（主体 + U08 provider 运维已实现，真实 Android FCM / iPhone APNs delivery/click 待真人验收）
```

另外，根目录已提供 `download-site/` 纯静态客户端下载落地页：桌面端展示 Windows / Android 下载与 iOS / macOS“敬请期待”，Android 端仅显示 APK 下载，iOS / macOS 仅显示对应“敬请期待”；Banner 为仓库内自带 SVG 资源。

尚不能称 Stable 1.0，因为：

- **当前 U30 总集成自动门禁已通过**：2026-08-13 `dart analyze --fatal-infos` 0 issue，iOS/Push/Media/QR/Calls 定向合同持续通过，Flutter 全量 459 PASS / 5 SKIP / 0 FAIL，`go test ./...` / `go vet ./...` PASS，PostgreSQL 18.4 33 migrations 后 Auth+Push lifecycle PASS；当前发布阻断主要是真实设备、签名 Secret 与生产环境验收债务；
- P9 QR 的定向 analyzer/tests/build 历史证据已恢复，但真人扫码/相机/跨设备登录仍未验收；
- P10 服务端/客户端 Push 主体与 U08 provider 运维代码已完成，但真实 Android FCM / iPhone APNs delivery/click 与 credential rotation 仍是 `HUMAN-PENDING`；
- P11 E2EE 已明确退出 V1，不再作为发布阻塞项；
- P12 Admin/Abuse/Data Rights 主链已 `IMPLEMENTED + AUTO-VERIFIED`：独立 Admin auth/security secret、MFA/RBAC/CSRF、Governance 与 Data Rights 已完成；普通用户 MFA/Passkey 仍是 P2 扩展；
- P13 U20-U25 已形成 Production Compose、TURN/TLS、DR、upgrade/rollback、Observability 与正式 Release provenance 代码闭环；U25 已加入 exact-SHA CI/Secret Scan 证据、SemVer/tag/changelog、四平台 artifact、SHA-256、SPDX SBOM、Trivy HIGH/CRITICAL gate、Sigstore/GitHub Attestation、原生签名 Secret contract、Production Environment approval 与 rollback retention。真实 Android/Windows 生产证书、GitHub Environment reviewers、公网跨运营商/移动网络、真实规模 RPO/RTO、真实告警接收方仍是 `SECRET/HUMAN-PENDING`；
- P14 iOS U30 已完成 iOS 15.0 Runner、Push/Auth lease、Media/File/Camera/QR、LiveKit/CallKit/Audio 与公共 native Xcode/registrar/AppDelegate 本地总集成并 `LOCAL-AUTO-VERIFIED`；真实 macOS/Xcode、Apple/Firebase Secret、TestFlight/APNs/iPhone/iPad 仍待证据，macOS U31 尚未正式交付；
- P6/P7/P8 仍有真人多端、弱网和规模验收债务；
- R15 历史 UI/通知/Windows/Android 修复仍有部分 `FIXED-PENDING-RETEST`。

---

# 2. 状态词

```text
IMPLEMENTED
AUTO-VERIFIED
HUMAN-PASS
FIXED-PENDING-RETEST
KNOWN-FAILURE
IN-PROGRESS
PLANNED
```

规则：

- `IMPLEMENTED` 只说明代码存在；
- `AUTO-VERIFIED` 必须对应当前可重复自动证据；
- 当前门禁重新失败时，旧 `AUTO-VERIFIED` 必须撤销/降级；
- 目标平台没真人复测不能写 `HUMAN-PASS`。

---

# 3. 当前数据库进度

migration 当前到：

```text
000001_instance_settings
...
000016_stickers
000017_groups
000018_calls
000019_moments
000020_calls_conversation
000021_qr
000022_push
...
000026_push_product_chain
000027_custom_sticker_video
000028_moment_activity
000029_telegram_sticker_dynamic_video
000030_sticker_pack_share
000031_admin_auth
000032_admin_governance
000033_data_rights
000034_message_mentions
```

重要：

- `000017`～`000034` 当前相关 migration 均按 forward-only 继续演进；
- `000021_qr` 已做真实 PostgreSQL roundtrip；
- Push 基础 schema 来自 `000022_push`，产品链补充由 `000026_push_product_chain` 前向扩展；Push PostgreSQL lifecycle integration 已通过。

已发布/已经应用过的历史 migration **禁止回头改 checksum**；新增变化必须继续 forward-only migration。

---

# 4. 当前正式 Go 模块

```text
server/internal/auth
server/internal/contacts
server/internal/groups
server/internal/calls
server/internal/messaging
server/internal/moments
server/internal/media
server/internal/stickers
server/internal/qrcode
server/internal/realtimebus
server/internal/realtimev1
server/internal/push
server/internal/admin
server/internal/datarights
server/internal/observability
```

P10 Push 已形成正式 domain/service、HTTP API、provider 与 Worker consumer。

---

# 5. R15：历史真人 Bug 收敛

此前真人集中反馈过：

- Windows 无边框 Resize/灰块/标题栏；
- Windows 系统托盘与关闭生命周期：× / Alt+F4 / taskbar close 隐藏到托盘，普通最小化保留任务栏，托盘右键退出；
- Windows 性能/GPU 选择：Runner 已显式设置 `HighPerformancePreference`，多 GPU 设备优先高性能适配器；源码合同测试与 Windows Release 构建已通过，真人卡顿/“未响应”仍待复测；
- 正常联系人资料误报用户不可用；
- 视频缩略图；
- Android 聊天视频播放器：当前页可见视频应静音循环自动预览、单击缩略图直接放大、全屏必须有退出入口、系统分享必须可用；
- Sticker library/Telegram packs；
- Android Heads-up/icon；
- 消息复制操作；
- 深色模式入口；
- Android 悬浮 Footer 快速切换闪烁；
- Android 消息长按二级操作菜单点击外部空白区无法退出；
- 群聊空闲态在 Windows / Android / Web 等现有客户端统一把群语音/群视频发起入口从输入框上方移入 `+` 更多菜单；只有进行中的群通话保留紧凑加入入口；
- Android 切到“按住说话”语音输入态时不应继续显示表情图标；
- 全局短反馈 SnackBar/Toast 不允许贴底或覆盖聊天输入框、悬浮 Footer、底部操作按钮；
- Windows/通用群详情更换或移除群头像时，`PATCH /api/v1/groups/{id}` 仅携带 `avatarMediaId` 不得被误判为“空更新”；2026-08-12 已修复服务端字段判定并加入回归测试；
- Windows 通讯录左栏点击“群聊”不允许再 Push 成独立整页：必须保留通讯录分栏与左侧导航，高亮“群聊”，右侧 content pane 内嵌群聊搜索/列表；只有点击具体群会话后才切回消息区；
- Windows 联系人右侧详情已按用户参考图重做：移除“头像居中 + 多个胶囊按钮”，改为横向身份区、朋友资料/朋友圈/更多信息分组、右上更多菜单，以及底部无边框的消息/语音/视频图标动作；未提供的地区/来源数据不做假占位；
- Windows 点击消息内用户 Mention 时，详细资料改为 Telegram 式居中悬浮窗，不再塞进窄右侧 Inspector；同时修复 `PeerProfilePage` 嵌入窄容器时错误按整窗宽度计算 padding 导致内容被压扁的问题；
- Windows 全局新增 `Esc` 返回上一层：由窗口层统一把未被局部控件消费的 Escape 转成根 Navigator `maybePop`；Mention 候选、视频全屏等更具体的 Escape 行为优先，主导航根层 Escape 不退出、不缩托盘、不切换一级栏目；对应窗口快捷键/路由回退 Widget 回归已覆盖，本轮 Windows Release 已重新构建成功；
- Windows Ctrl+V 剪贴板图片本轮已完成独立 foundation：复用既有 `pasteboard`，新增可注入 `WindowsClipboardImageAdapter` 与 `WindowsClipboardPasteDecider`，支持 bitmap bytes / Explorer 图片 file path、图片优先于文本、typed error、96 MiB/像素边界、非 Windows 旁路与 key-repeat 去重；adapter 可直接输出 `XFile` 接入既有 `_sendImageFiles(...)`，不新造上传链。定向测试 10/10、`dart analyze --fatal-infos` 0 issue；最终 `text_chat_page.dart` Ctrl+V 热点接线留给 Wave 3，因此用户可见状态仍为 `IN-PROGRESS / FIXED-PENDING-RETEST`。
- 聊天内文件传输暂停后，传输条必须同时提供“继续”和“取消传输”，不能把用户困在只能恢复的状态。
- Android 下载文件打开/分享曾因 `files/DD/transfers/` 未进入 `FileProvider` 白名单而报 `Failed to find configured root`；Markdown/普通文档必须能打开与系统分享，APK 还必须进入系统包安装器链。
- 图片/视频发送不得再在 Composer 上方出现横向传输条；发送中应直接在消息流显示本地缩略图 + 圆环进度/取消，视频在 Android 可视区域内边上传边使用本地文件静音循环播放。
- 自定义表情必须支持 64 MiB 以内 PNG/WebP/GIF/MP4/WebM；16 MiB 级 GIF/MP4 不得因历史失败上传 reservation 累积而误报 `MEDIA_QUOTA_EXCEEDED`，MP4/WebM 在聊天中按静音循环 Sticker 播放、离开可视区暂停。2026-08-12 修复“刚上传视频表情可播放，但关闭并重新打开表情面板后候选格永久转圈”：根因是 ModalBottomSheet 入场动画首帧把视频 Sticker 判为不可见，而面板未滚动时后续没有可见性信号；`LoopingVideoSticker` 现监听所属 Route 动画持续重算可见性，地址解析提前于播放器创建，并对地址解析/播放器打开增加 15 秒失败收口与客户端日志，避免无限加载。视频 Sticker 同时正式接入 `VideoFileCache`：首次需要时按 `mediaId + 用户 namespace` 下载到 DD 持久视频缓存，聊天气泡、表情候选格和表情包分享预览复用同一份本地文件，重新打开面板不再依赖新的签名 URL 重新拉完整媒体；缓存继续受统一容量预算/LRU 与 30 天视频缓存年龄约束。新增 Widget 回归证明“已有 MP4 自定义表情直接重新打开面板”会重新触发媒体解析，并把真实 `sizeBytes` 传给缓存校验。
- 聊天缩略图必须区分 Sticker 与普通图片：移动端 Sticker 最大边约 176px（桌面内嵌约 220px），保持完整比例且不走图片式 `cover` 裁切；长按/右键对方仍可见的 Sticker 可直接“添加到我的表情”，服务端只允许复用当前用户有权访问的未撤回 STICKER 消息媒体。
- 全局 AlertDialog / 表单输入框不得继续使用绿色 seed tint + 超大胶囊圆角 + 聚焦整圈绿色描边；朋友圈正文编辑区必须保持无框，评论/回复/资料编辑统一改用中性控件 token。
- 朋友圈单视频不得再显示长期黑色“视频”占位：Feed 对已有 `MOMENT_VIDEO` 从授权视频源 probe 真实 poster/时长并复用 `InlineVideoPreview`；达到可视阈值后静音循环自动播放、同屏只播放最可见一条、滚出可视区暂停，单击直接打开完整播放器；poster probe 失败时仍保留可点击播放兜底。
- 朋友圈点赞/评论/回复不能只发一次通知：发现 Footer/桌面 Rail 与“朋友圈”入口必须保留红色 `1 / 2 / ... / 99+` 未读角标，直到用户进入朋友圈查看；重启、漏 Realtime/Push 后仍能从服务端恢复。自己的朋友圈封面下方还必须保留“互动焦点”，点开可查看最近谁点赞、谁评论、评论正文与时间；清未读只改 read 状态，不能顺带删掉互动历史。
- 2026-08-14 B09/B10 回归修复：Android 会话列表左右滑已改为显式 `closed / leadingOpen / trailingOpen` 状态机；一侧打开后，同一次反向手势最多只能关闭，必须在已经回到 `closed` 后用下一次新手势才能打开另一侧，两个方向对称；action 完成、busy 与 drag cancel 都会收敛到离散状态。朋友圈评论作者/回复对象继续使用 `0xFF576B95` 蓝色，评论正文显式锁定普通正文色，评论按钮保持蓝色；已点赞红心以及删除/error 的 `danger` 语义不变。双向完整滑动序列、cancel/action 收口与“红色文本主题不得污染评论”Widget 回归已覆盖，当前状态 `FIXED-PENDING-RETEST`，仍需 Android/Windows 真实手势与朋友圈明暗主题人工复测。
- 需求0812 二次回归收口新增性能设置：`AppPerformanceStore` 作为设备本地事实源，提供节能模式、减少动画、视频自动预览与硬件加速视频解码；`MaterialApp` 将减少动画写入 `MediaQuery.disableAnimations`，视频 viewer / inline preview / poster probe 实际读取硬件解码开关，视频自动预览受节能/自动播放开关约束。Windows Runner 继续使用 `HighPerformancePreference`。2026-08-12 后续设置层级重排后，`外观 / 聊天背景 / 媒体与缓存 / 性能 / 传输中心` 已全部迁到“我的”外层；内层设置改名“隐私与设备”，不再重复这些入口。
- 需求0812 新增账号/表情/链接闭环：删除“刷新登录会话 / 切换账号”两个独立入口，改为“账号管理”；Native 客户端按 `origin + userId` 把最多 8 个 Refresh Token 独立存入现有安全存储，已有有效会话可直接切换，失效槽位回落密码登录，“添加账号”进入登录页但不主动注销原账号。发现页删除“更多能力”，替换为正式“表情”入口，复用自定义 Sticker + Telegram Pack 导入/移除/分享/整理链。文本消息中的 HTTP/HTTPS URL 使用蓝色富文本，首链接可加载 `/api/v1/link-preview` 元数据卡，点击链接文字或预览卡统一交给系统外部浏览器/处理器；预览服务端强制 Bearer 鉴权、只允许 80/443 公网 HTTP(S)，DNS 后再次拦截私网/回环/链路本地等地址，最多 5 次安全重定向、5 秒超时、512 KiB HTML 上限。相关 Flutter 78 项回归、Dart analyzer 0 issue、服务端 `go test ./...` 已通过，Android Debug APK 与 Windows Release 均已完整构建；Web 按既有安全模型不持久化多份 Refresh Token，因此直接切换账号会回落重新认证。
- 2026-08-13 正式客户端默认 API origin 已统一为 `https://api.85746.pro`：首次启动、无本地会话进入登录页、直接打开 Auth 页面时均默认使用该公网地址；本地 Realtime/Calls/PoC 调试页继续保留 `127.0.0.1:18473`，避免破坏开发联调入口。
- 2026-08-14 注册验证码发送 UX 已修复：只有 `/api/v1/auth/register/email/send-code` 成功返回后才开始 60 秒独立倒计时，按钮进入禁用态并显示剩余秒数，失败请求不触发假 cooldown；Timer 在页面销毁时取消，登录/注册 Tab 切换不会重复创建；成功提示统一为“验证码发送成功，收不到就去邮箱垃圾箱看看”。服务端既有 60 秒限流不变，当前状态 `FIXED-PENDING-RETEST`，待真人确认禁用灰色视觉与文案。
- Telegram Sticker 本轮补齐传播、跨端刷新和动态格式：整包分享现使用正式 `STICKER_PACK` 消息，客户端提交该包排序第一项的媒体引用，服务端再次校验它确实是首项、挂入 `message_media` PRIMARY，并生成 `dd://stickers/telegram/...` 导入元数据；因此接收方尚未订阅表情包时也能直接看到首个表情真实缩略图，点击仍调用 Telegram pack import，普通转发也能继承媒体授权与缩略图。旧 TEXT 分享卡继续兼容但只能显示兜底图标。表情面板打开即从服务端拉取 `user_sticker_packs`，并每 4 秒静默同步。Relay 现接受静态 WebP/PNG、动态 TGS（`application/x-tgsticker`）和视频 WebM；TGS 在 Flutter 端使用 gzip Lottie decoder 循环播放，WebM 复用 `LoopingVideoSticker`；旧版 `unsupported_sticker_count > 0` 的 partial cache 再次导入会强制刷新。GIF 候选不再显示“GIF”文字占位，直接播放真实动图；新增 16 MiB GIF 流式回归测试。
- Android 大视频发送已于 2026-08-12 用真机 ADB Logcat 抓到实际崩溃根因：并非 `uploadStream` 或 poster probe 本身，而是 Android 文件选择返回阶段会先按文件大小 materialize 整个 `byte[]`，约 128 MiB / 503 MiB 视频分别触发同量级 Java 堆申请，在约 256 MiB growth limit 下直接 OOM。聊天相册、文件、自定义表情的 Android 选择入口已改为 DD 原生 `ACTION_OPEN_DOCUMENT` + 后台 64 KiB 流式复制到 `cacheDir/dd_picker`，MethodChannel 只返回路径/名称/MIME/大小；poster 继续使用 `MediaMetadataRetriever` scaled frame，上传继续 `uploadStream`。大文件选择合同测试、相关 analyzer 和标准 Android Debug APK 构建均通过；2026-08-12 还复现并定位了 Windows 主机上旧 Gradle daemon 的 `PreCreateOutputParentsStep / File.parent == null` 脏状态，`scripts/build-client.ps1` 的 Android 构建阶段现强制关闭 Gradle daemon，随后用项目正式 `-Target android` 入口成功重建 APK。当前状态 `FIXED-PENDING-RETEST`，仍需同一真机用 100 MiB/500 MiB 级样本人工复测。
- 群会话 API 新增可选 `lastMessageSender`，群列表预览显示“昵称：内容”，群头像叠加群组标识；朋友圈本轮继续收口真圆角标、头像进入个人朋友圈、昵称优先、PC 刷新按钮与单视频等比例展示。

朋友圈互动角标本轮新增 `000028_moment_activity`、`GET /api/v1/moment-activity` / `POST /api/v1/moment-activity/read`、Realtime `moment-*` reason 刷新以及 Flutter 发现 Footer/桌面 Rail/朋友圈入口三处红色数字角标；Push 只负责提醒，不再充当未读事实源；进入朋友圈立即清除现有角标并写回服务端。2026-08-12 进一步把 Activity API 扩展为未读数 + 最近 30 条仍可见互动明细，Flutter 自己的朋友圈封面下新增“互动焦点”，点击后显示头像、昵称、点赞/评论正文与相对时间；mark-read 后历史仍保留。该项代码与定向 API/Feed 测试已完成，但真机/桌面和多端一致性尚未真人复测，状态仍是 `FIXED-PENDING-RETEST`。

代码已经有针对性修复，其中聊天视频 poster 还有 Windows Release Engine smoke 自动证据；朋友圈视频黑占位/不自动播放已于 2026-08-12 改为远程 poster probe + 可视区静音循环自动播放，并增加 Moments Feed Widget 回归；2026-08-12 Android 聊天视频预览新增前台/可视区域仲裁，同一时刻仅最主要视频静音循环，Android 单击缩略图直接进入 viewer；Android viewer 全屏改为同页沉浸式系统 UI，保留 DD 顶部退出按钮并让系统返回键优先退出全屏；视频分享改为 Dart 下载到本地传输缓存后通过 FileProvider 调系统 Share Sheet，修复 `dd_shared_files/` / `DD/transfers/` FileProvider 路径合同，本轮播放器/分享定向测试 6/6 通过、相关 Dart analyze 0 issue，并通过仓库标准 `scripts/build-client.ps1 -Target android` 短路径门禁产出最新 Debug APK；真机仍待复测；2026-08-11 系统托盘新增了 Flutter 标题栏 contract test，并通过 Windows Release build；2026-08-12 群聊空闲态群通话入口已统一移入 `+` 更多菜单：原先仅 Android 生效导致 Windows 仍在输入框上方显示“发起群通话”横条，本轮已去除平台限制，并新增 Windows 回归测试；Android 原回归继续通过，进行中群通话仍保留加入入口；同日语音输入态隐藏表情入口也已有代码/测试改动；Windows 群聊目录已改为通讯录同款分栏内嵌逻辑，并新增“桌面点击群聊不触发整页回调、侧栏继续存在、右侧群内容可见”以及 GroupChatsPage embedded 无 AppBar 的 Widget 回归测试；Android `DdActionSheet` 已补显式外部空白 dismiss hit layer，并加入 Widget 回归测试，避免透明布局层吞掉菜单外点击；聊天内暂停中的文件传输条已补齐红色取消按钮 + 继续按钮，并有定向 Widget 测试覆盖取消后进入 `canceled` 状态；Android 文件链已补齐窄范围 `FileProvider` 的 `DD/transfers/` / `dd_shared_files/` 路径，打开/分享继续使用临时只读 `content://` URI；APK 识别 `.apk` 后改走系统 `ACTION_INSTALL_PACKAGE`，Manifest 声明 `REQUEST_INSTALL_PACKAGES`，对应 Android contract test 2/2 通过且 Debug APK 构建已通过；图片/视频上传已从 Composer 横向传输条迁移为消息流内乐观缩略图，缩略图中央圆环展示真实字节进度并可直接取消，图片使用本地压缩结果预览，视频 probe 后使用本地文件作为可视区静音循环播放源，定向 Widget 测试已覆盖图片上传不再出现旧横条、圆环 25% 进度与取消闭环；自定义 Sticker 已扩展到 PNG/WebP/GIF/MP4/WebM（单项 64 MiB），并允许从 Telegram Pack/已收到消息保存 TGS；Telegram Relay 已通过 `000029_telegram_sticker_dynamic_video` 补齐 TGS/WebM 数据库 MIME 约束与旧 partial cache 失效，服务端对 TGS gzip/Lottie 和 WebM 容器做内容校验；失败/取消/重试上传会调用新的 upload cancel API 释放 reservation，避免旧 reservation 把 32 个 active slot / 512 MiB reserved quota 卡死；MP4/WebM Sticker 已接入 `media_kit` 静音循环播放并在滚出可视区域后暂停，TGS 已接入 Flutter Lottie 循环渲染；聊天 Sticker 缩略图已从普通图片尺寸拆分为独立最大边规格，并补充“收到的 Sticker 长按加入我的表情”客户端动作与服务端可见消息媒体授权；群通话 Windows/Android 两条定向 Widget 回归当前 2/2 通过，`text_chat_page.dart` + `text_chat_page_test.dart` 定向 analyzer 为 0 issue；Telegram 表情包分享测试已移除会被 4 秒静默同步计时器永久拖住的 `pumpAndSettle`，改为有界 pump，并新增“分享发送 STICKER_PACK + 首表情缩略图渲染”回归，`text_chat_page_test.dart` 当前 39/39 全文件通过；PostgreSQL 集成覆盖已补但本机因未配置 `DD_STICKERS_TEST_DATABASE_URL` 跳过；此前 GIF/MP4 Sticker 的 Flutter media/sticker tests 与 analyzer 证据继续有效；消息通知摘要已禁止泄露 `[STICKER]` 等协议枚举，前台 banner/Windows 自定义通知与后台 Push 改用 `贴纸/图片/GIF/视频/语音消息/文件` 等用户可读文案，并增加 Flutter/Go 定向回归测试。全局 `SnackBarTheme` 已统一预留 88px 底部安全区，定向 Widget Test 验证短反馈位于持久底部控件区域上方；当前源码未发现 `fixed SnackBar` 或显式 SnackBar `margin` 绕过该规则。全局 Dialog/Input 视觉已改为中性表面、10px 输入圆角、14px 弹窗圆角、灰色聚焦边界，朋友圈正文编辑区显式隔离全局输入边框，主题/朋友圈/资料编辑定向测试通过。Windows 联系人详情新增独立 `desktop_contact_detail_reference_test.dart`，并与 `contacts_page_test.dart` 合计 8/8 Widget 测试通过，定向 `dart analyze` 0 issue。Windows Mention 资料悬浮窗已补独立 Dialog Widget Test、Mention 点击路由回归和 340px 窄容器布局回归，三项定向测试均通过；此前 Shell/Sticker 类型可见性曾导致 App analyzer 回归，现已补齐类型引用并重新执行本轮 `dart analyze --fatal-infos lib ...` 为 0 issue。托盘图标/Alt+F4/Explorer 重启/右键退出、本次 Windows 群聊目录视觉、本次联系人详情真实 Windows 视觉、本次 Windows Mention 悬浮窗视觉、本次 SnackBar Windows/Android 视觉、本次通知真机/桌面视觉、本次 Android Markdown 打开/分享与 APK 系统安装器链以及本次全局控件视觉尚未真人覆盖，因此对应真实平台验收仍保持：

```text
FIXED-PENDING-RETEST
```

后续大功能开发不能把这些真人债务删掉；发布前仍需统一回归。

---

# 6. R16：工程门禁收口

已经完成主体：

- runtime route ↔ OpenAPI path/method contract；
- Redocly strict；
- Windows 中文路径 analyzer 的 ASCII `subst` 规避；
- Windows Release CI；
- Android APK CI；
- Web/Android/Windows artifact upload；
- Windows real-engine MP4 poster smoke；
- Group/Calls PostgreSQL integration job。

历史 `Unexpected end of input` 已不再是当前 Flutter 门禁问题；P9 QR 的 analyzer/test/build 阻断已于 2026-08-11 04:42 修复并重新验证。

---

# 7. P6：Group

状态：

```text
IMPLEMENTED + AUTO-VERIFIED
HUMAN-PENDING
```

已实现：

- migration `000017_groups`；
- `server/internal/groups`；
- 创建群；
- Owner/Admin/Member；
- 邀请成员；
- 踢人；
- 退出；
- 群主转让；
- 解散；
- 群名称/公告；
- 可编辑群头像（上传 `GROUP_AVATAR` 媒体后可单独设置/移除；2026-08-12 修复 avatar-only PATCH 误判）；
- 群昵称；
- INVITE_ONLY / APPROVAL；
- join request/approve/reject；
- Group Outbox；
- GROUP Messaging/Sync/read/unread/media/sticker；
- `@member` 仅 active member；
- `@all` 仅 OWNER/ADMIN；
- `000034_message_mentions` 持久化当前 viewer 的 GROUP mention 事实；conversation summary 返回 `latestUnreadMentionMessageId` / `latestUnreadMentionSequence`，read cursor 越过目标后自然消失，edit/recall 同事务同步索引；
- Flutter 发起群聊、群会话、群聊天 sender identity、群详情权限管理。

自动证据：真实 PostgreSQL `TestGroupLifecycleWithPostgres`，以及 Group HTTP/OpenAPI/Flutter 定向测试；`TestUpdateGroupInputHasChanges` 覆盖“仅设置/仅移除群头像也属于有效 PATCH”。2026-08-14 AI07 另以 PostgreSQL 18.4 实跑 durable mention lifecycle 与 `000034` migration up/down roundtrip PASS；最终会话列表/聊天页提醒 UI 未在本分支接线，状态 `FIXED-PENDING-RETEST`。

仍需：

- Windows/Android/Web 真人三端；
- 500 人边界；
- 弱网/重启/成员变化同步；
- 群详情视觉和滚动手感。

---

# 8. P7：正式一对一 Calls

状态：

```text
IMPLEMENTED + AUTO-VERIFIED
HUMAN-PENDING
```

已从旧实验架构完成：

```text
内存 CallStore
+ 客户端自报 caller identity

→

server/internal/calls
+ PostgreSQL calls
+ Bearer Principal
+ /api/v1/calls
```

已实现/验证：

- caller device 绑定；
- callee 多设备同时 ringing；
- 第一台 accept 设备胜出；
- losing device 无权取 LiveKit token/挂断；
- contact required；
- Block；
- busy；
- reject/timeout/end；
- authenticated `/api/v1/realtime`；
- Call 绑定 DIRECT conversation；
- 通话终态 SYSTEM message + Outbox 服务端事务主体。

仍需：

- 真公网 TURN/TLS；
- 30 分钟语音/视频；
- Windows ↔ Android/Web 多网络；
- Wi-Fi/移动网络切换；
- 后台来电与 P10 Push 联动；
- 多 API 节点/LiveKit restart 场景。

---

# 9. P8：Moments

状态：

```text
IMPLEMENTED + AUTO-VERIFIED
HUMAN-PENDING
```

已实现：

- migration `000019_moments`；
- `server/internal/moments`；
- Feed；
- 文字；
- 最多 9 图；
- 单视频；
- 视频 probe/poster；Feed 对历史/现有 `MOMENT_VIDEO` 可从授权视频源生成 poster，并在可视区静音循环自动播放（单击打开完整播放器、滚出可视区暂停）；
- `MOMENT_IMAGE/MOMENT_VIDEO` 私有媒体；
- 点赞/取消；
- 评论/回复/删除；回复评论在 Feed 中按 `回复@DDID：内容` 保留回复对象上下文（2026-08-12 修复，`FIXED-PENDING-RETEST`）；
- 动态删除；
- ALL_CONTACTS / PRIVATE / EXCLUDE；
- “不看他 / 不让他看”；
- Block；
- 删除好友后旧动态重新失权；
- 非互相好友互动身份隔离；
- Moment Outbox；
- Flutter 发现→朋友圈→发布→Feed。

自动证据：真实 PostgreSQL隐私生命周期、HTTP/OpenAPI、Flutter Moments API/Feed/Publish 定向回归；2026-08-12 新增朋友圈视频 poster + `InlineVideoPreview` 自动播放配置回归，并与共享 Inline Video 测试合计 9/9 通过。

仍需真人：真图片/视频连续发布播放、弱网、大图/长视频、Android/Windows/Web UI。

---

# 10. P9：QR

## 当前总状态

```text
SERVER:  IMPLEMENTED + AUTO-VERIFIED
FLUTTER: IMPLEMENTED + QR-DIRECTED-AUTO-EVIDENCE / GLOBAL-GATE-PASS
OVERALL: AUTO-GATE-PASS / HUMAN-PENDING
```

## 服务端已实现

migration：

```text
000021_qr
```

正式能力：

- 实例绑定个人 QR；
- 群二维码 invite；
- Owner/Admin 签发；
- 随机 nonce；
- expire/revoke/maxUses；
- Block/capacity；
- redeem + member + Group Outbox + use_count 同事务；
- PC/Web QR login；
- 登录 nonce 数据库只存 SHA-256；
- 2 分钟级短 TTL；
- scanner user/device binding；
- target device name/platform/appVersion；
- 手机 approve/reject；
- target Session creation + consume 同事务；
- second consume 不生成第二套 Session；
- secret nonce 通过 POST body，不进入 URL path/query。

真实 PostgreSQL、HTTP、OpenAPI、migration roundtrip 已通过。

## Flutter 已写代码

```text
features/qrcode/domain/dd_qr_payload.dart
features/qrcode/data/qr_api_client.dart
features/qrcode/presentation/qr_code_card.dart
features/qrcode/presentation/my_qr_page.dart
features/qrcode/presentation/group_qr_page.dart
features/qrcode/presentation/qr_scanner_page.dart
features/qrcode/presentation/qr_login_page.dart
```

入口已开始接：

- 我的二维码；
- 发现→扫一扫；
- 群二维码；
- 登录页扫码登录。

依赖：

```text
qr_flutter
mobile_scanner
```

## 2026-08-11 04:42 历史定向自动验证

本轮已修复 MainShell/QR 接线、Contacts/PeerProfile import、我的二维码入口、QR API lint、group redeem fixture 与 scanner test platform restore。

重新验证：

```text
dart analyze --fatal-infos = 0 issue
QR 4 个定向测试文件 = 10/10 PASS
flutter build windows --release = PASS
flutter build web --release = PASS
flutter build apk --debug = PASS
```

同时修复 Windows 单实例保护：历史残留 `im_client.exe` 即使没有顶层窗口，也不再让后续双击静默退出。

### 下一步第一优先级

P9 自身的旧 QR 定向阻断已经解除，2026-08-12 当前全 App analyzer/test 也已重新通过。下一步直接继续 P9 真人验收：

- Android 相机扫码与权限拒绝/恢复；
- Windows 手动 payload fallback；
- 手机扫描并确认 PC/Web 登录；
- 群二维码过期/撤销/次数耗尽；
- 不同实例二维码拒绝；
- 多端真实账号状态同步。

---

# 11. P10：后台 Push

状态：

```text
SERVER: IMPLEMENTED + AUTO-VERIFIED
ANDROID FIREBASE BUILD: HISTORICAL-AUTO-EVIDENCE
FLUTTER APP GATE: AUTO-GATE-PASS
REAL DEVICE DELIVERY: HUMAN-PENDING
OVERALL: AUTO-GATE-PASS / HUMAN-PENDING
```

已经完成：

1. `000022_push` 基础 schema + `000026_push_product_chain` 前向补充。
2. `server/internal/push` 正式 domain/service。
3. 当前设备 endpoint register/update/delete API。
4. user notification preference API。
5. Messaging/Relationship/1:1 Call/Group Call/Moments 事件生成 durable Push Job。
6. dedupe。
7. retry/backoff。
8. FCM HTTP v1 adapter。
9. APNs provider adapter。
10. UnifiedPush endpoint adapter。
11. invalid/unregistered token 自动失效。
12. Worker 消费 `push_jobs`。
13. Flutter Android FCM token registration + token refresh。
14. iOS FCM/APNs 注册代码路径（平台正式交付仍归 P14）。
15. provider mock、HTTP、Flutter API、PostgreSQL lifecycle integration 自动测试。
16. Android `google-services.json` + Google Services Gradle 插件正式接入。
17. Firebase Admin service-account 以 `.data` 私密文件 + `*_FILE` 方式供本地 Worker 使用，不进入 Git。
18. 真实服务账号本地 Worker 启动成功，说明凭据 JSON/私钥可以被正式 provider 初始化。
19. Android Firebase Debug APK 构建成功，根目录 `DD-Android.apk` 已刷新。
20. `scripts/build-client.ps1` Android 路径改为 ASCII `subst` 构建，修复中文路径 `compressDebugAssets` 失败。
21. 2026-08-12 真机通知中心回归定位：旧 FCM 消息含顶层 `notification`，后台由 FCM/Android 系统自动渲染，绕开 DD `MessagingStyle`，因此只能看到首字母占位头像且 app small icon 表现不符合 DD 设计。
22. Android user-visible FCM 改为 data-only + HIGH priority；后台 isolate 注册 `FirebaseMessaging.onBackgroundMessage`，统一调用 `AppNotificationService` 渲染 sender avatar large icon、DD monochrome small icon，并支持本地通知冷启动/运行中点击恢复 `conversationId`。
23. 服务端用 `AUTH_TOKEN_SECRET` 为 sender avatar 生成 24h HMAC capability URL；Push payload 不下发 Access/Refresh Token，`HIDDEN` 预览不下发头像 capability；`/push-assets/avatars/{userId}` 仅在 capability 有效期内返回头像。开发 runner 用私网 device-reachable origin，生产仍保持 HTTPS-only。
24. 自动证据：Push/Notification 定向 Flutter tests 4/4 PASS、Android Debug APK 构建 PASS；2026-08-12 本轮重新执行 `go test ./...`、全 App `dart analyze --fatal-infos` 与 Flutter 全量测试均通过。
25. 2026-08-14 Android killed-process Push 链加固：Firebase 默认 App 先初始化再注册顶层 `@pragma('vm:entry-point')` background handler；后台 isolate 不再复用会读取 launch details/绑定前台回调的完整通知初始化，而是只创建 `dd_messages_v2` channel 后直接渲染 data-only 通知。后台结果按 `displayed / invalid payload / Firebase init failure / notification display failure` 写入不含 token、正文、账号/会话 ID 的诊断日志；Android 设置页同时显示应用级通知权限和消息频道开关状态。代码与自动测试状态 `FIXED-PENDING-RETEST`，仍需真实 Android 对“最近任务划掉/系统回收”和 Force Stop 对照组验收。
26. U08 生产运维补齐：provider request/latency/auth failure/last success-last failure、Push backlog/retry/invalid endpoint ratio 等 Prometheus-compatible metrics 已接入 Worker；provider secret `_FILE`、credential rotation、失效凭据诊断、backlog 与 delivery/click 边界写入 `docs/runbooks/push-provider-operations.md`。
27. provider 明确返回 InvalidToken 时 endpoint 立即进入 `INVALID` 并停止投递；Worker 每小时分批清理已 INVALID 且超过 30 天的 endpoint，避免敏感投递地址永久堆积，同时保留短期故障分析窗口。
28. U24 可观测性已接入 API/Worker/PostgreSQL/Redis/WebSocket/Realtime/Outbox/Push/S3/SMTP/runtime；`infra/observability/` 提供独立 Prometheus/Grafana/blackbox/node-exporter overlay 与正式告警规则。metrics label 禁止 userId/deviceId/message content/token/raw error 等高基数或敏感值。

当前 Flutter 全局 analyzer/test 已恢复通过；P10 还剩设备级退出标准：

```text
安装最新 DD-Android.apk
→ 登录/保持会话
→ 客户端取得 FCM token 并注册 /api/v1/push/endpoints
→ 前台通知
→ 后台通知：真实 sender avatar + DD 小标
→ App 被系统杀进程后的通知：真实 sender avatar + DD 小标
→ 点击通知恢复目标会话/来电
```

2026-08-11 23:35 ADB 已识别真机 `V2241A`，但覆盖安装被设备侧拒绝：`INSTALL_FAILED_ABORTED: User rejected permissions`。这是手机“USB 安装/安装确认”真人权限门槛，不能由服务端或 ADB 绕过，因此 P10 目前不能写 `HUMAN-PASS`。

原则：

> **Push 是提醒和唤醒，不是消息数据库。客户端被唤醒后通过 Sync/Call API 获取真实状态。**

---

# 12. P11：Production E2EE

状态：`OUT-OF-SCOPE / V1 不开发`。

2026-08-11 产品决策：DD V1 不再把端到端加密作为发布目标。当前聊天继续采用 TLS/WSS 传输加密、服务端鉴权、私有对象存储与严格权限控制；服务端在授权边界内仍具备读取消息明文的能力，因此产品和文档禁止宣称“端到端加密”或“服务器无法读取消息”。

历史 `crypto/e2ee_poc` / vodozemac 只保留为技术研究档案，不参与主产品构建、CI、发布门禁或当前开发优先级。未来若重新引入，应作为新的产品决策重新立项，而不是默认恢复 P11。

---

# 13. P12：Admin / Abuse / Data Rights

状态：`IMPLEMENTED + AUTO-VERIFIED`。

U10/U11/U12 已进入总集成主链：

- 独立 Admin identity/session 与 `ADMIN_SECURITY_SECRET` / `_FILE`，禁止 fallback 到 `AUTH_TOKEN_SECRET`；
- TOTP MFA、Recovery Code、Session revoke/expiry、CSRF；
- `SUPER_ADMIN / MODERATOR / SUPPORT_READ_ONLY` RBAC，普通用户 Bearer 明确拒绝；
- 举报队列、用户 suspend/unsuspend、治理动作与管理员 audit；
- 2026-08-15 Control Plane 第一阶段已升级为 Refine + Ant Design：运营 Dashboard、14 天趋势图、用户详情/设备/Push、群组与 Moments 元数据治理视图、媒体存储、Push 队列、RTC、服务健康、管理员账号/会话、Telegram Sticker Relay、审计筛选与运行时注册开关；
- Control Plane 只展示有权威事实的数据：设备近 5 分钟活跃仅作为“在线用户”近似口径，TURN relay/Worker 等无法由 API 直接证明的状态保持 `UNKNOWN`，不伪造健康；
- `SUPER_ADMIN` 管理员账号操作带最后一个 Active Super Admin 保护；管理员 MFA reset 会撤销目标后台会话；敏感 Secret 除专用加密 Telegram Token 外继续由服务器 Secret 管理；
- 运行时注册开关真正进入 Auth policy；只有 EMAIL_CODE_PEPPER + SMTP 依赖就绪时才允许 `closed → open`，持久化配置无法激活时 API fail-closed；
- 数据导出 request/status/幂等/私有短时下载、异步 Worker；
- 账号注销二次认证、冷静期、取消/恢复、Token/Device/Push 撤销、群主/消息/Moments/媒体等生命周期；
- `000031_admin_auth`、`000032_admin_governance`、`000033_data_rights`、`000037_admin_integrations`、`000038_admin_runtime_settings` 已进入当前 schema；Control Plane 自动门禁通过后仍需生产真人 UI/权限验收。

仍未开发的 P12/P2 扩展只有 U16 普通用户 MFA 与 U17 Passkey/WebAuthn；不应把已完成的 Admin/Data Rights 重新列为 PLANNED。

---

# 14. P13：Production Self-host

状态：`IMPLEMENTED / AUTO-VERIFIED / HUMAN-PENDING`。

U20-U24 当前已落地：

- `infra/prod/compose.yml` 正式单机 Production Compose，PostgreSQL/Redis/MinIO 或 external S3、API/Worker/LiveKit、Caddy + HAProxy SNI mux；
- HTTPS/WSS、LiveKit embedded TURN/UDP 443、TURN/TLS TCP 443、ICE/TCP 7881、RTC UDP range 与防火墙/NAT runbook；
- 独立 production secret files，包括 `ADMIN_SECURITY_SECRET_FILE`；
- PostgreSQL + object storage backup/restore、严格 data-only manifest parser、quiesced DR recovery point；
- destroy-source → fresh-volume restore drill 已 PASS；
- N→N+1 forward-only upgrade/rollback/compatibility policy，最终总集成已真实验证 `000030 → 000033`；旧版本拒绝新 schema 时只能恢复 verified pre-upgrade backup，禁止 `migrate down`；
- `infra/observability/` overlay 已与 Production Compose 正式对齐，Prometheus/blackbox 与 API/Worker/LiveKit/tls-mux 共用 `internal` network；Prometheus config、19 条 alert rules、blackbox config 均通过。

U25 Release provenance 已完成代码闭环：`.github/workflows/release.yml` 只接受正式 `vSemVer` tag，并要求同一 commit 最多一个 DD 正式 SemVer tag，避免 iOS prerelease build identity collision；之后验证 tag/CHANGELOG/clean tree/current `origin/master`，再通过 GitHub Actions API 要求同一 SHA 的 `ci.yml` 与既有 U06 `secret-scan.yml` master push 均为 success。通过后才允许 Trivy dependency gate、Web/Android/Windows/Server 构建，以及通过 Codemagic bridge 触发的正式 signed iOS build。Android/Windows/iOS 原生签名都绑定 `release-signing` gate；iOS Apple signing material 仅存在 Codemagic Secret/Integration，GitHub 只持有 Codemagic API token/app id。Codemagic signed workflow 会在 App Store Connect publishing 前把 Dart/native evidence 分离，保留 `Package.resolved`/`Podfile.lock` 原 basename/路径并要求至少一个非空 resolved lockfile；固定版本+SHA256 完整性校验的 Syft/Trivy 只扫 native evidence，Trivy 必须识别 native target/packages > 0、SPDX packages > 0，HIGH/CRITICAL fail closed，并产出 pre-publish SBOM/report；GitHub 随后按同一语义独立二次扫描。所有 iOS IPA/evidence/SBOM/report 与其他平台一起进入 SHA-256、provenance、Sigstore/GitHub Attestation；最终 GHCR/GitHub Release 发布绑定 `production-release` Environment。Actions evidence/final bundle 保留 90 天，正式 Release/GHCR SemVer 标签必须至少保留当前+上一正式版本，包含 prerelease predecessor；rollback 继续沿用 U23 compatibility gate + verified pre-upgrade backup，禁止数据库 down migration。

仍需真人/项目所有者/生产环境证明：为 `release-signing` 提供真实 Android/Windows signing material、Codemagic Apple integration/certificate/profile/Team identity，并配置 Required reviewers；`production-release` 也需要 Required reviewers。公网 DNS/证书续期、UDP/NAT/跨运营商/移动网络通话、真实 FCM/APNs delivery/credential rotation、真实规模 RPO/RTO、真实告警接收方也仍是 `HUMAN-PENDING`。这些外部 Secret/Environment 配置缺失时，正式 native production release 会 fail closed，不能冒充已经生产签名。

---

# 15. P14：iOS / macOS

状态：`IN-PROGRESS / LOCAL-AUTO-VERIFIED / CLOUD-SECRET-HUMAN-PENDING`。

2026-08-12 U30 Platform Foundation 已在既有 Runner 上完成，不重复生成平台目录：

- iOS Bundle ID 固定为 `org.openimx.client`，deployment target 统一为 iOS 15.0（与当前锁定的 `firebase_core 4.13.0` / `firebase_messaging 16.5.0` iOS native 最低要求一致），Debug/Profile/Release 保持一致并保留 Flutter build name/number 接线，目标设备族为 iPhone + iPad；
- `Info.plist` 已配置当前真实链路需要的 Camera、Microphone、Photo Library read/add、Local Network 权限文案，后台模式只声明 `audio` / `remote-notification`；未为了“以后可能用”添加 CoreBluetooth；
- `dd://` custom URL scheme + Scene/Application lifecycle 已接入 `NativeRouteService`，作为 notification tap / QR / conversation restore 的 native → Flutter 基础入口；
- `Runner.entitlements` 建立 Push capability 结构，Debug/Profile 使用 development、Release 使用 production `aps-environment` build setting；真实 Apple App ID capability + Provisioning Profile 仍是 `SECRET/HUMAN-REQUIRED`；
- Keychain 继续复用 `flutter_secure_storage` 默认 iOS Keychain 路径；`AuthSessionVault` 多账号 refresh token 不回落 Web model；没有跨 App/Extension 共享需求，因此不额外声明 keychain access group；
- `Runner/Services/NativeServiceRegistrar.swift` 是统一 native service 入口；Push/Media/Call native service 以独立文件接线，避免膨胀 AppDelegate；
- Codemagic 固定 Flutter 3.44.9 / Xcode 26.6；保留无 Apple Secret 的 `ios-unsigned-validation`（Release `--no-codesign` + unsigned archive），并新增 `ios-signed-release` 正式签名/App Store Connect 上传合同；
- U25 `release.yml` 的 `build-ios` 只能在 exact-SHA CI、Secret Scan、Trivy gate 后进入 `release-signing` Environment 再触发 Codemagic；Codemagic 在 Apple publishing 前执行 pinned + checksum-verified resolved-native Syft/Trivy HIGH/CRITICAL fail-closed gate，GitHub 再独立重扫；IPA/native evidence/两套 SBOM+Trivy report 纳入统一 checksum/provenance/attestation/GitHub Release；
- 签名材料扩展名已加入 `.gitignore`，仓库不保存 Apple 私钥/Provisioning Profile；仓库也没有正式 iOS `GoogleService-Info.plist`，若走 FCM，项目 owner 仍需为 `org.openimx.client` 提供正式 Firebase iOS client config；
- Windows 本地只能验证工程文本合同与 Dart/Go 测试，真实 macOS/Xcode unsigned/signed build 仍是 `CLOUD-PENDING`。

2026-08-12 U30 iOS Push / Notification 生命周期已完成代码闭环：

- iOS notification authorization 支持状态查询、一次性请求、provisional 状态识别、denied 后系统设置恢复；启动时只在 `notDetermined` 且后台 Push 已启用时请求，不会每次启动无脑弹框；
- 继续复用既有 `/api/v1/push/endpoints` 与 FCM/APNs provider/job/retry/invalid-endpoint 主链；iOS Auth 设备仍使用 `platform=IOS`，没有新增 iOS-only Push API；
- FCM 模式会同时观察 APNs transport token 与 FCM registration token，DD endpoint 只登记选定 provider；direct APNs 模式支持 native token/refresh，token 变化会重新 upsert；账号切换/退出使用串行 generation lease，先解绑旧账号 endpoint，再允许新账号绑定，过期异步 token callback 不得串号；token refresh listener 按 session generation 重绑。authoritative local abandon 只接受明确的服务端 delivery-authority 失效事实：当前设备 revoke API 成功、logout-all 成功，或服务端数据库确认 `devices.revoked_at IS NOT NULL` 后返回专用 `DEVICE_SESSION_REVOKED`；普通 `401/SESSION_EXPIRED`、Refresh Token 过期/复用、Push DELETE/网络失败均不得推断为 device revoke，正常 DELETE 继续 fail-closed；
- iOS 前台强制关闭 Firebase system foreground presentation，使用 DD realtime/sync + in-app banner 作为唯一可见路径；前台 preview privacy 关闭时统一显示 `DD / 你收到了一条新消息`；后台/杀进程由系统 APNs/FCM 通知负责，MainShell 不再额外创建第二份 iOS message local notification；
- Push payload 携带 `recipientUserId`、conversation identity、preview mode 与数值 badge；群聊使用群名 + 群内 sender/nickname 生成 title/body；`HIDDEN` 不携带 sender/avatar/group title capability；mute message 使用 badge-only 静默 payload，不弹、不响但继续刷新 unread badge；
- notification tap 覆盖 conversation / Moments / generic fallback；进入会话前重新 Sync + resolve 授权目标，目标已删除/无权访问时回到安全列表；无有效 session/账号不匹配的 payload 直接忽略，不能用 deep link 绕过 auth；
- 多账号切换遵守 Refresh Token rotation，并引入 Auth session epoch/transition gate：进入 A→B 临界区先取消 A timer、阻止新的 A refresh，再等待 A Push release；release 失败则 B stored refresh token 完全不消费并恢复 A refresh 调度；release 成功后推进 epoch，再消费 B refresh。已经发出的旧 A refresh 完成时必须核对 captured origin/user/device/epoch：late A success 只把 rotated refresh token 保存回 A 自己的 account slot，不得覆盖当前 vault pointer/UI/B Push；late A 401 只清 A account slot，不得 clear/abandon B；B refresh 成功后先持久化 B rotated token 再切 UI，B refresh 失败则恢复 A Push/refresh lifecycle；logout/SESSION_EXPIRED 只有 endpoint DELETE 已成功，或 device revoke API 成功/`DEVICE_SESSION_REVOKED` 已确认时，才允许清 current vault / account credential / active Auth session；若 endpoint DELETE 与 device revoke 都未确认，则保留 A session/vault/Push ownership并阻止 B 接管 Push，杜绝 `Auth forgotten + ACTIVE Push endpoint forgotten` split-brain；
- Auth 服务端错误合同区分：Refresh Token reuse/过期仍返回 `401 SESSION_EXPIRED` 且不修改 `devices.revoked_at`；只有数据库确认 device 已撤销时，refresh/access authentication 才返回 `401 DEVICE_SESSION_REVOKED`。真实 PostgreSQL integration 已验证 reuse/expiry ≠ device revoke、实际 revoke/password-reset device revoke = authoritative code；未修改 Refresh Token rotation 机制；
- `PushNotificationService.swift` 已提供 permission/APNs token/badge/tap/foreground native bridge；U30 总集成已将其注册到 `NativeServiceRegistrar`、加入 Runner Sources，并由 `AppDelegate` 转发 APNs registration success/failure、foreground `willPresent` 与 notification response tap；所有回调继续调用 `FlutterAppDelegate` 的 `super` 链，避免截断 FlutterFire/Firebase Messaging delegate。

2026-08-12 U30 Media / Camera / File / QR 代码切片已补齐，但仍保持 `CLOUD/DEVICE-PENDING`：

- `dd/file_picker` 在 iOS 使用 path-based native contract：Files 由系统 document picker 导入后做文件级复制到 App cache；Photos 使用 `PHPickerViewController` + `loadFileRepresentation`，多选结果只向 Dart 返回 path/name/MIME/size；权限拒绝返回可识别错误并提供“去设置”恢复路径；正式最低版本为 iOS 15.0，不再把 iOS 13 fallback 作为产品支持范围；
- `dd/camera_capture` 的 Dart contract 已允许 iOS，native service 使用系统相机完成拍照、取消、权限拒绝、无相机与前后台中断处理，不新增自定义相机 UI；
- `dd/media_export` 已定义 iOS native service：图片/视频写入 Photos、普通文件导出到 Files、系统 share sheet、文件系统 handler/open，以及本地文件 clipboard item-provider；大视频/远程文件先落本地 path，未引入 whole-file `Data(contentsOf:)` / Dart 巨大 `Uint8List` / base64；
- 聊天相册与 Moments image/video picker 复用 Photos source；视频/普通文件上传继续 `openRead`/stream path。图片仍在现有 96 MiB 明确上限内做解码压缩，100 MiB+/500 MiB 级视频/文件不得走 whole-file bytes；
- QR 继续复用 `mobile_scanner`，iOS 增加显式 app lifecycle start/stop、duplicate suppression、相册 QR path、malformed/expired/revoked/login QR 自动测试；Camera denied 后提供系统 Settings 恢复入口，用户改为允许后回到 `resumed` 会重新 `start()` 探测权限，首次系统权限弹窗进入 `inactive` 时不会错误 stop；
- `FilePickerService.swift`、`CameraCaptureService.swift`、`MediaExportService.swift` 已在 U30 总集成中加入 Runner Sources 并注册到 `NativeServiceRegistrar`；Windows 仍不能替代真实 Xcode/device runtime 编译证据。

Windows 本地可验证工程静态合同、Dart analyzer/test 与 Go provider/worker tests；Swift/Xcode compile、archive、codesign、native service hook、权限真机弹窗、APNs/FCM delivery、Photos/UIKit、100 MiB+/500 MiB 真机文件、MP4/MOV/WebM 实际 decoder 行为仍必须由 macOS/Codemagic/真实 iPhone/iPad 给证据。

U30 native 公共工程接线已经完成。2026-08-13 总集成本地门禁已通过：`dart analyze --fatal-infos` 0 issue；iOS/Push/Media/QR/Calls 定向合同持续通过；Flutter 全量基线更新为 459 PASS / 5 SKIP / 0 FAIL；`go test ./...` / `go vet ./...` PASS；临时 PostgreSQL 18.4 应用 33 个 migrations 后 Auth lifecycle 与 Push lifecycle integration 均 PASS；Release contract/native validator/aggregate pipeline、YAML parse、diff-check 与 full-history Secret Scan 均 PASS。Codemagic 前置 analyzer、macOS Flutter test 与两条 Swift compile blocker 已逐步修复；最新真实 `ios-unsigned-validation` 已在 Mac mini M2/Xcode 26.6 完整通过 `flutter build ios --release --no-codesign` 和 unsigned `xcodebuild archive`，因此 iOS Release compile/archive 已获得真实云端证据并升级为 `UNSIGNED-CLOUD-AUTO-VERIFIED`。这仍不等于 signed IPA、codesign、TestFlight、APNs/FCM 或真实 iPhone/iPad 通过。后续主要剩 Apple/Firebase Secret、Codemagic `ios-signed-release`、App Store Connect/TestFlight 与真实 iPhone/iPad Release 验收。真 APNs delivery 仍为 `HUMAN-PENDING`，不能用 Firebase/mock 测试替代。

2026-08-13 首次接入 `api.85746.pro` 生产云端后，真实 iOS 客户端暴露出一组网络/媒体/通话回归，当前代码修复状态统一为 `FIXED-PENDING-RETEST`：Native HTTP 对 `GET/HEAD/OPTIONS` 遇到连接在响应头前被 Nginx/移动网络关闭时只做一次安全重放，Auth POST 关闭持久连接以降低 stale keep-alive 风险；临时网络错误不再清除本地 Refresh Token，Messaging 错误条新增显式“重试”恢复入口；个人头像选择改走既有 iOS `PHPicker` path-based `dd/file_picker` Photos contract，并保留 Photos 权限拒绝后的系统设置恢复；Call API 将“对方不是联系人”拆成 `CALL_CONTACT_REQUIRED`，不再误报为“当前身份无权操作”，真实设备/会话控制权失败仍保持独立 `CALL_FORBIDDEN`；同时修复 reject/cancel/hangup 终态只更新 Call、不在聊天事务内落系统消息的问题；Codemagic 两条 iOS 工作流在构建前运行 AppIcon generator，从仓库品牌母版 `DD-logo-master.png` 生成整套无 alpha `AppIcon.appiconset`，避免派生图二次缩放把安全边距带进 iOS 安装包。上述修复已有定向自动回归，但必须重新生成 iOS 包并在真实 iPhone 上复测登录、错误后恢复、头像、1:1 音视频和桌面图标后才能升级为 `HUMAN-PASS`。

2026-08-14 AI03 继续收口 iOS 视觉回归，状态为 `FIXED-PENDING-RETEST`：AppIcon 白边最终定位到共享品牌脚本把 `DD-icon-1024.png` 预先按 0.86 缩放并铺白底，随后 iOS generator 又原样缩放该派生图；当前 iOS generator 已改为直接读取未预缩放的 `DD-logo-master.png`，并拒绝具有明显整圈不透明白色 inset 的 source，`generate-brand-assets.ps1` 的 iOS 1024 canonical 也改为 1.00 full-bleed，同时测试锁住 Android launcher、Windows ICO、Web icon 原比例不变。聊天底栏则移除 `_composerBar()` 外层整条不透明 theme surface，让底层 `ChatWallpaperSurface` 连续延伸到 SafeArea/home indicator；输入框继续使用 filled input surface，voice/emoji/+ 保留局部半透明圆形 surface，因此 light/dark 下仍有对比度。AppIcon 合同 4/4、`text_chat_page_test.dart` 41/41、`dart analyze --fatal-infos` 0 issue；真实 iPhone 主屏/设置 App 列表/Spotlight/通知图标以及不同自定义 wallpaper 下的 footer 仍必须真人复测，不能据此标记 `HUMAN-PASS`。

2026-08-14 公网 1:1 Calls 基础主链已完成真实生产验收，状态升级为 `HUMAN-PASS`（基础公网语音/视频）：第一层，正式 `/api/v1/calls` 的 `event_available` hint 已同时由专用 Calls realtime、主 `MessagingCoordinator` realtime、durable `CALL_RINGING` Sync、Push 与 App resume 触发 `/api/v1/calls/active` 权威恢复，因此被叫来电不再依赖单一瞬时 WebSocket delivery；第二层，正式 `/api/v1/calls/{callId}/token` 已从错误复用旧 POC `participant_token + expires_at` 响应修正为 OpenAPI/Flutter 一致的 `token + room_name + participant_identity + expires_in_seconds` 正式 schema，旧 `/api/calls/token` 继续保留兼容响应。修复部署到生产 API 后，真人复测已确认来电/接听正常，公网语音有声音、视频有人像，LiveKit 媒体主链实际可用。Go httpapi 定向测试、`go test ./...`、`go vet ./...` 与 Flutter formal token 4/4 均通过。30 分钟长时通话、弱网/多网络切换、后台/锁屏与更多跨运营商组合仍保持 `HUMAN-PENDING`，不得由本次基础通话通过外推为全部 P7 验收完成。

2026-08-14 群通话生产配置缺陷已完成代码修复，状态为 `FIXED-PENDING-RETEST`：此前 `server/internal/groups/calls.go` 绕过 `appconfig` 直接读取 `LIVEKIT_API_KEY` / `LIVEKIT_API_SECRET` 明文环境变量，而 Production Compose 正确使用 `LIVEKIT_API_KEY_FILE` / `LIVEKIT_API_SECRET_FILE`，因此正式 1:1 Calls 可用但群通话 Start/Join 会返回 `GROUP_CALL_UNAVAILABLE`。现已把 LiveKit URL/key/secret 与群通话人数上限显式注入 `groups.Config`，`server/cmd/api` 使用 `appconfig.Load()` 解析后的值装配 Group Service，Start/Join 只使用 Service config；配置确实缺失时仍 fail closed。自动测试覆盖 secret-file 装配、显式 Group Service 配置、Start/Join token、缺失配置 unavailable、HTTP 201 与 token room/identity 权限边界；生产真实群 Start/Join/同房间/leave 仍待真人复测，UDP/TURN/ICE 网络问题必须独立验收，不能与本配置缺陷混为一谈。

2026-08-14 AI08 语音播放与转文字底层能力状态为 `FIXED-PENDING-RETEST`：客户端新增独立 playback source/resolver/controller，缓存命中优先本地源，未命中使用短时授权 URL 进入 starting 并流式播放，失败最多刷新一次授权并在 8 MiB 上限内 bytes fallback；只有远端未撤回 VOICE 在播放器真实 playing 后才标记已听，顺序连播由用户主动点击首条未听语音触发，并可由暂停、离开会话、通话、其他媒体、已听/撤回下一条中断。服务端新增 `000035_voice_transcriptions`、Whisper-compatible provider、durable STT worker、单条转文字与自动转文字偏好 API；provider 配置仅留在服务端，未配置时返回 `VOICE_TRANSCRIPTION_UNAVAILABLE`，自动模式只处理开启偏好且成员加入之后的新 VOICE，并以 message 级唯一约束防重复。Flutter 定向测试 17/17、Dart analyzer、Go 全量 test/vet 已通过；PostgreSQL 18.4 临时数据库已应用全部 34 个 migration（含 `000035`），STT lifecycle integration 实跑通过且测试容器已清理，最终聊天热点 UI 与真实设备/provider 验收待 Wave3/人工复测。

2026-08-13 U30 Calls / LiveKit / Voice / Audio 已完成 **Dart 业务链 + iOS native service + Runner/registrar 总接线**，当前仍是 `CLOUD-HUMAN-PENDING`：

- 锁定并实际按 `livekit_client 2.10.0` 的 CallKit 音频协调接口接线：CallKit 管 AVAudioSession activate/deactivate 时序，LiveKit 使用 `externalCallSystem` + audio engine availability gate，不另写 RTC；非 CallKit/group call 继续走 LiveKit automatic audio management；
- 1:1 iOS 通话补齐麦克风/摄像头权限 preflight、拒绝态、真实 mic/camera track toggle、前后摄切换、前后台 camera suspend/resume、扬声器/听筒/蓝牙/耳机 route 状态、CallKit audio activation/interruption 同步；扬声器切换不 force，避免抢占已连接的有线/蓝牙设备；
- LiveKit `RoomReconnecting/RoomReconnected` 继续作为第一层恢复；若最终落到 unexpected disconnect，正式 Calls controller 会按有限退避重新签发 token 并重入媒体房间，不再因为一次媒体断线直接把 server call 状态强制挂断；
- 新增 `Runner/Services/CallPlatformService.swift`：CallKit incoming/outgoing presentation、answer/decline/cancel/end、CXProvider audio activate/deactivate、AVAudioSession route/interruption、mic/camera permission；2026-08-13 follow-up 已将 `CXAnswerCallAction` / `CXEndCallAction` 改为两阶段确认：Native 暂存 `CXAction + actionId`，Dart 先调用 DD 正式 Calls API，成功后才 `fulfill`、失败则 `fail`；incoming ringing end 映射 `reject`，outgoing ringing cancel 映射 `hangup`，accepted 双方向 end 均映射 `hangup`；EventChannel listener 暂缺时 action event 会等待，CallKit timeout 会清 pending，end record 只在 server 成功确认或已确认 ended 后删除；Call service 本身未引入 PushKit，总集成仅在共享 AppDelegate/Xcode/registrar 做必要接线；
- U30 总集成已把 `CallPlatformService.swift` 加入 Runner target Sources 并注册到 `NativeServiceRegistrar`；但在 macOS/Xcode 编译通过前，仍不得宣称 Swift bridge 已获得真实 iOS 编译证据；
- Background：audio call / microphone continuation 依赖现有 `UIBackgroundModes=audio` 与真实 iPhone 行为；video 进入 inactive/hidden/paused 时主动停 camera，回前台按用户原状态恢复；普通后台 WebSocket 不应被当成可靠来电唤醒机制；
- CallKit 当前能承接 **进程已运行且 DD 已收到 signaling 来电事件** 的系统来电 UI。若要求 App 被系统终止/长期后台时仍可靠拉起 CallKit，仍需要独立设计 PushKit VoIP/APNs VoIP topic/server payload/provisioning；本切片没有用 PushKit 维持任意后台网络连接，也没有伪造该能力已完成；
- voice playback 与 message sound 在 active call 期间会让路，避免 `audioplayers` 抢占通话 AudioSession；现有 `ChatVoiceRecorder` 已确认没有 Windows/Android-only guard，录音权限/record/stop/cancel 状态链继续由 `record` 实现。由于 `core/media/**` 属并行 Media AI，本切片没有越界改 recorder；真实 iPhone 仍需验证录音与通话切换后的 AudioSession 恢复；
- Group call 继续复用现有 LiveKit Room/signaling/server：join/leave、participants、mic、camera、speaker、reconnect 均保留，iOS 前后台会暂停/恢复 camera，并共享 call-audio activity guard。

U30 的本地代码与公共 native 接线已经完成。后续不再重复开发 Push token lifecycle、Media/QR 或 Calls registrar 接线，而是获取真实 Codemagic/macOS Xcode compile/archive 证据，配置 Apple/Firebase signing/client material，跑 signed IPA → App Store Connect/TestFlight，并在真实 iPhone/iPad 验证 APNs、Camera/Photos/Files/QR、CallKit、Bluetooth、锁屏/后台、网络切换和长时通话。若 V1 要求 App 被系统终止/长期后台时仍可靠拉起系统来电，还需单独完成 PushKit VoIP/APNs VoIP topic/server/provisioning 闭环。

不能因为 Flutter package 声明支持 iOS/macOS、或 Windows widget/unit test 通过，就宣称 DD 已交付这些平台。

---

# 16. 当前 CI/自动门禁事实

`.github/workflows/ci.yml` 当前已经定义：

```text
server-contracts
  gofmt / go test / go vet
  migrations
  Auth / Contacts / Groups / Calls / Messaging PG integration
  Moments / QR 指定 PostgreSQL lifecycle + migration integration
  Push PG lifecycle + worker integration
  Redis realtime integration
  OpenAPI strict

admin-web
flutter-client
  analyze / test / Web Release / Android Release
windows-release
  Windows Release / real-engine video poster smoke

secret-scan
  full-history Gitleaks / narrow allowlist / detector self-test

codemagic.yaml (cloud build pending)
  ios-unsigned-validation: analyze / test / unsigned iOS Release compile + unsigned archive
  ios-signed-release: App Store signing identity / Release IPA / App Store Connect upload contract
```

已新增 U25 正式发布链：

```text
release (tag-only)
  exact tag / SemVer / changelog / clean tree / current origin/master SHA
  exact-SHA ci.yml success + exact-SHA secret-scan.yml success
  Trivy dependency HIGH/CRITICAL gate
  Web / Android / Windows / Server release builds + Codemagic signed iOS build
  Android production signer / Windows Authenticode / iOS Apple signing identity verification
  SPDX SBOM (including iOS resolved dependency evidence) + server-image Trivy scan
  SHA-256 manifest + Sigstore keyless signature
  GitHub Artifact Attestation
  production-release Environment approval
  immutable GHCR version image publication + image signing/attestation
  GitHub Release
```

代码层仍不冒充外部环境已配置：真实 Android/Windows certificate/keystore、Codemagic Apple signing material、`release-signing`/`production-release` Required reviewers 属于 `SECRET/HUMAN-REQUIRED`。iOS unsigned/signed Codemagic 工作流与 U25 集成已经配置，但仍缺首次 macOS 云构建、真实 signed IPA/TestFlight processing 证据与 macOS Runner；P10 真 FCM/APNs 设备级 delivery/click 仍需 human gate。

P10 当前 Go 全量、OpenAPI strict、Flutter analyze/test、Push PostgreSQL integration 已通过；Android Firebase Debug APK 已真实构建。Windows Release 当前仍有本机历史 build cache/中文路径环境债务，不能拿 Android 通过替代 Windows 门禁。

---

# 17. 当前开发唯一优先级

从现在开始按：

```text
当前真人回归暴露问题 Stop-the-line 修复
→ 配置 U25 release-signing / production-release 的真实 Secret 与 reviewer policy
→ P9 QR 真人多端验收
→ P10 Android/iPhone 真机 Push 最终验收
→ P13 公网 TURN/DR/Observability 真人生产验收
→ P14 iOS/macOS
```

并行保留：

```text
R15 真人 Bug 复测
P6 Group 真人/500人/弱网
P7 Calls 公网TURN/长时/弱网
P8 Moments 真媒体/多端
```

**不得再跳回去重新规划 P6/P7/P8；P10 代码主链已收口，只等待 Android 设备权限放行后的最终真人 Push 验收。**

---

# 18. 下一个 AI 的执行规则

1. 先读 `docs/README.md`。
2. 再读本文件。
3. P9 当前只保留真人扫码/相机/跨设备登录验收；自动门禁失败若回归则立即 Stop-the-line 修复。
4. P10 不再新增架构层；下一步只做 Android 真机 token/delivery/click 验收和发现问题后的 Stop-the-line 修复。
5. P10 已有 analyzer/test/build/PG/provider 自动门禁，任何真机修复不得破坏这些门禁。
6. 真机出现问题时必须保留 FCM token、endpoint/job/provider 日志证据定位，禁止靠反复重装猜原因。
7. migration 已应用后禁止修改历史 checksum。
8. 所有 identity/role/userId/deviceId 都由服务端 Principal/数据库授权，禁止相信客户端自报。
9. Push/Admin 都不得复用“看起来能跑”的临时安全模型；E2EE 已退出 V1 范围。
10. 每轮代码事实改变，同轮同步 docs。

---

# 19. Stable 1.0 最低退出标准

至少：

- 没有 P0/P1 `KNOWN-FAILURE`；
- Flutter analyze/test/build 绿；
- Go/OpenAPI/migration 绿；
- Windows/Android/Web 核心真人主链通过；
- Group/Moments/QR 人工通过；
- 离线 Push 真 provider/真设备通过；
- Admin/Data Rights 可用；
- 公网 TURN/TLS 通过；
- Backup/Restore/Upgrade/Rollback 演练通过；
- 生产 secrets、日志、metrics、alerting 达标；
- iOS/macOS 如果列为 1.0 支持平台则必须真机构建/验收。

在这些条件之前，只能使用 Alpha/Beta/RC 等状态，不得因为代码量大就把产品称为“已上线完成”。
