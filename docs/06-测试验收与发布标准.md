# DD 测试、验收与发布标准

> 更新时间：2026-08-13（U30 iOS 总集成、Push/Auth、Media/QR/Calls 与 Release 门禁复核）
>
> 本文是 DD 的质量门禁。**自动测试通过不等于真人通过，真人某次通过也不等于后续代码不会回归。**

---

# 1. 状态口径

使用统一状态：

```text
IMPLEMENTED
AUTO-VERIFIED
HUMAN-PASS
FIXED-PENDING-RETEST
KNOWN-FAILURE
IN-PROGRESS
PLANNED
```

任何用户可见功能只有真人在目标平台通过后，才能标记 `HUMAN-PASS`。

---

# 2. 测试金字塔

## L0 静态与合同

- `gofmt`/编译。
- Flutter analyze。
- TypeScript typecheck。
- OpenAPI lint。
- migration 格式/顺序。
- Secret scan。

## L1 单元测试

- 纯函数。
- 状态机。
- validation。
- parser。
- Mention UTF-16。
- Telegram link parser。
- media format sniff。
- cache policy。

## L2 组件/Widget

- Flutter 页面。
- Action Sheet。
- 键盘行为。
- Footer。
- 资料页。
- Sticker library。
- 消息操作。

## L3 服务集成

- PostgreSQL transaction。
- Auth/Contact/Messaging。
- media reservation/complete。
- Sticker authorization。
- Outbox/Sync。

## L4 构建

- Windows Release。
- Android APK。
- Web Release。
- Admin build。

## L5 真实设备/真人验收

- Windows 11。
- Android 真机。
- Web 浏览器。
- 多账号、多设备、弱网、后台、DPI、真实媒体。

## L6 生产演练

- 公网部署。
- TURN/TLS。
- 备份恢复。
- 升级/回滚。
- 安全审计。

---

# 3. 2026-08-13 当前门禁快照

U30 五条并行支线完成后已在独立总集成 worktree 合流并重新执行门禁；自动通过仍不等于 Windows / Android / iPhone 真人通过，也不等于真实 Xcode/Apple signing 已完成。

本轮最新证据：

- `dart analyze --fatal-infos`：**PASS / 0 issue**；
- iOS Platform / Push / Media / QR / Calls 定向合同：**46 / 46 PASS**；
- Flutter 全量：**447 PASS / 5 SKIP / 0 FAIL**；5 个 skip 仍是缺少真实 MP4/MOV/WebM/MKV 样本/本机 native runtime 的既有环境型跳过项；
- `go test ./...`、`go vet ./...`：**PASS**；
- 临时 PostgreSQL 18.4 应用当前 **33 migrations** 后，Auth lifecycle 与 Push lifecycle integration：**PASS**；
- Release contract **22/22**、iOS native scan validator **11/11**、aggregate **33/33**、`scripts/test-release-pipeline.ps1`、YAML parse：**PASS**；
- final U30 integration full-history Secret Scan：**96 / 96 commits、no leaks**，broken Git metadata false-green guard 与 injected credential detector self-test 均 PASS；
- 仍必须真人/云端验证 Windows/Android 历史回归项、iOS Xcode compile/archive、APNs、Camera/Photos/Files/QR、CallKit/Bluetooth/background/network switching、生产签名和公网链路。

## 3.1 Go

```bash
go test ./...
```

结果：**PASS**。

最近完整 `go test ./...` 已通过，覆盖原有模块并包含：

- groups；
- calls；
- moments；
- qrcode；
- httpapi formal runtime/OpenAPI contract；
- observability metrics contract、低基数 label/敏感值不泄漏、PostgreSQL operation tracer、Push provider result/auth classification；
- 其它 auth/contacts/media/messaging/realtime/stickers/platform 模块。

P6/P7/P8/P9 服务端均已有真实 PostgreSQL integration test；其中 QR `000021` 已做 migration roundtrip。P10 已有 Push PostgreSQL lifecycle + worker integration，并在当前 CI 中显式设置 `DD_PUSH_TEST_DATABASE_URL` 运行；Push provider mock/retry/invalid-token 仍由 Go 测试覆盖。U08 本轮又在临时 PostgreSQL 18.4 上实跑 Push lifecycle，并覆盖 INVALID endpoint 超过 30 天后的分批清理。

U24 配置门禁还必须执行 YAML/Compose syntax、Prometheus `promtool check config`、`promtool check rules` 与 blackbox `--config.check`；规则语义通过不代表 Alertmanager 接收方或真实公网 LiveKit/TURN 已完成真人验收。

注意：带真实外部依赖的 integration test 是否执行仍取决于测试配置；看到 package `ok` 不能自动推断所有公网/RTC 场景都已跑。

## 3.2 Flutter Analyze

使用 ASCII `subst` 短路径重新执行：

```bash
dart analyze --fatal-infos
```

2026-08-11 04:42 当前结果：**PASS / 0 issue**。

本轮已修复 P9 QR 的 MainShell 接线、Contacts/PeerProfile import、我的二维码入口和 QR API lint；历史中文路径/LSP 仍通过 ASCII `subst` 规避。

## 3.3 Flutter Test

视频旧门禁已改为 Windows Release Engine smoke，并曾实测产出真实 poster：

```text
DD_VIDEO_PROBE_OK 64x48 800ms poster=2212
```

2026-08-11 04:42 P9 QR 四个定向测试文件当前 **PASS / 10 tests**：

- `dd_qr_payload_test.dart`；
- `qr_api_client_test.dart`；
- `qr_login_page_test.dart`；
- `qr_scanner_page_test.dart`。

本轮已补齐 group fixture 的 `ownerUserId`，并在 scanner test 结束前恢复 foundation debug platform，避免 framework invariant 污染。

## 3.4 Admin

```bash
npm run build
```

结果：**PASS**。

Admin 已不再是纯工程壳：独立 Admin identity/session、TOTP MFA、Recovery Code、RBAC、CSRF、用户治理、举报与审计均有 Go/HTTP/PostgreSQL 自动证据；前端 typecheck/build 仍只证明管理界面工程可构建，真实生产管理员初始化和运营流程继续需要真人验收。

---

# 4. 基础 CI 门禁

## 4.1 当前 CI 已实际执行

`.github/workflows/ci.yml` 当前包含：

```text
server-contracts:
  gofmt
  go test ./...
  go vet ./...
  migration up
  Auth PostgreSQL + Mailpit integration
  Contacts PostgreSQL concurrency integration
  Groups PostgreSQL lifecycle/authorization integration
  Calls PostgreSQL lifecycle/multi-device integration
  Messaging PostgreSQL reliability integration
  Moments PostgreSQL lifecycle + 000028 migration roundtrip
  QR PostgreSQL lifecycle + 000021 migration roundtrip
  Push PostgreSQL lifecycle + worker integration
  Realtime Redis cross-node integration
  runtime route ↔ OpenAPI contract（随 Go test）
  Redocly strict OpenAPI lint
  docker compose config validation

admin-web:
  npm typecheck
  npm build

flutter-client:
  Ubuntu
  Flutter 3.44.9 pinned
  realtime protocol dart tests
  flutter analyze
  flutter test
  flutter build web --release
  flutter build apk --release
  upload Web/Android artifacts

windows-release:
  Windows runner
  flutter build windows --release
  upload Windows artifact
  real-engine MP4 poster smoke
```

当前 `ci.yml` 已覆盖 Web/Android/Windows build artifact；U07 已把 Moments/QR 指定 PostgreSQL lifecycle/migration 加入显式门禁。U25 另新增 `release.yml`，只接受正式 SemVer tag，并在任何 release build 前验证同一 commit SHA 的 master push `ci.yml` 与既有 full-history `secret-scan.yml` 均成功。CI/Release 结构存在仍不等于真人设备、公网网络和生产签名 Secret 已完成。

仓库根目录保留 Codemagic `ios-unsigned-validation`，固定 Flutter 3.44.9 / Xcode 26.6，执行 analyze、test、`flutter build ios --release --no-codesign` 与 unsigned Release `xcodebuild archive`；它不需要 Apple Secret，也不上传 TestFlight。另已配置 `ios-signed-release`：正式 U25 tag release 通过 `release-signing` Environment 后触发 Codemagic，要求 Apple integration、App Store distribution certificate/profile、Bundle/Team identity 全部存在，缺任一项 fail closed。U25 同时要求每个 commit 最多一个 DD 正式 SemVer tag，避免 prerelease 共用同一 Apple `(Bundle ID, version, build)` identity。Codemagic 在 App Store Connect publishing 前先把 Dart/native evidence 分离并保留 native lockfile 原 basename/路径，要求至少一个非空 `Package.resolved`/`Podfile.lock`；固定版本+SHA256 完整性校验的 Syft/Trivy 只扫 native 目录，Trivy 显式 `--list-all-pkgs` 且必须识别 native target/packages > 0，SPDX packages 也必须 > 0，并以 `HIGH/CRITICAL --exit-code 1` 阻断；之后再验证 `codesign`、embedded profile、Bundle ID、Team ID、CFBundleShortVersionString/CFBundleVersion。产物固定 `DD-<tag>-ios-arm64.ipa`，GitHub 仍对同一 evidence 二次扫描。当前没有真实 Apple Secret/Codemagic run，因此状态仍只能记 `CONFIGURED / SECRET-HUMAN-REQUIRED / CLOUD-PENDING`，不能写成 signed/TestFlight 已验收。

## 4.2 目标完整门禁（当前 CI 尚未全部具备）

PR/主分支最终至少执行：

```text
server:
  go test ./...
  go build ./cmd/api
  go build ./cmd/worker
  migration tests

contract:
  OpenAPI lint
  runtime route ↔ OpenAPI coverage

flutter:
  flutter analyze --fatal-infos
  flutter test
  flutter build web --release
  flutter build windows --release   # Windows runner
  flutter build apk --release       # Android runner

admin:
  npm run build
```

当前 CI 已补齐：

- Ubuntu Flutter analyze/test/Web/Android Release。
- Windows Flutter Release runner。
- Web/Android/Windows artifact upload。
- runtime route ↔ OpenAPI path/method coverage test。
- Windows real-engine MP4 poster smoke。
- PostgreSQL Auth/Contacts/Groups/Calls/Messaging integration。
- PostgreSQL Push lifecycle + worker integration。
- Go `go test ./...` 覆盖 Push provider mock、retry/backoff、invalid token 等单元/合同路径。
- 独立 Secret Scan：完整 Git 历史 Gitleaks、普通 checkout / linked worktree Git metadata 支持、`git rev-list --count --all HEAD` 与 Gitleaks commit-count 一致性校验、Git fatal / 0 commits / shallow repository 防假绿、窄 allowlist，以及运行时注入假 GitHub PAT 的 detector self-test。

U25 正式发布门禁已经补齐：

- tag-only `vSemVer` 正式发布入口；
- exact SHA `ci.yml` + `secret-scan.yml` 成功证据；
- Trivy dependency/image HIGH/CRITICAL fail gate；
- Windows/Android/iOS/Web/Server versioned artifact；
- Android production signer identity / Windows Authenticode identity / iOS Apple Distribution + provisioning identity 强校验；
- SPDX SBOM、SHA-256、Sigstore keyless checksum/image signing、GitHub Artifact Attestation；iOS 同时保留 Codemagic pre-publish SBOM/Trivy 与 GitHub 二次验证 SBOM/Trivy；
- `production-release` Environment approval；
- 90 天 Actions rollback bundle + 当前/上一正式版本长期 artifact/image retention contract。

当前明确仍需外部环境/真人补齐：真实 Android/Windows signing certificates、Codemagic Apple signing certificate/profile/App Store Connect API key、`release-signing`/`production-release` Required reviewers、Android 真机 native video/container、iOS 首次 unsigned/signed Codemagic macOS 云构建、真实 TestFlight processing、macOS Runner（P14）、真实 FCM/APNs delivery/click。不能用 mock/provider 单测、本地 debug-sign APK 或 IPA 结构检查替代 Apple `codesign`/TestFlight 实证。

P9 Flutter analyzer/定向测试和 Windows/Web/Android 构建阻断已解除；当前 P9 重点是真人相机/扫码/跨设备登录。

任何门禁如果因为平台条件无法在某 CI job 运行，必须：

- 有单独的平台 runner；或
- 在发布前人工运行并保留结果。

不能直接删掉门禁。

---

# 5. Auth 验收矩阵

## 自动

- 邮箱 normalization。
- DDID validation。
- Argon2id。
- 注册并发唯一性。
- 登录成功/失败。
- 登录限流。
- Access expiry。
- Refresh rotation。
- Refresh replay。
- device revoked。
- logout-all。
- password reset。
- email change code。
- profile update。

## 真人

Windows/Android/Web：

1. 注册。
2. 登录。
3. 大退重开自动回会话。
4. 断网启动不会误清账号。
5. 历史登录头像/昵称正确。
6. 改昵称/签名/邮箱/头像。
7. 远程撤销另一设备。
8. 密码重置后旧设备失效。

---

# 6. Contacts 验收矩阵

至少两个账号 A/B，最好加 C/D。

## 自动

- exact DDID search。
- SELF search。
- request lifecycle。
- duplicate request。
- reverse simultaneous request。
- accept transaction。
- reject/cancel/expire。
- contact metadata。
- block/unblock。
- block IDOR/privacy。
- relationship outbox。
- rate limits。

## 真人

1. A 搜 B。
2. A 发申请。
3. B 不重启即可看到新的朋友。
4. B 点申请可进入资料/聊天目标流程。
5. B 同意。
6. A 第一时间看到关系刷新。
7. 聊天出现好友通过系统消息。
8. Windows/Android 打开 3 个正常联系人资料，不出现“用户不可用”。
9. 删除联系人后历史会话仍在。
10. 拉黑后不能继续发消息/加好友。

### 当前状态

正常联系人资料大面积“用户不可用”曾真人失败；当前代码已经加入 stable-id 关系 API 与 fallback，状态为：

```text
FIXED-PENDING-RETEST
```

---

# 6.1 Groups 验收矩阵

## 自动

- migration `000017_groups` roundtrip。
- 创建群时只能邀请 active + eligible contact，block 拒绝。
- OWNER/ADMIN/MEMBER 权限。
- 非成员 IDOR。
- 被踢/退出后无法继续读取或发送群消息。
- Owner 不可直接 leave，必须 transfer/dissolve。
- Admin 不能踢/降级 Owner，也不能越权修改他人群昵称。
- `@member` 只解析 active member。
- `@all` 只由 OWNER/ADMIN 生成 entity。
- JOIN APPROVAL 在批准时再次校验 active/block/capacity。
- GROUP outbox → sync。
- 群资料 PATCH 字段判定：仅 `avatarMediaId` 也必须视为有效更新，空字符串表示移除群头像。
- OpenAPI group route/method contract。
- Flutter create group / group API / group detail role UI / group chat sender identity。

当前证据：真实 PostgreSQL `TestGroupLifecycleWithPostgres` 已通过；Go Groups/HTTP/Messaging/Contacts 定向回归、Redocly strict、Flutter analyzer 均通过。

## 真人

至少 A/B/C 三账号，Windows + Android + Web：

1. A 选 B/C 创建群。
2. 三端无需重启即可出现群会话。
3. 每个成员发消息，头像/昵称不能串人。
4. A 设 B 为管理员；B 可邀请/踢普通成员但不能操作群主。
5. C 修改自己的群昵称，只影响该群显示。
6. `@成员` 点击进入正确 stable userId 资料；管理员 `@all` 生效，普通成员手输 `@all` 只是文字。
7. 被踢成员立即失去新消息读写能力；重新邀请按当前 block/contact 状态重新授权。
8. APPROVAL 模式申请/通过/拒绝实时同步。
9. 转让群主后旧群主权限立即下降。
10. 解散后所有成员都不能继续读写。
11. Owner/Admin 单独更换群头像成功并即时刷新；移除群头像也成功，不得出现 `INVALID_GROUP_REQUEST / Group input is invalid`。
12. 断网重连、客户端重启、Worker/API 重启后群状态一致。
13. 500 人边界做独立性能/滚动/mention 压测，不用 3 人测试冒充大群稳定。

当前状态：

```text
IMPLEMENTED + AUTO-VERIFIED
HUMAN-PENDING
```

---

# 6.2 Moments 验收矩阵

## 自动

- migration `000019_moments` + `000028_moment_activity`。
- ALL_CONTACTS / PRIVATE / EXCLUDE。
- “不看他 / 不让他看”。
- Block。
- 删除好友后旧动态失权。
- 非互相好友的点赞/评论身份隔离。
- 评论删除权限。
- Moment media 授权与 orphan cleanup 引用保护。
- MOMENT Outbox。
- 点赞/评论/回复写入持久互动未读事实；重复点赞不重复累计，取消点赞/删除评论/删除动态清理对应记录，自操作不产生自己的角标。
- `GET /api/v1/moment-activity` 与 `POST /api/v1/moment-activity/read` 鉴权/合同；未读汇总与最近 30 条互动列表都遵守当前 Moment 可见性和 Block；列表按时间倒序返回 actor、点赞/评论类型、评论正文、read 状态，mark-read 后历史仍保留但 read=true。
- Flutter Feed / Publish / API；客户端在启动/恢复/`moment-*` Realtime hint 时刷新未读，进入朋友圈时清除；发现 Footer、桌面 Rail、朋友圈入口都必须显示 1～99 / `99+`；自己的朋友圈封面下方必须显示互动焦点，点击后能看到最近谁点赞、谁评论、评论正文与时间。
- 单视频 probe/poster/upload/player。
- Feed 视频不能长期显示黑色占位：poster 必须可见；达到可视阈值后静音循环自动播放，同屏只允许最可见视频自动播放，滚出可视区/切页暂停；单击直接进入完整播放器；poster probe 失败保留点击播放兜底。

当前自动证据：真实 PostgreSQL `TestMomentPrivacyLifecycleWithPostgres` 已通过；Flutter Moments API/Feed/Publish 定向回归已覆盖朋友圈视频 poster + `InlineVideoPreview` 自动播放配置；2026-08-12 Moments Feed 与共享 Inline Video 定向测试合计 9/9 通过；OpenAPI strict 已覆盖 Moments 路由。

状态：

```text
IMPLEMENTED + AUTO-VERIFIED
HUMAN-PENDING
```

真人仍需：Windows/Android/Web 连续发图/视频、弱网、大图/长视频、隐私切换后的即时可见性；以及另一账号连续点赞/评论/回复后验证发现页 `1 / 2 / 99+` 角标、重启恢复、进入朋友圈清零与多端一致性；进入朋友圈后还要确认顶部互动焦点仍能回看刚才的点赞/评论，点开后昵称、头像、评论正文和时间正确。

---

# 6.3 QR 验收矩阵

## Server 自动

- migration `000021_qr` roundtrip。
- 登录 nonce 仅保存 SHA-256。
- PENDING → SCANNED → CONFIRMED/REJECTED → CONSUMED/EXPIRED。
- scanner user/device 绑定。
- target device 信息由 server challenge 返回。
- Session creation + consume 同事务。
- second consume 不再创建第二设备 Session。
- 群 invite：Owner/Admin、expire、revoke、maxUses、Block、capacity。
- 群成员写入 + Outbox + use_count 同事务。
- HTTP/OpenAPI contract。

Server 状态：`AUTO-VERIFIED`。

## Flutter 当前状态

2026-08-11 04:42 当前：

- analyzer：0 issue；
- QR 定向测试：10/10 PASS；
- Windows Release：PASS；
- Web Release：PASS；
- Android Debug APK：PASS。

状态：

```text
IMPLEMENTED
SERVER-AUTO-VERIFIED
CLIENT-AUTO-VERIFIED
HUMAN-PENDING
```

下一步进入 Android 相机扫码、Windows 手动 payload、PC/Web 扫码登录与群二维码的真人多端验收。

---

# 7. Messaging 可靠性

## 自动

- DIRECT 幂等创建。
- send idempotency。
- concurrent sequence allocation。
- history cursor。
- local delete。
- recall。
- edit version conflict。
- reply same-conversation authorization。
- read sequence。
- Outbox retry。
- Sync cursor。
- SELF conversation。
- save/unsave。
- pin/unpin。
- forward authorization。
- search access。
- archive/local hide。

## 压测

至少：

- 单会话连续 1k 消息不重复。
- 并发不同 device client IDs。
- Worker 短暂停止再恢复。
- WebSocket 中断后 Sync 补齐。
- API 重启后客户端恢复。

目标：

```text
消息静默丢失 = 0
重复业务消息 = 0
sequence 冲突 = 0
```

---

# 8. 已读/未读

真人必须覆盖：

- 对话正在可见时才自动 read。
- 焦点在联系人/我的/发现不能误 read。
- App 窗口失焦时根据产品规则不误 read。
- 新消息红色角标准确。
- 标为已读。
- 多设备 read 同步。

---

# 9. 消息操作 UI

逐类型断言菜单：

| Type | Copy | Reply | Forward | Save | Pin | Recall (mine) | Local Delete |
|---|---:|---:|---:|---:|---:|---:|---:|
| TEXT | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| IMAGE | 否 | 是 | 是 | 是 | 是 | 是 | 是 |
| GIF | 否 | 是 | 是 | 是 | 是 | 是 | 是 |
| STICKER | 否 | 是 | 是 | 是 | 是 | 是 | 是 |
| FILE | 否 | 是 | 是 | 是 | 是 | 是 | 是 |
| VOICE | 否 | 是 | 是 | 是 | 是 | 是 | 是 |
| VIDEO | 否 | 是 | 是 | 是 | 是 | 是 | 是 |

当前代码已经 `_canCopyMessage()` 限定 TEXT + 非空，状态：

```text
AUTO-VERIFIED / FIXED-PENDING-RETEST
```

---

# 10. Mention 测试

## Parser

- `@alice`。
- 多 Mention。
- emoji 前后 offset。
- 中文 + emoji + mention。
- `@@alice` 不识别。
- email `a@b.com` 不识别。
- 句首/标点边界。
- maximum entities。

## Security

- client forged entity 被忽略。
- wrong userId 被服务端重建。
- blocked user 不进入 suggestion。
- old message stable userId 在 DDID 改名后仍可跳原用户。
- Forward 不错误重绑定历史 Mention。

## UI

- Windows ↑↓ Enter 选择 suggestion，不直接发送消息。
- Escape 关闭。
- Android tap suggestion 保持输入焦点/键盘。
- IME composing 不被破坏。

---

# 11. 媒体上传

## 自动

对 IMAGE/GIF/FILE/VOICE/VIDEO/STICKER：

- reserve。
- purpose。
- size。
- sha256。
- complete。
- mismatch 拒绝。
- expired upload。
- unauthorized media reference。
- cancel closes transport。
- 失败/取消/重试会调用 upload cancel API 释放 reservation，不能连续失败后把 active upload / reserved bytes quota 卡死。
- 5xx retry bounded。
- 4xx no retry。

## 真人

- 真手机大图片。
- 50MB 级普通 GIF 边界（按产品限制）。
- 16MB 级自定义 GIF/MP4 Sticker 能加入个人库并正常播放。
- 小/中文名/大文件。
- 断网中断上传。
- 取消上传确认后台没有继续跑流量。

---

# 12. 媒体缓存

真人测试：

1. 第一次打开远端图片。
2. 返回再开，不应重新长时间转圈。
3. 重启 App 后再开，仍优先本地。
4. Grant 过期后刷新一次。
5. 缓存文件人为截断，客户端不应当作完整媒体。
6. 大量媒体后触发 LRU/age 清理。

---

# 13. 视频

## 自动

- VIDEO payload validation。
- 主视频与 poster mediaId 不同。
- media purpose authorization。
- poster authorization。
- local probe metadata。
- real sample native test。
- Android 聊天内视频：可见区域仲裁、静音循环预览、单击进入 viewer 合同。
- Android viewer：沉浸全屏仍保留 DD 控制层，顶部退出按钮/系统返回键优先退出全屏合同。
- Android 分享：Dart 本地下载 → `DD/transfers/` → FileProvider `content://` → 系统分享合同；`dd_shared_files/` 与 `DD/transfers/` 均进入窄范围 FileProvider 白名单。

2026-08-12 本轮 Android 播放器定向 Flutter/contract tests 6/6 通过，相关 Dart 定向 analyze 0 issue；随后通过仓库标准 `scripts/build-client.ps1 -Target android` 短路径构建门禁并产出最新 Debug APK。真实设备仍需复测。

## 当前已知环境问题

`video_media_probe_real_sample_test.dart` 在当前 DD 测试宿主缺 `libmpv-2.dll`。

## 真人必须测试

Windows ↔ Android：

- MP4。
- MOV。
- WebM。
- MKV（如果宣称支持）。
- 小于 1 秒短视频。
- 竖屏视频。
- 大视频。
- 取消。
- 断网。
- 聊天停留在包含视频的当前页面时，主要可见视频无需点击即可静音循环预览；滚出可视区、切页、退后台后停止。
- 单击视频缩略图直接放大进入 viewer，不要求双击。
- Android 全屏后顶部退出按钮可见/可唤回，系统返回键先退出全屏而不是直接退出 viewer。
- 点击分享能正常打开 Android 系统分享面板，并能把实际视频文件交给目标应用读取。

### 当前历史失败

曾报：

```text
无法生成视频缩略图
```

当前代码已改为：

- 多 metadata source。
- 12%/35%/62% 多候选帧。
- JPEG/PNG 双截图格式。
- timeout/retry。
- Poster 统一压到 720 长边 JPEG。

状态仍是：

```text
FIXED-PENDING-RETEST
```

直到真实 MP4 双端通过。

---

# 14. 语音条

真人：

- Android 按住录音。
- 滑动取消。
- 松发送。
- 长按响应速度。
- Windows 播放。
- 多条语音切换。
- 跳出页面再回来。
- 不同 MIME/header。
- 至少 30 分钟累计播放/录音稳定。

---

# 15. Sticker

## 自动

- Custom CRUD/order。
- 只允许 READY STICKER media。
- Custom Sticker 允许 PNG/WebP/GIF/MP4/WebM，单项最大 64 MiB；从 Telegram Pack/消息保存到个人库时允许 `application/x-tgsticker`；Telegram relay 仍执行独立总下载限制，并按静态/TGS/WebM 做更严格的格式上限。
- MP4/WebM 自定义 Sticker 在聊天中静音循环，离开可视区域暂停；TGS 使用 gzip Lottie decoder 循环渲染。
- send authorization。
- Telegram setName validation。
- arbitrary URL rejection。
- token absent error。
- provider failure。
- static WebP/PNG import。
- animated TGS import + gzip/Lottie 内容校验 + Flutter 面板播放。
- video WebM import + EBML/WebM 内容校验 + 静音循环播放。
- 旧版 partial pack（`unsupported_sticker_count > 0`）再次导入必须强制刷新，不能继续命中 24h 旧静态缓存。
- subscriber isolation。
- shared pack cleanup。

## 真人

1. 打开 Sticker library。
2. 上传 PNG。
3. 上传 WebP。
4. 上传 16 MiB 左右 GIF，确认可加入、发送、循环动画。
5. 上传 MP4/WebM，确认可加入、发送，聊天内静音循环播放且滚出可视区会暂停。
6. 连续取消/制造上传失败后再次上传，不得出现错误的 `MEDIA_QUOTA_EXCEEDED`。
7. 立即显示并发送。
8. 重启后仍在。
9. 删除个人库不破坏历史消息。
10. 配置真实 Telegram Bot Token。
11. 分别导入真实静态、TGS 动态、WebM 视频 Pack；面板不得出现“动态/视频表情暂不支持”，所有合法项目必须可见并播放。
12. 从 TGS/WebM Pack 发送到聊天，确认 TGS 循环动画、WebM 静音循环，并验证收到后可“添加到我的表情”再发送。
13. 用升级前已只缓存静态子集的 Pack 重新导入，确认动态/视频项目自动补齐。
14. 第二账号订阅同 Pack，验证复用。

### 当前历史失败

真人曾报告整个 Sticker/Telegram Pack 主链加载失败。

当前代码与服务端 wiring 已做针对性修复，状态：

```text
FIXED-PENDING-RETEST
```

---

# 16. Notification

## Android 真人

- 首次安装请求权限。
- App 前台：顶部 DD banner。
- App 后台：系统 Heads-up。
- small icon 是 DD monochrome，不是 Android robot。
- 发送者头像 large icon。
- mute 不弹。
- active chat 不重复弹。

当前 channel：

```text
dd_messages_v2
Importance.max
Priority.max
```

因为 Android channel importance 创建后不可变，历史 `v1` 被版本化替换。

2026-08-12 针对真机“通知只有首字母占位头像、DD 小标不正确”的回归，已确认旧 FCM payload 含顶层 `notification`，App 在后台时由系统直接落通知，绕开 DD 自己的 `MessagingStyle` / sender avatar 渲染链。当前修复要求：

- Android FCM 改为 data-only，由后台 handler 调用 `flutter_local_notifications` 统一渲染；
- Push data 携带 `title/body` 与短期签名 `avatarUrl`，**不得携带 Access/Refresh Token**；`HIDDEN` 预览不得下发 avatar capability；
- sender avatar 获取失败时允许退化为首字母占位，但不能阻断通知本体；
- small icon 固定为 `@drawable/ic_stat_dd` 的白色单色 DD 字标，不再使用 launcher 彩色图或旧聊天气泡占位；
- 本地通知点击必须恢复原 `conversationId` 导航；冷启动与已运行状态都要测；
- 开发环境签名头像允许私网 HTTP（仅 Debug manifest 的 cleartext LAN），生产 `IM_PUBLIC_BASE_URL` 仍必须 HTTPS。

历史定向证据：Push/Notification Flutter tests 4/4 PASS，标准 Android Debug APK 曾构建 PASS 并刷新根目录 `DD-Android.apk`。2026-08-12 本轮重新执行 `go test ./...`、全 App `dart analyze --fatal-infos` 与 Flutter 全量测试均通过；下一步继续真机视觉和后台/杀进程通知复测。

状态：

```text
FIXED-PENDING-RETEST
```

## iOS 真人

U30 iOS Push / Notification 自动合同已落代码，但以下均不得用 mock FirebaseMessaging 冒充真机证据：

- 首次 `notDetermined` 才允许触发系统权限请求；拒绝后重启不得重复弹，设置页必须能跳 iOS Notification Settings 恢复；provisional/granted/denied 状态要与系统一致。
- FCM 模式确认 APNs transport token 可用、FCM token 已登记当前 `device_id`；刷新 token 后旧值被 upsert 替换。direct APNs 模式确认 APNs token/refresh 同样可登记。
- 账号 A 登录收 Push；切到 B 前 A endpoint 必须解绑，B token 不得落到 A；退出/远程 revoke/all-device revoke 后旧设备不得继续被 Worker 选为 active endpoint。
- App 前台收到消息只有 DD in-app banner，不得同时出现 iOS system banner；关闭“前台消息预览”后 sender/content 必须泛化。
- App 后台与 killed：direct/group notification 的 title/body/thread/badge 正确；mute 只允许静默 badge update，不得弹/响；`HIDDEN` 不得泄漏 sender、群名、正文或 avatar。
- notification tap 从后台/killed 启动后先恢复有效 session，再进入 conversation/Moments；账号不匹配、session 失效、目标已删除/无权限时安全 fallback，不能绕过 auth。
- badge 实测覆盖 0/1/99/100+；系统仍使用整数 badge，App 内显示模型才使用 `99+`。
- sender avatar 只允许短期授权 URL；过期/失败必须 fallback initials/default icon，不能改回永久公开 URL。

状态：

```text
HUMAN-PENDING
```

## Windows 真人

- App 前台 custom DD notification。
- 头像圆形展示。
- native popup 失败时系统通知 fallback。
- 不因通知造成 hover/窗口卡死。

---

# 17. Theme

真人：

```text
设置 → 外观
→ 跟随系统 / 浅色 / 深色
```

要求：

- 易发现。
- 立即切换。
- 重启保持。
- Desktop Rail/Chat/Sidebar/TitleBar 一致。
- Android Footer/Sheet 一致。

入口当前已落代码，状态：

```text
FIXED-PENDING-RETEST
```

---

# 18. Android Footer

真人：

- 4 tab 快速连续切 20 次。
- 不闪白/闪黑。
- indicator 追随最后选择。
- 只有一个 selected。
- 键盘打开时合理隐藏。
- 60Hz 设备稳定。

当前代码已使用稳定 Stateless tree + RepaintBoundary + AnimatedPositioned indicator，历史“快速切换闪烁”仍需复测。

---

# 19. Windows Native Window

这是当前最高风险真人测试之一。

测试尺寸：

```text
881 × 657
```

DPI：

```text
100%
125%
150%
```

逐项：

- 左/右/上/下 resize。
- 四角斜向 resize。
- 拖动标题栏。
- 双击最大化。
- 最小化：仍在任务栏，可从任务栏恢复。
- 最大化/还原。
- 标题栏 ×：窗口隐藏到系统托盘，`im_client.exe` 仍运行。
- Alt+F4 / taskbar close：与标题栏 × 一致，不直接退出。
- 托盘左键：恢复/前置 DD 主窗口。
- 托盘右键：窗口显示时有“最小化到托盘”，隐藏/最小化时有“打开 DD”，并始终有“退出 DD”。
- 只有托盘“退出 DD”正常结束进程；关闭到托盘后再次双击 DD 启动入口应恢复已有单实例窗口，而不是启动第二个实例。
- Explorer 重启后托盘图标可重新注册；若托盘注册失败，关闭行为至少退化为任务栏最小化，不能形成不可找回的隐藏进程。
- Windows 11 圆角。
- 无灰块/黑角/白边。
- Resize 时 Flutter 内容不闪。

历史真人结果：全部边缘 resize 失败 + 标题栏灰块。

当前 native runner 已增加 child HWND subclass + hit-test 透传 + DPI border + erase background 修复；并新增 `Shell_NotifyIconW` 系统托盘、关闭拦截、托盘恢复/退出和 Explorer `TaskbarCreated` 重注册。Flutter 标题栏关闭按钮 contract test 与 Windows Release build 已通过。

状态：

```text
FIXED-PENDING-RETEST
```

---

# 20. Calls

## 自动

- create call validation。
- self-call rejection。
- busy。
- action authorization。
- invalid state。
- scoped token only after accepted。
- participant membership。
- timeout。

## 真人

Windows ↔ Android：

- 语音互打。
- 视频互打。
- 接听/拒绝/取消/挂断。
- 计时。
- 结束 1 秒退出。
- 通话结果聊天消息。
- 摄像头开/关。
- 横竖屏。
- 网络切 Wi-Fi/蜂窝。
- 弱网。
- 30 分钟。
- TURN relay 强制场景。

P7 Calls 已正式化为 PostgreSQL + `/api/v1/calls`，不再因“实验 API”本身阻断；但公网 TURN/TLS、跨端 30 分钟、弱网切换、API/Worker/LiveKit 重启恢复与真人 UX 未完成前，仍不能标 `HUMAN-PASS / Stable P7`。

---

# 21. OpenAPI Contract Gate

当前已知 drift 是发布阻断项。

必须新增自动测试：

```text
enumerate registered /api/v1 routes+methods
↔
load server/openapi/openapi.json paths+methods
```

允许白名单：

- compatibility legacy routes。
- explicitly experimental `/api/calls`。

其余正式 route 缺失即 CI fail。

---

# 22. 安全测试

每个资源 API：

- no token。
- invalid token。
- revoked device。
- other user's UUID。
- nonexistent UUID。
- blocked relationship。
- over-limit input。
- malformed JSON。
- duplicate/idempotency。

高风险专项：

- Refresh replay。
- Media IDOR。
- Sticker SSRF。
- Telegram token leakage。
- Mention entity forgery。
- SELF conversation member injection。
- Call participant injection。

---

# 23. 性能验收

## 客户端

- 881×657 无布局 overflow。
- Android 键盘动画无明显连续掉帧。
- 4 Tab 连续切换无整条 Footer rebuild flash。
- 大量会话/消息列表滚动稳定。
- 50+ Sticker grid 不一次解码全部动画。

## 服务端

必须做 benchmark/pressure：

- Auth password cost。
- Message send transaction。
- Sync pagination。
- WebSocket fanout。
- Media reservation。
- Mention scan。
- Message search。

不能用单机 localhost 平均值替代公网 P95/P99。

---

# 24. 发布阻断级别

## P0

- 数据丢失。
- Auth bypass。
- 横向读私聊/媒体。
- 消息重复/错发。
- App 无法启动/登录。
- 数据库 migration 破坏数据。

## P1

- 核心平台主链不可用。
- Windows resize/titlebar 完全异常。
- 正常联系人资料大面积不可用。
- 图片/文件/视频/语音主链失败。
- 通知功能与宣称明显不符。

## P2

- 次要 UI 错位。
- 文案/小动画问题。
- 非主路径易用性。

Stable 发布：P0=0，P1=0；P2 有明确已知问题清单和版本计划。

---

# 25. 版本发布 DoD

一个 Release Candidate 至少：

- [ ] Go test 全绿。
- [ ] Flutter analyze 可重复全绿，不再 analysis_server crash。
- [ ] Flutter tests 全绿，包括正确运行的 native video sample gate。
- [ ] Admin build 绿。
- [ ] OpenAPI contract 绿。
- [ ] Windows Release build 绿。
- [ ] Android Release APK 绿。
- [ ] Web Release 绿。
- [ ] 核心人工测试全绿。
- [ ] Windows DPI/resize 绿。
- [ ] Android notification 绿。
- [ ] 真媒体跨端绿。
- [ ] 真实 TURN 网络绿。
- [ ] Backup/restore 演练绿。
- [ ] 正式 tag 对应同一 SHA 的 `ci.yml` 与 full-history `secret-scan.yml` 绿。
- [ ] Trivy dependency + server image HIGH/CRITICAL gate 绿。
- [ ] Release artifacts 的 SHA-256 manifest 校验绿。
- [ ] SPDX SBOM 已生成；GitHub Artifact Attestation / Sigstore provenance 可验证。
- [ ] Android 正式 APK 使用项目所有者证书签名并验证 certificate SHA-256；不得使用 debug key 冒充正式签名。
- [ ] Windows 主程序 Authenticode + RFC3161 timestamp 验证绿并匹配预期 certificate thumbprint。
- [ ] `production-release` GitHub Environment Required reviewers 已实际配置并完成本次审批。
- [ ] 当前正式版本与上一正式版本 artifact/GHCR image 仍可取回用于 U23 rollback。
- [ ] 文档同步。

任何一项打叉，都不能仅通过“改文档写已知问题”把 RC 直接改 Stable。
