# 2026-08-13 U30 iOS 总集成与全量文档同步记录

> 本文是一次性同步记录，不替代 `docs/README.md`、`docs/15-当前实现状态与开发路线.md` 和根目录 `未开发任务.md` 的实时状态。

## 1. 本轮范围

本轮先对仓库全部 `*.md` 做目录级扫描：

- 物理 Markdown 文件共 186 个；
- 排除 `admin/node_modules/**`、Flutter/Firebase build/extracted 第三方 README 后，DD 项目自维护/历史资料共 62 个；
- `归档需求/**`、`docs/16-*`、`docs/17-*` 等带日期的历史需求/同步记录只作为证据检查，不把过去的测试数字和当时状态改写成今天；
- `tasks/*.md` 保留为历史实施计划/Checklist，并在文件头明确它们不再是当前进度事实源；
- 当前实现事实继续以源代码、runtime route、migration、自动测试/构建为最高优先级。

本轮重点追平长期活跃文档中的状态漂移：README、产品总览/PRD、架构、部署、API、测试标准、Auth、E2EE 当前范围、Push ADR、当前路线图、开发进度和未开发任务。

## 2. U30 iOS 当前真实状态

U30 五条并行开发支线已在独立总集成 worktree 汇合并完成公共 native 接线：

- Platform Foundation：Bundle ID `org.openimx.client`，Debug/Profile/Release 统一 iOS 15.0，权限文案、`audio`/`remote-notification` background modes、Push entitlement、Keychain secure-storage contract、`dd://` native route；
- Push/Auth：APNs/FCM permission/token/tap/badge/foreground lifecycle，多账号 Push endpoint lease、Refresh Token rotation、session epoch、safe logout/SESSION_EXPIRED cleanup，服务端专用 `DEVICE_SESSION_REVOKED` 语义；
- Media/File/Camera/QR：Files/Photos path-based picker、system camera、Photos/Files export/share/open/clipboard、大文件 path/stream contract、QR permission/lifecycle/Settings recovery；
- Calls：LiveKit 2.10、CallKit、AVAudioSession、speaker/receiver/Bluetooth route、interruption、camera lifecycle、媒体重连；CallKit system action 使用 server-confirmed two-phase ack；
- Release：Codemagic unsigned Release compile/archive、signed App Store Connect workflow、resolved-native Syft/Trivy fail-closed evidence、GitHub checksum/provenance/attestation/rollback DAG；
- 公共接线：`PushNotificationService`、`FilePickerService`、`CameraCaptureService`、`MediaExportService`、`CallPlatformService` 已加入 Runner Sources 并注册到 `NativeServiceRegistrar`；`AppDelegate` 转发 APNs registration、foreground notification 与 tap，同时继续调用 `FlutterAppDelegate` super chain，避免截断 FlutterFire。

因此当前 U30 状态是：

```text
LOCAL-AUTO-VERIFIED
CLOUD-SECRET-HUMAN-PENDING
```

不能写成 `HUMAN-PASS` 或“iOS 已正式发布”。

## 3. U30 总集成自动证据

2026-08-13 总集成实际执行并通过：

```text
dart analyze --fatal-infos
0 issues

iOS / Push / Media / QR / Calls directed contracts
46 / 46 PASS

Flutter full suite
447 PASS / 5 SKIP / 0 FAIL

go test ./...
PASS

go vet ./...
PASS

PostgreSQL 18.4
33 migrations applied
TestAccountLifecycleWithPostgresAndMailpit PASS
TestPushLifecycleWithPostgres PASS

Release contract
22 / 22 PASS

iOS native scan validator
11 / 11 PASS

aggregate release/native tests
33 / 33 PASS

scripts/test-release-pipeline.ps1
PASS

YAML parse / git diff --check
PASS

full-history Secret Scan
96 / 96 commits scanned
no leaks
broken Git metadata false-green guard PASS
injected credential detector self-test PASS
```

5 个 Flutter skip 仍属于缺少真实 MP4/MOV/WebM/MKV 样本或本机 native runtime 的既有环境型 skip，不应被写成 iOS 真机通过。

U30 合入 `master` 后又触发了一次 Secret Scan 防假绿检查：普通 Git patch log 会省略 merge commit 的 patch，导致 Gitleaks commit count 比 Git reachable count 少 1，脚本因此按设计 fail closed。最终修复为 Gitleaks `--log-opts '--all HEAD --diff-merges=first-parent'`，让 merge commit 也进入 first-parent patch 扫描，同时继续要求扫描计数与 Git 可达提交数严格相等；没有通过降低 ExpectedCommitCount 绕过门禁。

## 4. 仍缺的 iOS 证据

Windows 本机没有 Xcode/iOS SDK，因此以下仍必须由 macOS/Codemagic/真实 Apple 设备完成：

- Codemagic unsigned Release compile + unsigned archive；
- Swift/Xcode 对最终 Runner 的真实编译；
- Apple App ID / Team / certificate / provisioning profile；
- 正式 Firebase iOS `GoogleService-Info.plist`（若使用 FCM）；
- signed IPA、App Store Connect upload、TestFlight processing；
- APNs Sandbox/Production delivery 和 notification tap；
- Camera / Photos / Files / QR 权限与恢复；
- CallKit、receiver/speaker、Bluetooth HFP/A2DP、Siri/蜂窝电话 interruption；
- lock screen/background audio、video camera background suspend/resume；
- Wi-Fi ↔ mobile data 切换和长时 1:1/Group call；
- 多账号 Auth/Keychain/Push lease 真机生命周期。

如果要求 App 已被系统终止或长期后台时仍可靠拉起系统来电，还需要独立 PushKit VoIP/APNs VoIP topic/server/provisioning 闭环；当前没有伪造这项能力已完成。

## 5. 本轮同步掉的主要文档漂移

本轮明确纠正：

- P10 不再写成“只有 `000022_push` migration”；正式 Push domain/API/Worker/provider/client lifecycle 已实现；
- P12 Admin 不再写成“React 工程壳/PLANNED”；独立 Admin identity、MFA/RBAC/CSRF/Governance 已实现；
- Data Rights 不再写成“未来/PLANNED”；`000033_data_rights` 和正式 API/Worker 生命周期已实现；
- P13 不再写成“生产部署/DR/Observability 尚未闭环”；代码闭环已形成，剩生产环境真人证据；
- P11 Production E2EE 从 `PLANNED` 统一为 `OUT-OF-SCOPE FOR V1`，历史 PoC 继续保留；
- Codemagic unsigned contract 从旧 `--debug --no-codesign` 统一为最终 `--release --no-codesign` + unsigned Release archive；
- P14 iOS 不再写成 `PLANNED/未开始/待 AI1 接线`；U30 本地总集成已经完成；
- 旧 `tasks/*.md` 明确标成历史 Checklist，防止后续 AI 按 2026-08-09 的 checkbox 重复开发已经完成的能力。

## 6. 当前下一步

当前真正的主线：

```text
Stop-the-line 修复新的真人回归问题
→ U30 Codemagic/Xcode 云编译
→ 配置 Apple/Firebase iOS Secret/Client config
→ signed IPA / App Store Connect / TestFlight
→ iPhone/iPad 全链真人验收
→ 配置 Android/Windows/iOS production signing + GitHub Environment reviewers
→ 公网 TURN / DR / Observability 生产验收
→ U31 macOS
→ U16 普通用户 MFA / U17 Passkey（后续扩展）
```

P6-P10 和历史 0812 修复仍保留真人多端/弱网/大媒体/视觉回归债务，但这些有代码的项目不能因为尚未真人复测就重新标成 `PLANNED`。
