# 2026-08-11 DD 全量文档同步记录

> 同步时间：2026-08-11 03:36 起
>
> 目的：把 `docs/` 从 2026-08-10 的 P5/R16 视角追平到当前 P6-P10 worktree，避免后续 AI 继续按过时“Group/Moments/QR 都未实现、Calls 仍在内存”的信息开发。

---

# 1. 本次事实源

优先读取/核对：

```text
server/internal/httpapi/server.go
server/internal/httpapi/openapi_contract_test.go
server/openapi/openapi.json
server/migrations/*
server/internal/groups/*
server/internal/calls/*
server/internal/moments/*
server/internal/qrcode/*
clients/app/lib/features/groups/*
clients/app/lib/features/calls/*
clients/app/lib/features/moments/*
clients/app/lib/features/qrcode/*
.github/workflows/ci.yml
```

并重新执行 P9 Flutter analyzer/QR 定向测试以确认当前真实门禁。

---

# 2. 关键状态变化

## P6 Group

旧文档：`PLANNED / schema reserved`。

当前：

```text
IMPLEMENTED + AUTO-VERIFIED
HUMAN-PENDING
```

正式模块、migration、HTTP/OpenAPI、Flutter 与 PostgreSQL lifecycle test 均已存在。

## P7 Calls

旧文档：实验 `/api/calls` + 进程内 CallStore。

当前：

```text
server/internal/calls
PostgreSQL calls
/api/v1/calls
Bearer Principal
multi-device arbitration
```

状态：`IMPLEMENTED + AUTO-VERIFIED / HUMAN-PENDING`。

## P8 Moments

旧文档：`PLANNED`。

当前：Feed、最多 9 图、单视频、点赞评论、单条可见范围、长期关系隐私、Block、私有媒体均已实现并有自动证据。

状态：`IMPLEMENTED + AUTO-VERIFIED / HUMAN-PENDING`。

## P9 QR

旧文档：`PLANNED` 或曾短暂写成“全绿”。

当前必须拆开：

```text
SERVER  = IMPLEMENTED + AUTO-VERIFIED
FLUTTER = IMPLEMENTED + KNOWN-FAILURE
OVERALL = RELEASE-BLOCKED
```

服务端：个人码、群 invite、QR login、nonce hash、短 TTL、扫码设备绑定、确认、一次 consume、事务一致性均已存在。

2026-08-11 03:36 Flutter 当前失败：

- MainShell QR 接线 analyzer error；
- contacts/profile QR 跳转引用未解析；
- `qr_flutter` package language 导致 Widget test 编译失败；
- group redeem fixture 缺字段；
- scanner test debug platform 未恢复。

## P10 Push

当前：`IN-PROGRESS`。

在文档同步期间 worktree 又新增：

```text
000022_push.up.sql
000022_push.down.sql
```

migration 已成对；当前仍未发现正式 `internal/push`、Push API、Worker consumer、FCM/APNs/UnifiedPush adapter 或 Flutter token registration。

因此不得写成 Push 已实现。

---

# 3. 本次更新的主文档

以下主文档均已同步到 2026-08-11：

```text
README.md
00-需求完善总览.md
01-产品需求规格书-PRD.md
02-技术架构与模块设计.md
03-安全隐私与威胁模型.md
04-部署运维与开源交付规范.md
05-API与数据模型草案.md
06-测试验收与发布标准.md
07-P0实时通信PoC.md
08-P0音视频通话PoC.md
09-P0-E2EE候选库评估.md
10-P2账号认证垂直切片.md
11-P3好友关系链.md
12-产品体验与UI功能基线.md
13-2026-08-09-1(1)-体验与能力增量需求.md
14-2026-08-09-登录联系人媒体缓存稳定性增量.md
15-当前实现状态与开发路线.md
```

---

# 4. ADR 同步

现有 canonical ADR 已复核，并修正：

- Go modular monolith 已包含 groups/calls/moments/qrcode；
- Calls 已持久化，不再是 ADR-003/005 中的“未来内存迁移”；
- Outbox 已扩展 Group/Moment/Call terminal event 语义；
- 私有媒体已覆盖 Moments；
- E2EE 仍是 P11 planned。

新增：

```text
ADR-010-二维码采用实例绑定Payload与服务端一次性凭证.md
ADR-011-Push只负责提醒并以Sync为业务真相.md
```

旧重复 ADR 继续保持 `Superseded`，不删除历史路径。

---

# 5. 后续开发驱动

当前唯一主线：

```text
修 P9 Flutter QR KNOWN-FAILURE
→ 完成 P10 Push
→ P11 E2EE
→ P12 Admin / Abuse / Data Rights
→ P13 Production Self-host
→ P14 iOS/macOS
```

P6/P7/P8 不再当“待开发模块”，只保留真人/弱网/规模验收。

---

# 6. 文档防漂移要求

后续任何 AI：

1. 开工读 `README.md` + `15-当前实现状态与开发路线.md`。
2. runtime/migration/test 与 docs 冲突时先按代码事实核对。
3. 正式 route 变化同步 OpenAPI + contract test + `05`。
4. migration 变化同步 `04/05/15`。
5. 新安全模型同步 `03` + ADR。
6. 用户可见功能门禁变化同步 `06/12/15`。
7. `KNOWN-FAILURE` 未清零前不能沿用旧 `AUTO-VERIFIED`。
8. 真人未复测不能写 `HUMAN-PASS`。
