# ADR-005（旧文件名）：首版单实例，后续再做联邦

## 状态

Superseded

## 更新时间

2026-08-11

## 被替代为

请使用当前 canonical ADR：

```text
docs/decisions/ADR-005-首版单实例优先再扩展集群.md
```

## 说明

早期文档把“联邦”与“水平扩展”混在同一个阶段描述中。当前代码和产品路线已经明确：

- 第一优先级是单实例自托管可靠。
- 之后按指标扩 API/Worker/数据库/RTC 容量。
- Redis 只用于 realtime hint。
- 当前并没有实现跨实例用户发现、跨实例消息寻址、跨实例信任与联邦协议。

因此“联邦”不属于当前已接受的扩容设计；未来如果 DD 真正开发跨实例联邦，必须单独写新的 ADR 和威胁模型。
