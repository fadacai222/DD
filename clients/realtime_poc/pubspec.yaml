name: realtime_poc
version: 0.1.0
publish_to: none
description: Reusable Dart realtime client PoC for the self-hosted IM project.

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  http: ^1.6.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  test: ^1.31.2
