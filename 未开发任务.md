# DD 未开发任务

> 核对时间：2026-08-12 09:xx（总集成：U06/U07/U08/U10/U11/U12/U20-U24 已合并验证）
> 项目：`C:\Users\admin\Desktop\复刻微信`
> 本文件只列 **仍缺代码、仍缺正式产品闭环或当前 CI/生产能力仍明确缺失** 的任务。

`IMPLEMENTED` / `AUTO-VERIFIED` / `FIXED-PENDING-RETEST` / `HUMAN-PENDING` 但代码已经存在的功能，不重复列成“未开发”。真人验收债务单独放在文末。

## 1. 当前优先未开发 / 未闭环

当前 P1 **代码**闭环已没有 U25 缺口：U25 Release provenance / 安全发布已在当前 worktree 实现，并有可重复 release contract、实际 Trivy/Syft/Server image 与 Web/Windows/Android build 验证。真实 Android/Windows signing material、GitHub `production-release` Required reviewers 以及生产环境真人验证仍属于 `SECRET/HUMAN-PENDING`，统一放在第 7 节，不伪装为已经完成的生产签名/审批。

> 已从本清单移除：U01“Flutter analyzer 门禁”、U06“CI 独立 Secret Scan”、U07“Moments/QR PostgreSQL CI integration”、U08“生产级 Push provider 运维代码闭环”、U10/U11“Admin / Governance”、U12“Data Rights”、U20-U23“Production Self-host 主链”、U24“Observability / Alerting”，以及旧 U02/U03/U05。上述项目均已有代码与自动证据；真实 FCM/APNs、公网 TURN/跨运营商、生产规模 RPO/RTO 等仍保持 `HUMAN-PENDING`。

## 2. P12：Admin / Abuse / Data Rights

U10/U11/U12 已完成并通过总集成自动验证：独立 Admin identity/session、独立 `ADMIN_SECURITY_SECRET`、TOTP MFA、Recovery Code、RBAC、CSRF、治理/举报/审计、普通 Bearer 拒绝，以及用户导出/注销生命周期均已落地。当前 P12 仅保留 P2 扩展项：

| ID | 优先级 | 状态 | 模块 | 当前代码事实 | 完成标准 |
|---|---:|---|---|---|---|
| U16 | P2 | `PLANNED` | 普通用户 TOTP/MFA | Admin MFA 已独立实现，但普通用户主产品尚无完整 MFA 闭环 | 开启/绑定/验证/恢复码/关闭/丢失恢复/敏感操作二次验证全链 |
| U17 | P2 | `PLANNED` | Passkey / WebAuthn | 当前未实现 | 注册、登录、challenge、credential 管理、撤销、跨端兼容和恢复策略完整 |

## 3. P13：Production Self-host

U20-U24 主链已完成并通过总集成自动验证：Production Compose、HTTPS/WSS/TLS SNI、LiveKit embedded TURN、备份/恢复、forward-only upgrade/rollback、Prometheus/Grafana/blackbox/node-exporter overlay、19 条告警均已落地。最终 `upgrade-compat-drill` 已真实验证 `000030 → 000033`，旧版本对新 schema 不兼容时必须恢复 verified pre-upgrade backup，禁止数据库 down migration。

| ID | 优先级 | 状态 | 模块 | 当前代码事实 | 完成标准 |
|---|---:|---|---|---|---|
| U25 | P1 | `IMPLEMENTED + AUTO-VERIFIED / SECRET-HUMAN-REQUIRED` | Release provenance / 安全发布 | tag-only SemVer release、exact-SHA CI/Secret Scan gate、四平台 versioned artifact、SHA-256、SPDX SBOM、Trivy dependency/image gate、Sigstore/GitHub Attestation、Android/Windows production signing contract、Environment approval 与 rollback retention 已落地 | 代码闭环完成；项目所有者仍需提供真实 JKS/PFX 与 certificate identity，并在 GitHub `production-release` Environment 配置 Required reviewers 后才能形成真实 production release 证据 |

## 4. P14：iOS / macOS 正式交付

| ID | 优先级 | 状态 | 模块 | 当前代码事实 | 完成标准 |
|---|---:|---|---|---|---|
| U30 | P1 | `IN-PROGRESS` | iOS 正式客户端 | Runner Platform Foundation 已统一到 iOS 15.0（Debug/Profile/Release），Bundle ID `org.openimx.client`，具备 iPhone/iPad、权限文案、audio/remote-notification background modes、Push entitlement、Keychain secure-storage 合同、统一 native service registrar 与 `dd://` lifecycle routing；iOS Push/Notification 代码闭环已完成 permission/query/settings recovery、APNs/FCM token refresh、active-account endpoint lease、foreground 去重、background/killed system notification contract、conversation/Moments tap routing、server/local unread badge、mute badge-only、HIDDEN/avatar privacy；`PushNotificationService.swift` 已落地但公共 AppDelegate/registrar/Xcode hook 尚待本轮总集成接线，真 APNs/FCM delivery 仍无真机证据 | 完成本轮总集成 native hook 与 Codemagic/macOS 编译验证，再完成媒体/QR、LiveKit/CallKit、正式签名/TestFlight CI 与真机 Release 验收；Apple/Firebase signing/client config 仍属 HUMAN/SECRET-REQUIRED |
| U31 | P1 | `PLANNED` | macOS 正式客户端 | `clients/app/` 当前没有 `macos/` 平台目录 | macOS project、签名、公证、窗口/通知/文件/通话/媒体、CI、Release 真人验收 |

> `clients/app/` 当前同样没有 Linux 正式平台目录。如果未来把 Linux 宣传为支持平台，也必须单独建立平台工程、构建和真人验收，不能只引用 Flutter 理论支持。

## 5. 扩展能力 backlog（当前不阻塞 V1）

| ID | 状态 | 能力 | 说明 |
|---|---|---|---|
| F01 | `PLANNED` | 地图 / 定位消息 | 位置权限、发送、地图查看、隐私和跨平台地图供应商策略 |
| F02 | `PLANNED` | 定时发送 | 服务端可靠调度、时区、离线、取消/编辑语义 |
| F03 | `PLANNED` | 频道 | 广播、订阅、管理员、历史、通知与大规模扇出 |
| F04 | `PLANNED` | 多语言 i18n | 文案资源化、语言切换、日期/数字格式、RTL |
| F05 | `PLANNED` | 全服务器搜索 | 必须先定义权限/索引/管理员与普通用户边界，不能绕过会话可见性 |

## 6. 已有代码，不准重复从零开发

以下能力在当前 worktree 已经存在：

- P6 Group 正式 PostgreSQL domain/API/Flutter 主链。
- P7 Formal Calls PostgreSQL 状态机和 `/api/v1/calls`。
- P8 Moments Feed/媒体/隐私/点赞评论回复/持久互动未读/互动焦点。
- P9 个人码/群二维码/扫码登录。
- P10 Push domain/API/durable job/Worker/FCM/APNs/UnifiedPush/Flutter token；provider 运维指标、认证失败/重试分类、失效 endpoint 30 天保留后分批清理与生产 runbook 已补齐。
- U10/U11 Admin/Governance：独立 Admin auth + `ADMIN_SECURITY_SECRET`、TOTP MFA、Recovery Code、RBAC、Session/CSRF、用户治理/举报/审计，普通用户 Bearer 无法进入 Admin API。
- U12 Data Rights：数据导出、短时授权下载、冷静期注销、取消/重试、Token/Device/Push 撤销、共享数据匿名化/对象删除 Worker 生命周期。
- U20-U23 Production Self-host：`infra/prod/` Production Compose、Caddy/HAProxy、LiveKit embedded TURN、MinIO/external S3、DR backup/restore、升级/回滚/compatibility drill。
- U24 Observability：Prometheus-compatible API/Worker/DB/Redis/WebSocket/Realtime/Outbox/Push/S3/SMTP/runtime 指标、LiveKit/TURN listener blackbox、node-exporter disk 指标、Grafana datasource 与带 `for` 的 19 条告警规则已落地在 `server/internal/observability` 与 `infra/observability/`。
- U25 Release provenance：`.github/workflows/release.yml`、`release/` contract、SemVer/tag/changelog、exact-SHA upstream gate、Trivy/Syft、SHA-256、Sigstore/GitHub Attestation、原生 production signing Secret contract、GitHub Release 与 rollback retention 已落地；未提供真实签名 Secret 时正式发布会 fail closed。
- Android data-only FCM、自定义通知 sender avatar、DD small icon、点击会话恢复代码。
- Telegram Sticker Relay 静态 WebP/PNG、动态 TGS、视频 WebM。
- Telegram Pack 4 秒静默账号同步、正式 `STICKER_PACK` 分享首图、转发保留导入能力。
- 自定义 Sticker PNG/WebP/GIF/MP4/WebM，64 MiB 上限。
- 视频 Sticker `VideoFileCache` 持久缓存、LRU/年龄约束、超时失败态。
- Android 大文件原生 `ACTION_OPEN_DOCUMENT` + 64 KiB 流式缓存选择链。
- 性能设置页：节能、减少动画、视频自动预览、硬件加速解码。
- Windows 系统托盘、联系人详情、Mention 悬浮资料窗、群聊目录分栏。
- Android 二级菜单外部 dismiss、文件打开/分享/APK 安装器链。
- 消息流内媒体上传圆环进度/取消。
- Sticker 与普通图片展示尺寸区分，收到的 Sticker 可加入我的表情。
- 朋友圈视频 poster/可视区自动预览、互动红色数字角标。

这些能力如果真人失败，应新建/更新 `KNOWN-FAILURE`，先修真正根因，不要因为用户再次反馈就把整项错误地标回 `PLANNED`。

## 7. 仅待真人验收，不计入“未开发”

1. Push 真 provider：Android FCM 前台/后台/系统杀进程下的真实 sender avatar、DD 小标、点击恢复；iPhone APNs Sandbox/Production delivery；FCM/APNs credential rotation 真环境演练。服务端当前只能证明 provider API accepted/failed，不能证明设备 delivery/click。
2. Android 大视频 OOM 修复：约 100 MiB / 500 MiB 选择、poster、上传、取消/重试。
3. Telegram Pack：Windows ↔ Android 同账号自动同步、移除/重登/重启一致性。
4. Telegram 真实公开 TGS/WebM Pack 播放与整包分享首图。
5. 视频 Sticker 关闭面板再打开、本地缓存命中、滚动可见性/暂停。
6. Windows 托盘、Alt+F4、Explorer 重启、右键退出、125%/150% DPI、Resize。
7. P6 群聊多端/500 人边界/弱网/重启。
8. P7 公网 TURN/TLS、30 分钟语音/视频、Wi-Fi ↔ 移动网络切换。
9. P8 真媒体连续发布、朋友圈角标/互动焦点多设备一致性。
10. P9 Android 相机扫码、群二维码过期/撤销/次数耗尽、PC/Web 扫码登录。
11. Android Markdown/普通文件打开分享、APK 系统安装器链。
12. 全局 SnackBar/Dialog/Input、Footer、联系人/群聊等本轮真实视觉。
13. Production Self-host：公网 DNS/证书续期、UDP 443、ICE/TCP 7881、RTC UDP range、跨运营商/移动网络、30 分钟真实通话。
14. DR：用生产规模数据库/对象存储和独立恢复主机实测 RPO/RTO；本地 destroy→fresh-volume restore 已自动 PASS。
15. Production Observability：真实告警接收方、磁盘/资源阈值、LiveKit/TURN 公网故障演练；Compose 内 listener/配置验证不能替代公网链路。
16. Admin/Data Rights：正式生产管理员初始化、MFA 恢复码保管、真实用户导出下载与冷静期注销产品验收。
17. U25 正式发布环境：提供真实 Android JKS/Windows PFX 与固定证书 identity；配置 `release-signing` / `production-release` Environments，后者启用 Required reviewers，并执行一次真实 tag release 验证 GitHub Release/GHCR/Sigstore/Artifact Attestation 全链。

## 8. E2EE 说明

Production E2EE 当前确实没有开发进主产品，但这是 **2026-08-11 已明确的 V1 产品决策**，因此不列入 V1 未开发清单。`crypto/e2ee_poc` 仅保留研究档案，当前 CI 已移除 E2EE PoC job。

未来重新引入 E2EE 必须重新立项、重新做协议/多设备/群组/备份/密钥恢复与安全评审，不能默认恢复旧 PoC。
