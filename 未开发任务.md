# DD 未开发任务

> 核对时间：2026-08-12 06:xx
> 项目：`C:\Users\admin\Desktop\复刻微信`
> 本文件只列 **仍缺代码、仍缺正式产品闭环或当前 CI/生产能力仍明确缺失** 的任务。

`IMPLEMENTED` / `AUTO-VERIFIED` / `FIXED-PENDING-RETEST` / `HUMAN-PENDING` 但代码已经存在的功能，不重复列成“未开发”。真人验收债务单独放在文末。

## 1. 当前优先未开发 / 未闭环

| ID | 优先级 | 状态 | 任务 | 当前代码事实 | 完成标准 |
|---|---:|---|---|---|---|
| U07 | P1 | `PARTIAL` | Moments / QR 指定 PostgreSQL CI integration | CI 已显式覆盖 Auth/Contacts/Groups/Calls/Messaging/Push PostgreSQL integration，但 Moments/QR 仍没有独立可见步骤；本机未配置专用 DB URL 时对应测试可能 SKIP | CI 使用临时 PostgreSQL 显式运行 Moments/QR lifecycle/migration；不得依赖开发机环境变量；失败阻断合并 |
| U08 | P1 | `PARTIAL` | 生产级 Push 真 provider 运维闭环 | FCM/APNs/UnifiedPush provider、Worker、retry/token invalid 代码已存在，Android Firebase 也已接入；但生产凭据轮换、监控、失败告警、设备级 delivery/click 证据尚未形成发布闭环 | provider secret 管理/轮换、job backlog/失败指标、告警、失效 token 比例、真实 Android/iOS 设备交付记录和故障排查手册齐全 |

> 已从本清单移除：U01“Flutter analyzer 门禁”（已恢复 0 issue + 全量测试通过）、U06“CI 独立 Secret Scan”（`.github/workflows/secret-scan.yml` + `.gitleaks.toml` + `scripts/test-secret-scan.ps1` 已落地），以及旧 U02“性能设置页”、U03“Telegram Pack Android 多端一致性代码”、U05“Telegram TGS/WebM 支持”。当前代码已经存在对应实现，它们不再属于“未开发”。

## 2. P12：Admin / Abuse / Data Rights

| ID | 优先级 | 状态 | 模块 | 当前代码事实 | 完成标准 |
|---|---:|---|---|---|---|
| U10 | P1 | `PLANNED / FOUNDATION-ONLY` | 独立管理员认证 + MFA + RBAC | `admin/src/pages/LoginPage.tsx` 仍明确提示“正式认证尚未接入”，表单不会把密码发往服务端 | 独立 admin identity/session；强制 MFA；RBAC；会话撤销/超时；服务端强制鉴权；普通用户 token 不能访问管理 API |
| U11 | P1 | `PLANNED` | 用户治理 / 举报 / Abuse / 审计后台 | 当前只有部分 Auth audit，不是完整治理链 | 举报队列、用户状态、处置动作、风控/限流、管理员审计、配置变更历史、最小权限操作闭环 |
| U12 | P1 | `PLANNED` | 数据导出 / 账号注销 | 当前没有完整普通用户数据权利产品链 | 用户可申请导出和注销；导出范围明确；对象存储、消息、设备、Push endpoint、会话等生命周期明确；状态可追踪；高风险操作有二次确认/冷静期策略 |
| U16 | P2 | `PLANNED` | 普通用户 TOTP/MFA | 文档有安全目标，主产品尚无完整闭环 | 开启/绑定/验证/恢复码/关闭/丢失恢复/敏感操作二次验证全链 |
| U17 | P2 | `PLANNED` | Passkey / WebAuthn | 当前未实现 | 注册、登录、challenge、credential 管理、撤销、跨端兼容和恢复策略完整 |

## 3. P13：Production Self-host

| ID | 优先级 | 状态 | 模块 | 当前代码事实 | 完成标准 |
|---|---:|---|---|---|---|
| U20 | P1 | `PARTIAL INFRA` | 正式生产部署形态 | `infra/` 当前只有 `infra/dev/compose.yml` 与开发 `.env` | 提供生产 Compose/Helm 中至少一套可复现方案；HTTPS/WSS、域名、证书、反代、滚动/受控重启和健康检查完整 |
| U21 | P1 | `PLANNED` | TURN/TLS 与公网通话生产化 | LiveKit/TURN PoC 已有，生产公网 TURN/TLS/跨运营商证据不完整 | TCP/TLS/UDP fallback、NAT、防火墙端口、带宽、跨运营商/移动网络验证及故障诊断文档 |
| U22 | P1 | `PLANNED` | Backup / Restore / DR | 当前没有形成可发布的数据库+对象存储恢复演练 | PostgreSQL + S3/MinIO 备份、恢复、RPO/RTO、灾难恢复演练和恢复校验 |
| U23 | P1 | `PLANNED` | Upgrade / Rollback / Migration compatibility | migration 已坚持 forward-only，但没有完整版本升级/回滚演练 | N→N+1 升级、兼容窗口、失败回滚、不可逆 migration 策略、客户端/服务端版本兼容矩阵 |
| U24 | P1 | `PARTIAL` | Observability / Alerting | 已有 slog、requestId、health/live/ready 等基础 | metrics、API latency/error、DB pool、Outbox/Push backlog、WebSocket、对象存储、SMTP、LiveKit、磁盘/cache 指标与告警 |
| U25 | P1 | `PARTIAL` | Release provenance / 安全发布 | 当前已有 CI artifact 与独立 Secret Scan，但没有完整签名/来源/发布流程闭环 | artifact checksum/signing、SBOM/依赖扫描、版本号/tag/changelog、回滚包与发布审批；现有 Secret Scan 必须持续作为发布阻断门禁 |

## 4. P14：iOS / macOS 正式交付

| ID | 优先级 | 状态 | 模块 | 当前代码事实 | 完成标准 |
|---|---:|---|---|---|---|
| U30 | P1 | `PLANNED` | iOS 正式客户端 | `clients/app/` 当前没有 `ios/` 平台目录 | iOS project、签名、权限、Secure Storage、媒体、相机/QR、LiveKit、APNs/Push、后台模式、CI、真机 Release 验收 |
| U31 | P1 | `PLANNED` | macOS 正式客户端 | `clients/app/` 当前没有 `macos/` 平台目录 | macOS project、签名、公证、窗口/通知/文件/通话/媒体、CI、Release 真人验收 |

> `clients/app/` 当前同样没有 Linux 正式平台目录。如果未来把 Linux 宣传为支持平台，也必须单独建立平台工程、构建和真人验收，不能只引用 Flutter 理论支持。

## 5. 扩展能力 backlog（当前不阻塞 V1）

| ID | 状态 | 能力 | 说明 |
|---|---|---|---|
| F01 | `PLANNED` | 地图 / 定位消息 | 位置权限、发送、地图查看、隐私和跨平台地图供应商策略 |
| F02 | `PLANNED` | 定时发送 | 服务端可靠调度、时区、离线、取消/编辑语义 |
| F03 | `PLANNED` | 频道 | 广播、订阅、管理员、历史、通知与大规模扇出 |
| F04 | `PLANNED` | 多语言 i18n | 文案资源化、语言切换、日期/数字格式、RTL |
| F05 | `PLANNED` | 全服务器搜索 | 必须先定义权限/索引/管理员与普通用户边界，不能绕过会话可见性 |

## 6. 已有代码，不准重复从零开发

以下能力在当前 worktree 已经存在：

- P6 Group 正式 PostgreSQL domain/API/Flutter 主链。
- P7 Formal Calls PostgreSQL 状态机和 `/api/v1/calls`。
- P8 Moments Feed/媒体/隐私/点赞评论回复/持久互动未读/互动焦点。
- P9 个人码/群二维码/扫码登录。
- P10 Push domain/API/durable job/Worker/FCM/APNs/UnifiedPush/Flutter token。
- Android data-only FCM、自定义通知 sender avatar、DD small icon、点击会话恢复代码。
- Telegram Sticker Relay 静态 WebP/PNG、动态 TGS、视频 WebM。
- Telegram Pack 4 秒静默账号同步、正式 `STICKER_PACK` 分享首图、转发保留导入能力。
- 自定义 Sticker PNG/WebP/GIF/MP4/WebM，64 MiB 上限。
- 视频 Sticker `VideoFileCache` 持久缓存、LRU/年龄约束、超时失败态。
- Android 大文件原生 `ACTION_OPEN_DOCUMENT` + 64 KiB 流式缓存选择链。
- 性能设置页：节能、减少动画、视频自动预览、硬件加速解码。
- Windows 系统托盘、联系人详情、Mention 悬浮资料窗、群聊目录分栏。
- Android 二级菜单外部 dismiss、文件打开/分享/APK 安装器链。
- 消息流内媒体上传圆环进度/取消。
- Sticker 与普通图片展示尺寸区分，收到的 Sticker 可加入我的表情。
- 朋友圈视频 poster/可视区自动预览、互动红色数字角标。

这些能力如果真人失败，应新建/更新 `KNOWN-FAILURE`，先修真正根因，不要因为用户再次反馈就把整项错误地标回 `PLANNED`。

## 7. 仅待真人验收，不计入“未开发”

1. Android Push：前台、后台、系统杀进程下的真实 sender avatar、DD 小标、点击恢复。
2. Android 大视频 OOM 修复：约 100 MiB / 500 MiB 选择、poster、上传、取消/重试。
3. Telegram Pack：Windows ↔ Android 同账号自动同步、移除/重登/重启一致性。
4. Telegram 真实公开 TGS/WebM Pack 播放与整包分享首图。
5. 视频 Sticker 关闭面板再打开、本地缓存命中、滚动可见性/暂停。
6. Windows 托盘、Alt+F4、Explorer 重启、右键退出、125%/150% DPI、Resize。
7. P6 群聊多端/500 人边界/弱网/重启。
8. P7 公网 TURN/TLS、30 分钟语音/视频、Wi-Fi ↔ 移动网络切换。
9. P8 真媒体连续发布、朋友圈角标/互动焦点多设备一致性。
10. P9 Android 相机扫码、群二维码过期/撤销/次数耗尽、PC/Web 扫码登录。
11. Android Markdown/普通文件打开分享、APK 系统安装器链。
12. 全局 SnackBar/Dialog/Input、Footer、联系人/群聊等本轮真实视觉。

## 8. E2EE 说明

Production E2EE 当前确实没有开发进主产品，但这是 **2026-08-11 已明确的 V1 产品决策**，因此不列入 V1 未开发清单。`crypto/e2ee_poc` 仅保留研究档案，当前 CI 已移除 E2EE PoC job。

未来重新引入 E2EE 必须重新立项、重新做协议/多设备/群组/备份/密钥恢复与安全评审，不能默认恢复旧 PoC。
